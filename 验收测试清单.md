# 《喵星破晓》（Dawn for Meow）—— 验收测试清单

> **文档定位**：把 WBS 里的验收标准落成**可执行的测试用例**（前置 / 步骤 / 期望三列），重点覆盖边界与异常。
> 使用方式：每完成一个模块，先跑本清单对应小节；提测前跑全量。
> **自动化落地**：模块 A / B / C 的引擎级与接口级用例已在 `backend/tests/` 落地（`python -m pytest -p no:cacheprovider`）；
> 真机 MySQL 用 `$env:DWM_TEST_MYSQL_URL="mysql+aiomysql://root:root@127.0.0.1:3306/dawn_meow?charset=utf8mb4"` 后跑 `tests/test_mysql_integration.py`。

**编制日期**：2026-09-12　|　**版本**：v1.0（随模块开发补充）

---

## 模块 A：时钟与离线结算

| # | 前置 | 步骤 | 期望 |
| :--- | :--- | :--- | :--- |
| A-1 | 存档正常，猫薄荷有盈余 | 关闭页面 1 小时后重新进入 | 弹出《离线休整报表》，资源量 = 按《数值平衡表》§3 公式计算值（误差 < 0.1%），`last_tick_time` 刷新为现在 |
| A-2 | 猫薄荷存量低于消耗 | 离线期间发生断粮 | 报表标注断粮；科研/拾荒产出按"断粮时刻"截断；繁育进度按 −5%/分钟倒扣；农夫保留 30% 求生产出 |
| A-3 | 资源接近上限 | 离线 1 小时 | 产出严格裁剪在仓储上限，超出部分丢弃并在报表标注"爆仓" |
| A-4 | 任意状态 | Δt = 0 时读档 | 不产生任何资源变化，报表显示"无离线收益" |
| A-5 | 任意状态 | 把系统时间往回调后再读档 | `last_tick_time` 被重置为当前时间并记日志，**不做补偿**，不出现负数资源 |
| A-6 | 跨午夜场景 | 在 23:50 离线、次日 00:20 上线 | 结算正确跨越日期边界，无重复或漏算 |

## 模块 B：资源流转与断粮

| # | 前置 | 步骤 | 期望 |
| :--- | :--- | :--- | :--- |
| B-1 | 任意 | 猫薄荷归零 | 科研归 0、拾荒归 0、踩轮归 0、农夫保留 30%；界面出现罢工提示 |
| B-2 | 处于断粮 | 猫薄荷回正 | 立即全面复工，无残留惩罚 |
| B-3 | 电力净值为负 | 观察科研产出 | 图灵终端断电、科研产出归 0；恢复供电后自动复工 |

## 模块 C：人口与繁育

| # | 前置 | 步骤 | 期望 |
| :--- | :--- | :--- | :--- |
| C-1 | N=1、K=2、饱食 | 静置 | 约 200 秒（±10%）后猫口 +1，进度池不丢小数（不出现"每 tick 截断导致永远不涨"） |
| C-2 | N 接近 K | 静置 1 小时 | 猫口不超过 K，繁育速率平滑趋 0 |
| C-3 | 处于断粮 | 静置 10 分钟 | 繁育进度按每分钟 −5% 倒退，不低于 0 |
| C-4 | 任意 | 新增猫口 | 新猫一律进入空闲待命池，不自动上工 |
| C-5 | 任意 | 拆除纸箱窝使 K < N | 不出现负猫口；给出明确提示与处理规则（不崩） |

## 模块 D：设施建造与工位

| # | 前置 | 步骤 | 期望 |
| :--- | :--- | :--- | :--- |
| D-1 | 废铁足够 | 建造第 n 座设施 | 扣费 = 基础造价 × 递增系数^(n-1)（与《数值平衡表》§5 完全一致） |
| D-2 | 已达工位上限 | 继续点 `[+]` | 按钮置灰，接口返回 `400 WORKSTATION_LIMIT_EXCEEDED` |
| D-3 | 添加新设施到 `facilities.json` | 重启后端 | 新设施自动出现在建造面板，**无需改表结构** |
| D-4 | 空档口、手上有 5 废铁 | 建造第一座【瓦楞纸箱窝】 | 总猫口 0 → 1（第一只折耳猫入驻）、承载力 K = 1、空闲待命 1，返回 `narrative` 温情文案 |
| D-5 | 废铁不足 | 建造【水培农田】 | 接口返回 `400 INSUFFICIENT_RESOURCE`，材料不扣除 |
| D-6 | 已建 1 座蓄电池 | 再次建造蓄电池 | 返回 `409 FACILITY_MAX_LEVEL`（唯一建筑，上限 1 级） |
| D-7 | 任意 | 建造【火箭垂直发射井】 | 返回 `400 BAD_REQUEST`（造价待定稿，本轮只有等级 0 占位行） |
| D-8 | 无拾荒猫在岗 | 反复手点废墟 | 每次 +1 废铁；累计 40 次后报 `400 COLD_START_EXHAUSTED`；有拾荒猫上工后报 `400 COLD_START_FINISHED` |

## 模块 E：科技树

| # | 前置 | 步骤 | 期望 |
| :--- | :--- | :--- | :--- |
| E-1 | 前置未解锁 | 请求研发后续节点 | 拒绝并返回 `400 BAD_REQUEST`（DAG 校验生效） |
| E-2 | 研发中 | 观察进度 | 算力按极客猫产出累加，科研等级折扣按 8 折 / 6 折生效 |
| E-3 | 手动重 Roll | 点重 Roll | 扣除 `target_cost` 的 5%（最低 50 算力），产出新卡且**原有进度不丢** |
| E-4 | LLM 连续失败 2 次 | 触发兜底 | 自动从 `fallback_techs.json` 抽卡，玩家零感知 |

## 模块 F：警戒度与安防

| # | 前置 | 步骤 | 期望 |
| :--- | :--- | :--- | :--- |
| F-1 | 开局 2~3 座设施 | 挂机 6 分钟 | 警戒度约 +3 点（0.005 + 设施 0.002 点/s 口径） |
| F-2 | 警戒度 = 100 | 库存有诱饵 | 自动弹射诱饵：警戒度 −30 点并进入 **5 分钟误报冷却**，冷却期内不再触发满值判定 |
| F-3 | 无诱饵、有闲置载具 | 警戒度 = 100 | 自动派车迎击；胜则清警报并缴获，败则车损、乘员进急救舱（**无猫伤亡**） |
| F-4 | 无诱饵无载具 | 警戒度 = 100 | 触发静默关灯：全员停工，警戒度 ×5 速率衰减到 20 后自动复工，**全程零弹窗** |
| F-5 | rage 累积 | 连续截杀 4 波 | rage 满 100 → 触发轨道轰炸 → 资源 −75%、设施变受损被埋、猫口降到 3、其余标记走散 |
| F-6 | 轰炸后 | 等待 | rage 归零 + 10 分钟和平期；修复仅需 50% 材料；走散猫猫每 15 秒归队 2 只 |

## 模块 G：载具、远征与战斗

| # | 前置 | 步骤 | 期望 |
| :--- | :--- | :--- | :--- |
| G-1 | 机位已满 | 组装新车 | 拒绝并给出明确提示（机位上限 12 / 星际 24） |
| G-2 | 载具出战 | 观察乘员池 | 每辆出库载具锁 2 只乘员猫；返库后归还待命池 |
| G-3 | 交火中 | 施加破甲武器 | 命中按 `min(75%, 装甲 ÷ 400)` 减伤，并**削蚀装甲上限**（跨战斗持久化） |
| G-4 | 结构归零 | 观察 | 100% 触发紧急弹射，乘员进急救舱；车辆进入 `REPAIR`，维修消耗原造价 50% |
| G-5 | 远征进行中 | 关闭游戏 1 小时 | 回来后远征已完成（按 `ends_at` 判定），可收取战利品 |
| G-6 | 交火中 | 点 EMP / 超频 | 扣减蓄电池 kWh（15 / 20），电量不足时报 `400 INSUFFICIENT_RESOURCE` |

## 模块 H：水培实验室

| # | 前置 | 步骤 | 期望 |
| :--- | :--- | :--- | :--- |
| H-1 | 新存档 | 查看格盘 | 固定 7×7 场地，仅中央 3×3 = 9 格可播种 |
| H-2 | 连续扩建 | 每次扩建 | 每次只解锁 **1 格**，上限 49 格 |
| H-3 | 相邻成熟 + 有空格 | 静置 | 每 30 秒判定一次突变（基础 8%），无空格则不触发 |
| H-4 | 开启机械臂托管 | 刷出未知突变幼苗 | 绝不误铲，并高亮闪烁提醒 |
| H-5 | 放射性催化液 + 成熟未收割 | 静置 120 秒 | 作物枯萎（高风险高回报生效） |
| H-6 | 荧光苔藓在田 | 观察电力 | 每株 +5 kW 实时生效 |

## 模块 I：智械深网

| # | 前置 | 步骤 | 期望 |
| :--- | :--- | :--- | :--- |
| I-1 | 持有做空合约 | 挂机到合约到期 | 自动按市价强制买回交割，利息按 0.1%/分钟结算 |
| I-2 | 深网界面 | 连续发帖 | 10 分钟内第二次发帖被拒绝（冷却生效） |
| I-3 | 大额交易 | 下单 | 固定手续费 + 点差扣费；趋势强度按"每 100 股收缩 0.006"反噬，影响力封顶 |
| I-4 | 论坛帖子 | 查看 | **不显示 IP、不显示网段**，只显示随机代号 |
| I-5 | 真/假料混合 | 用三条线索判断 | 命中率显著高于瞎猜，但**永远无法 100% 确认** |
| I-6 | 暴露度 75%~99% | 触发追踪信标 | 地表警戒度 +40，60 秒内可派车拆除，超时后果生效 |
| I-7 | 暴露度 100% | 触发 | 封号 15 分钟 + 空袭扣设施耐久 |

## 模块 J：LLM 与防火墙

| # | 前置 | 步骤 | 期望 |
| :--- | :--- | :--- | :--- |
| J-1 | 断网状态 | 触发新行星登录 | 三道防火墙兜底生效，游戏流程不中断 |
| J-2 | 单日调用超预算 | 继续触发 | 自动降级到本地语料池，日志记录降级原因 |
| J-3 | 首次杂交出新图鉴 | 触发 | 调用一次 LLM 生成《异星植物学图鉴》论文，结果写入图鉴（全案最多 25~30 次） |

## 模块 N：存档

| # | 前置 | 步骤 | 期望 |
| :--- | :--- | :--- | :--- |
| N-1 | 槽位 1 有进度 | 在槽位 2 新开一局 | 两个槽位数据完全隔离，互不污染 |
| N-2 | 任意 | 导出 Base64 后导入 | 校验和一致可导入；篡改字符串后被拒绝 |
| N-3 | 旧版本存档 | 导入 | 走版本迁移函数，无脏数据死锁 |
| N-4 | 任意 | 观察快照 | 每 15 秒静默存盘，无 Loading 遮罩、无打断；网络异常时重试 |
| N-5 | 前端人为改大资源值 | 等下一次快照 | 后端按自己的公式重算覆盖，前端数值不影响结果，并记录对账 warning |

## 模块 O：小游戏

| # | 前置 | 步骤 | 期望 |
| :--- | :--- | :--- | :--- |
| O-1 | 密电译码当日次数用尽 | 再次进入 | 提示配额用尽，次日恢复 |
| O-2 | 矿脉扫描配额 5 | 挂机 30 分钟 | 恢复 1 次配额，不超过 5 |
| O-3 | 熔炉配比找到 10 条配方 | 继续投料 | 熔炼速度加成封顶 +50%，不再叠加 |
| O-4 | 任意小游戏 | 关闭游戏后重开 | 状态与 `last_tick_time` 正确恢复（离线结算生效） |
| O-5 | 任何新玩法提案 | 立项评审 | 必须同时满足"纯加速 / 零美术 / 可挂机 / 共用通用状态表"四条铁律 |

## 模块 E4/E5：外星球特化科技与载荷接线

| # | 前置 | 步骤 | 期望 |
| :--- | :--- | :--- | :--- |
| E4-1 | 已解锁一颗外星球 | 首次切进去 | 一次性铺满 12 个特化节点（5×500 / 4×2,000 / 3×8,000），名称/风味/机制由 LLM 或本地卡池填 |
| E4-2 | 同上 | 再切回来一次 | **不重复生成、不再调 LLM**（`is_agent_generated` 幂等） |
| E4-3 | LLM 返回一张想改成本的卡 | 看落库结果 | 该卡被防火墙丢弃，节点继续用本地卡池；`rejected_cards` 计数 +1 |
| E4-4 | 特化节点有算力进度 | 点「重 Roll」 | 卡面刷新（名称/风味/机制），**成本与进度不变**；10 分钟内再点被拒 |
| E5-1 | 未解锁【猫草水培基础】 | 看 `/colony/state` | `tech_effects.catnip_efficiency = 0` |
| E5-2 | 解锁【猫草水培基础】 | 同批农夫再跑一段离线 | 猫薄荷增量 ×1.2，面板徽标显示「猫薄荷 +20%（科技）」 |
| E5-3 | 任意节点带未接线载荷（如 `armor_bonus`） | 看 `/tech/tree` | 该键只出现在 `pending_effects`，**不参与结算** |
| E5-4 | 节点带 `power_kw` | 解锁后看电力 | `power.gen_kw` 恰好增加对应 kW（并入发电侧） |

## 模块 P：星际扩张（基地建行 → 迁猫 → 星球性格）

| # | 前置 | 步骤 | 期望 |
| :--- | :--- | :--- | :--- |
| P-1 | 外星球已解锁 | 切星或读档 | 按母星同构建 **5 张表**的行（`colony_state`/`labor_buckets`/`facility_state`/`military_state`/`garden_state`），资源与猫口全 0 |
| P-2 | 同上 | 看该星球 `last_tick_time` | 等于**解锁那一刻**而不是 0（否则会把历史时间补算成收益） |
| P-3 | 星区里已有基地 | 重复切星/读档 | 建行幂等：进度与锚点都不被重置 |
| P-4 | 母星有 N 只空闲猫 | 派出跨星航线 | 出发即离港（在途两边不占工位），60 秒后抵达并计入目标星球 |
| P-5 | 同一星球已有 2 条在途航线 | 再派一条 | 被拒 `STAR_ROUTE_SLOTS_FULL`；派 1 只物流调度官后带宽 3 |
| P-6 | 任意 | 遇到被劫掠的航线 | 只**延误 10 分钟**并标红，猫一只不少，期满照常交付 |
| P-7 | 星带 vs 母星，同 2 只拾荒猫 | 各跑 60 秒 | 星带废铁约为母星 1.25 倍；冰卫星芯片约为母星 1.3 倍 |
| P-8 | 同等级住房 | 分别看母星与熔岩星 | 承载力母星 20 / 熔岩星 16（×0.8） |
| P-9 | 星图面板 | 观察在途航线 | 逐秒倒计时；被劫掠的航线注明"猫一只不少" |

---

## 修订记录

| 版本 | 日期 | 内容 |
| :--- | :--- | :--- |
| v1.0 | 2026-09-12 | 首次成文：按模块 A ~ O 建立验收测试用例，重点覆盖离线结算、断粮、繁育边界、防抖换班、安防三级预案、载具战损、水培、深网、LLM 兜底、多槽位存档与小游戏配额 |
| v1.1 | 2026-09-12 | 标注自动化落地位置：A-1~A-6 / B-1 / B-3 / C-1~C-5 / D-2 / N-1 / N-5 已由 `backend/tests/` 覆盖（SQLite 全绿 + 真机 MySQL 实测） |
| v1.2 | 2026-09-12 | 模块 D 补 D-4 ~ D-8（第一座窝带来第一只猫 / 缺料 / 唯一建筑上限 / 发射井占位 / 手点废墟上限与关闭条件），均已在 `tests/test_cold_start_loop.py` 落地 |
| v1.3 | 2026-09-13 | 补齐后续三大块验收：**模块 E4/E5**（外星球特化科技卡的三道防火墙、重 Roll 只换卡面、载荷白名单与"未接线只进 `pending_effects`"）、**模块 P 星际扩张**（基地建行五表 / 锚点铁律 / 建行幂等 / 迁猫出发即离港 / 带宽与物流官扩容 / 被劫掠只延误不丢猫 / 星球产粮与专属产出 / 星图面板倒计时）；对应自动化用例分别落在 `test_planet_tech.py`、`test_tech_effects.py`、`test_tech_effect_visibility.py`、`test_star_colony.py`、`test_star_routes.py`、`test_star_capacity.py`、`test_star_catnip.py`、`test_star_output.py` |
