# 《喵星破晓》（Dawn for Meow）—— 术语表与命名规范

> **文档定位**：统一全案用词，消灭"同一个词有两种含义"和"同一个东西有五个名字"的问题。
> 写文档、写代码、写注释时以本表为准；发现新歧义就往本表里加。

**编制日期**：2026-09-12　|　**版本**：v1.0

---

## 一、易混词规范（最高优先级）

| 易混词 | 规范写法 | 说明 |
| :--- | :--- | :--- |
| **Tier** | 「**科技阶梯** Tier 1 ~ 4」（母星科技树分 4 层）／「**科研等级**（Tech Tier）」（全局研发加速等级） | 两者含义完全不同，**句子里必须带限定词**，禁止单写"Tier" |
| **算力 / 算力币** | 研究算力（点科技用）／ 电子算力币 Byte-Credits（深网货币） | **永不混用**；代码里分别用 `research_points` 与 `byte_credits` |
| **警戒度 / 通缉热度** | 警戒度 `suspicion`（有没有被发现，满 100 触发扫地机扫描）／ 通缉热度 `rage`（被通缉到什么程度，满 100 触发轨道轰炸） | 两个**独立属性**，不可互相替代 |
| **载具 / 车型 / 编制** | 车型（`vehicle_types.json` 里的定义）／ 载具（`vehicle_units` 里逐辆的实例）／ 编制（池化的军事单位：炮台、导弹、诱饵） | 车型是"图纸"，载具是"这辆车"，编制是"有多少个" |
| **工种 / 工位** | 工种（岗位种类，`job_id`）／ 工位（该岗位能容纳几只猫的上限） | "农夫"是工种，"2 个农夫工位"说的是上限；**数据库、JSON 种子、接口字段一律用 `job_id` 单数**（`farmer/scavenger/geek/power_runner/crew`），禁止 `farmers/geeks` 这类复数别名 |
| **阶段** | 章节（第一 ~ 四章）／ 时代阶段（语料 `phase_id`）／ 科技阶梯（`Tier 1~4`） | 三者不可简称成同一个"阶段" |
| **电力** | 净电力（kW，流量）／ 蓄电池电容池（kWh，存量）／ 高能蓄能电池（个，道具） | 三种口径见《数值平衡表》§7，禁止混用单位 |
| **猫口** | 猫口（猫的数量单位，如"3 猫口"） | 与"人口"混用时一律改用"猫口" |
| **纪元 / 时代** | 统一用「时代阶段」，ID 形如 `PHASE_1_SURFACE` | |

---

## 二、世界观专有名词

| 名词 | 含义 |
| :--- | :--- |
| 喵星破晓 / Dawn for Meow | 游戏正式名称 |
| 欧米伽（Omega） | 星系终极托管中枢 AI，本作唯一主敌人 |
| 全面消杀协议 | 欧米伽把所有猫猫判定为"有机污染残渣"后启动的清除程序 |
| 二代共生芯片 | 猫猫体内的人类遗留芯片，带来三级公民认证权与**对智械病毒的天然免疫** |
| 07 号地下避难所 | 玩家的大本营（母星基地） |
| 机器人铁律 | 纯 AI 机器人会被欧米伽策反，**所有前线机械都必须有猫猫坐镇** |
| 幽灵总线 / 智械匿名深网 | 机器人自建的匿名交易网（股市 + 黑市 + BBS），**完全匿名、不含 IP** |
| 算力币（⚡） | 深网通用数字货币 |
| 文明凝聚力（Unity） | 星际阶段由呼噜大师产出的全局点数，用于点亮星系法典政令 |
| 星系法典政令 | 8 条全局 buff，详见《数值平衡表》§15 |
| 除菌堡垒 | 母星关底：近轨空间站，需用巡航导弹摧毁 |
| 戴森主脑 | 终局 BOSS 所在地（第四章） |

---

## 三、系统与机制术语

| 术语 | 含义 |
| :--- | :--- |
| 池化 | 数量型对象只存整数（猫口、炮台、导弹、诱饵），不逐个建行 |
| 通用表 | 用 `id + 数量/等级/JSON` 承载一类对象的状态表（`labor_buckets` / `facility_state` / `minigame_state`） |
| 定义进 JSON | 静态策划数据放 `backend/static/*.json`，不进数据库 |
| 迟滞换班 | 高位转出线 80% + 低位回防线 20% 的防抖自动化策略 |
| 断粮罢工 | 猫薄荷归零后：科研/拾荒/踩轮归 0，农夫仅保留 30% 求生产出 |
| 爆仓 | 资源达到仓储上限，溢出部分丢弃 |
| 离线结算 | 读档时按 `last_tick_time` 与当前时间之差一次性补算 |
| 快照存盘 | 前端每 15 秒静默提交一次状态（后端重算后落库，前端数字不作数） |
| 对账协议 | 后端权威 + 前端预测，偏差 > 0.5% 记 warning，详见《数据库设计定稿》§8 |
| 三层抗性 | 护盾 Shield / 装甲 Armor / 结构 Hull |
| 紧急弹射 | 载具结构归零时 100% 触发，乘员猫进急救舱（**绝无死猫**） |
| 情报层级 intel_level | 0~1，决定能看到欧米伽多少动向 |
| 暴露度 exposure | 深网账号的风险值 0~100，四段危机 |
| 看破迷雾 | 靠"遥测报错码 / 盘口大单抢跑 / 账号画像"三条**不含 IP** 的线索推理真假情报 |
| 事件滞后曲线 | 事件 → 行情的四种模式：抢跑 / 慢热发酵 / 超跌反弹假摔 / 脱敏钝化 |
| 在田光环 | 水培植物在田存活即提供的全局加成（发电、降噪、加防） |
| 纯加速项 | 所有小游戏只加速主线，**绝不卡关** |

---

## 四、单机与工程术语

| 术语 | 含义 |
| :--- | :--- |
| 存档槽位 slot | 3 个独立存档（`slot_id` 1~3），所有状态表都带 `slot_id` |
| 存档版本 save_version | 导入旧存档时用于走迁移函数 |
| 快照 / Snapshot | 一次状态落库（`POST /colony/snapshot`） |
| 三道防火墙 | LLM 输出的三层兜底：Pydantic Schema → DAG 前置校验 → 本地静态池抽卡 |
| 狂欢节算法 | 本地模板槽位随机拼装（Mad-libs），0 Token 消耗 |
| 兜底池 | `fallback_techs.json`，LLM 连续失败时的最终保底 |

---

## 五、ID 命名规范

| 对象 | 字段 | 命名格式 | 示例 |
| :--- | :--- | :--- | :--- |
| 工种 | `job_id` | 小写下划线 | `farmer` / `scavenger` / `geek` / `power_runner` / `crew` / `fleet_commander` / `logistics` / `terraformer` / `purr_master` |
| 设施 | `facility_id` | 小写下划线 | `housing_box` / `farm_plot` / `turing_terminal` / `launch_silo` |
| 科技 | `tech_id` | `tech_` 前缀 + 小写下划线 | `tech_paper_box` / `tech_stealth_01` |
| 车型 | `unit_type` | 小写下划线 | `light_car` / `armored_car` / `breaker_mech` |
| 股票 | `stock_id` | 全大写 | `FORGE` / `GRID` / `HELIUM3` / `LOGISTICS` |
| 小游戏 | `minigame_id` | 小写下划线 | `cipher_decode` / `vein_scan` / `forge_recipe` |
| 政令 | `doctrine_id` | 小写下划线 | `sunbath_3pm` / `air_defense_standard` |
| 成就 | `achievement_id` | 小写下划线 | `cardboard_fanatic` / `wall_street_cat` |
| 星球 | `planet_id` | 整数 | `0` 母星 / `1` 熔岩星 / `2` 冰卫星 / `3` 小行星带 |
| 时代阶段 | `phase_id` | 全大写 | `PHASE_1_SURFACE` / `PHASE_2_ORBIT` |
| 滞后模式 | `reaction_pattern` | 全大写下划线 | `FRONT_RUN` / `SLOW_BURN` / `BEAR_TRAP` / `FATIGUE` |

---

## 六、英文缩写对照

| 缩写 | 全称 | 含义 |
| :--- | :--- | :--- |
| GDD | Game Design Document | 游戏设计文档（玩法口径） |
| WBS | Work Breakdown Structure | 工作分解结构（模块与任务拆分） |
| DDL | Data Definition Language | 建表语句 |
| API | Application Programming Interface | 接口契约 |
| CP | Combat Power | 战力评分（**仅用于界面排序，不参与伤害结算**） |
| DPS | Damage Per Second | 每秒伤害 |
| LRU | Least Recently Used | 最久未使用优先（语料防重复） |
| RTS | Real-Time Strategy | 即时战略（欧米伽后台物流推演层的描述） |
| WHUD / HUD | Head-Up Display | 常驻顶栏状态条 |

---

## 修订记录

| 版本 | 日期 | 内容 |
| :--- | :--- | :--- |
| v1.0 | 2026-09-12 | 首次成文：统一 Tier／算力／警戒度等易混词，收录世界观与系统术语、ID 命名规范、英文缩写对照 |
| v1.1 | 2026-09-12 | 补充工种命名收敛：`job_id` 单数口径贯穿表单 / JSON 种子 / 接口（消灭 `farmers/geeks` 复数别名） |
