# 《喵星破晓》（Dawn for Meow）—— 代码结构与核心工程详细设计规范

> **版本**：v1.1　|　**更新日期**：2026-09-12　|　**配套文档**：[数值平衡表.md](数值平衡表.md)、[数据库设计定稿.md](数据库设计定稿.md)

---

## 1. 工程架构全景与目录树

本项目遵循 **前后端完全解耦、单机自包含运行、强类型契约** 的设计原则。

### 1.1 全局项目目录树
```text
dawn-for-meow/
├── backend/                       # FastAPI 后端工程根目录
│   ├── app/
│   │   ├── __init__.py
│   │   ├── core/                  # 核心基础配置与数学运算引擎
│   │   │   ├── __init__.py
│   │   │   ├── config.py          # 环境变量与应用全局配置 (pydantic-settings)
│   │   │   ├── database.py        # SQLAlchemy 异步/同步引擎与会话工厂
│   │   │   ├── errors.py          # 统一业务异常与错误码 {code, message}
│   │   │   ├── balance.py         # 全部数值常量唯一出处 (产出/造价/系数)
│   │   │   ├── seed_loader.py     # static/*.json 定义数据加载（"定义进 JSON"原则）
│   │   │   ├── offline_engine.py  # 确定性离线收益与马斯洛断粮积分算法
│   │   │   ├── combat_engine.py   # 战术交火结算、三层抗性与欧米茄物流推演
│   │   │   ├── stock_engine.py    # 智械股市漂移布朗运动与做空强平计算
│   │   │   ├── labor_engine.py    # 通用猫口调度与迟滞双阈值防抖算法
│   │   │   └── template_engine.py # 本地狂欢节语料槽位随机拼装器
│   │   ├── models/                # SQLAlchemy ORM 实体映射（16 张表，按域分文件）
│   │   │   ├── __init__.py
│   │   │   ├── types.py           # 跨方言列类型助手 (TINYINT/DOUBLE/JSON/ENUM/DATETIME)
│   │   │   ├── colony.py          # 基地物理资源、人口、电力与警戒度
│   │   │   ├── tech.py            # 科技树研发进度与解锁加成
│   │   │   ├── military.py        # 军备载具、急救舱队列与三级安防预案
│   │   │   ├── boss.py            # 欧米茄后台物流车队、情报与要塞演化
│   │   │   ├── darknet.py         # 智械深网股市大盘、做空合约与黑市
│   │   │   ├── garden.py          # 水培实验室网格、变异图鉴与机械臂
│   │   │   └── template.py        # 公频电台语料模板库
│   │   ├── schemas/               # Pydantic 请求/响应契约与 LLM 防火墙 Schema
│   │   │   ├── __init__.py
│   │   │   ├── common.py          # {code, data} 统一响应封装助手
│   │   │   ├── colony.py          # 基地状态与调度指令 Schema
│   │   │   ├── military.py        # 战术指令、截杀行动与急救舱 Schema
│   │   │   ├── darknet.py         # 股票交易、做空与黑市挂单 Schema
│   │   │   ├── save.py            # Base64 存档导出/导入与槽位切换 Schema
│   │   │   ├── tech_agent.py      # LLM 科技卡生成 Pydantic 边界防火墙
│   │   │   ├── forum_agent.py     # LLM 论坛做局发帖裁判 Pydantic Schema
│   │   │   ├── plant_agent.py     # LLM 异星植物学图鉴论文 Pydantic 防火墙
│   │   │   └── epilogue_agent.py  # LLM 通关碑文 Pydantic Schema（场景 6）
│   │   ├── services/              # 业务逻辑服务层 (事务编排与持久化)
│   │   │   ├── __init__.py
│   │   │   ├── game_init_service.py # 新游戏初始化事务（数据库设计定稿 §2.4）
│   │   │   ├── colony_service.py  # 基地快照存盘、工位调整、离线结算服务
│   │   │   ├── combat_service.py  # 战车组装、补给线伏击、巡航导弹发射服务
│   │   │   ├── darknet_service.py # 股市撮合、借券做空平仓、黑市空投服务
│   │   │   ├── garden_service.py  # 水培杂交、光环生效与采摘结算服务
│   │   │   ├── planet_tech_service.py # 外星球特化科技树铺树与单卡重 Roll（LLM 场景 2）
│   │   │   ├── plant_codex_service.py # 异星植物学图鉴论文生成与本地模板（LLM 场景 5）
│   │   │   ├── save_service.py    # 多槽位存档与 Base64 Gzip 打包还原服务
│   │   │   └── llm_service.py     # 第三方 LLM 接入、重试与本地备用池兜底
│   │   └── api/                   # RESTful API 路由分发
│   │       ├── __init__.py
│   │       ├── api_v1.py          # 路由聚合器
│   │       └── endpoints/
│   │           ├── colony.py      # 基地核心状态与工位调度接口
│   │           ├── military.py    # 战备机库与全息沙盘战术交互接口
│   │           ├── darknet.py     # 智械股市与黑市交易接口
│   │           ├── garden.py      # 基因实验室种植与机械臂接口
│   │           ├── tech.py        # 科技研发与手动重 Roll 接口
│   │           └── save.py        # 存档多槽位切换、导入导出接口
│   ├── scripts/                   # 本地运维脚本
│   │   └── init_db.py             # 建库 + 建 16 张表 + 可选新游戏初始化
│   ├── tests/                     # pytest 用例（默认 SQLite；DWM_TEST_MYSQL_URL 跑真机 MySQL）
│   ├── pytest.ini                 # pytest 配置（-p no:cacheprovider，规避 Windows 挂起）
│   ├── static/                    # 本地 JSON 种子与兜底数据 (策划数值唯一出处)
│   │   ├── jobs.json              # 工种字典 (母星 5 工种 + 星际 4 大高维职业)
│   │   ├── facilities.json        # 设施字典 (造价曲线/工位产出/解锁条件)
│   │   ├── cat_plants.json        # 猫草图鉴 (四大流派/光环/变现)
│   │   ├── doctrines.json         # 星系法典政令 (消耗凝聚力与效果)
│   │   ├── minigames.json         # 小游戏字典 (解锁星球/奖励池/配额)
│   │   ├── vehicle_types.json     # 车型字典 (三层属性/造价/乘员/机位/模块槽)
│   │   ├── expedition_targets.json# 拾荒与远征目标 (耗时/掉落/警戒度代价)
│   │   ├── techs_planet0.json     # 母星 19 节点手工编排科技树
│   │   ├── templates_default.json # 初始时代广播语料
│   │   └── fallback_techs.json    # 三道防火墙的兜底科技卡池
│   ├── main.py                    # FastAPI 启动主入口与中间件配置
│   ├── requirements.txt           # Python 核心依赖表
│   └── .env.example               # 数据库与 LLM API 密钥配置模板
│
├── frontend/                      # Vue 3 前端工程根目录
│   ├── public/                    # 静态公共资源
│   ├── src/
│   │   ├── assets/                # 全局样式 (Tailwind 基础层与 CRT 扫描线)
│   │   │   └── main.css
│   │   ├── components/            # UI 组件库 (零外部美术切图，纯 Lucide 矢量)
│   │   │   ├── common/            # 通用原子组件
│   │   │   │   ├── Modal.vue      # 极客半透明遮罩弹窗
│   │   │   │   ├── GaugeBar.vue   # 三层抗性发光进度条 (Shield/Armor/Hull)
│   │   │   │   └── Badge.vue      # 词缀与状态徽章
│   │   │   ├── hud/               # 常驻顶部 HUD 监控条
│   │   │   │   ├── ResourceBar.vue# 猫口/薄荷/废铁/芯片/钛合金/算力币展示
│   │   │   │   └── ThreatBar.vue  # 净电力平衡与天网警戒度脉冲条
│   │   │   ├── panels/            # 主视图面板
│   │   │   │   ├── ProductionPanel.vue # 生产与劳动力工位调度面板
│   │   │   │   ├── FacilitiesPanel.vue # 设施升级与巨构建造面板
│   │   │   │   ├── TechPanel.vue       # DAG 科技树研发面板
│   │   │   │   ├── WarRoomPanel.vue    # 战术全息沙盘 (雷达/交火/伏击物流/日志流)
│   │   │   │   ├── ArchivesPanel.vue   # 历史档案、数据统计与成就馆面板
│   │   │   │   ├── StarChartPanel.vue  # 星区星图 (太空时代解锁)
│   │   │   │   ├── GardenSubPanel.vue  # 特色子系统一：猫草水培实验室
│   │   │   │   └── DarknetSubPanel.vue # 特色子系统二：智械匿名深网终端
│   │   │   └── radio/             # 常驻底栏
│   │   │       └── RadioTicker.vue# 滚动电台与黑客 BBS 简讯流
│   │   ├── composables/           # 组合式函数 (高频主循环与双时钟)
│   │   │   ├── useGameLoop.ts     # 100ms 丝滑高频主循环 (平滑进度/离散进位)
│   │   │   ├── useAutoSave.ts     # 15s 后台静默快照自动提交
│   │   │   └── useCombatTicker.ts # 战术交火日志动态滚动流
│   │   ├── stores/                # Pinia 响应式状态管理仓
│   │   │   ├── colony.ts          # 基地资源、工位配置、繁育与警戒度
│   │   │   ├── facilities.ts      # 建筑等级、工位上限与造价状态
│   │   │   ├── military.ts        # 载具编制、欧米茄情报、战术微操指令、急救舱
│   │   │   ├── tech.ts            # 已解锁科技与当前在研节点
│   │   │   ├── darknet.ts         # 股市行情、做空持仓、黑市与暴露度
│   │   │   ├── garden.ts          # 水培网格、在田光环与变异母本图鉴
│   │   │   ├── radio.ts           # 公频电台历史与滚动战报流
│   │   │   └── save.ts            # 多槽位管理、Base64 导出/导入
│   │   ├── types/                 # TypeScript 强类型契约定义
│   │   │   └── game.ts
│   │   ├── App.vue                # 根组件 (布局骨架与开局冷启动引导)
│   │   └── main.ts                # 前端挂载入口
│   ├── index.html
│   ├── package.json
│   ├── tailwind.config.js         # Tailwind 扩展 (自定义发光边框、震颤与扫描动效)
│   └── vite.config.ts
```

---

## 2. 后端核心计算引擎详细设计

后端负责保证所有数值与逻辑推演的**高确定性与防作弊**，绝不将关键数值交给前端不可靠的时间片。

### 2.1 离线收益与马斯洛断粮积分算法 (`offline_engine.py`)
```python
"""
离线收益计算引擎：
处理玩家关闭浏览器或离线期间的连续时间积分，严格卡死仓储上限与绝食罢工。
"""
from typing import Dict, Any

def calculate_offline_progress(
    current_state: Dict[str, Any],
    elapsed_seconds: float
) -> Dict[str, Any]:
    """
    输入：当前快照状态，离线经过秒数 Delta_t
    输出：离线结算结果（资源净增量、繁育猫口增长、是否发生断粮罢工、安防日志）
    """
    catnip = current_state["catnip"]
    pop = current_state["cats_total"]
    farmers = current_state["farmers"]
    scavengers = current_state["scavengers"]
    storage_limit = current_state["storage_capacity"]
    
    # 1. 粮食产出率与消耗率 (单位: /s)
    # 系数唯一出处：backend/app/core/balance.py，口径见《数值平衡表.md》§3
    catnip_prod_rate = farmers * 0.2
    catnip_consume_rate = pop * 0.05
    net_catnip_rate = catnip_prod_rate - catnip_consume_rate
    
    result = {
        "elapsed_seconds": elapsed_seconds,
        "is_starving": False,
        "starve_duration_seconds": 0.0,
        "gained_resources": {},
        "gained_cats": 0
    }
    
    if net_catnip_rate >= 0:
        # 粮食有盈余，正常积累
        final_catnip = min(catnip + net_catnip_rate * elapsed_seconds, storage_limit)
        scrap_gained = scavengers * 0.5 * elapsed_seconds
        result["gained_resources"]["catnip"] = final_catnip - catnip
        result["gained_resources"]["scrap"] = min(scrap_gained, storage_limit - current_state["scrap"])
        
        # S型曲线计算繁育 (dN/dt = r * N * (1 - N/K)，r 基准 0.01/s)
        # 保留小数进度池，避免每 tick 取整造成的进度损失
        k_cap = current_state["max_cat_capacity"]
        if pop < k_cap:
            r = 0.01
            total_progress = current_state.get("birth_progress", 0.0) + r * pop * (1.0 - pop / k_cap) * elapsed_seconds
            new_born = min(int(total_progress), k_cap - pop)
            result["gained_cats"] = new_born
            result["birth_progress"] = total_progress - new_born
    else:
        # 粮食处于亏空衰减
        time_to_empty = catnip / abs(net_catnip_rate)
        if elapsed_seconds <= time_to_empty:
            result["gained_resources"]["catnip"] = net_catnip_rate * elapsed_seconds
            result["gained_resources"]["scrap"] = scavengers * 0.5 * elapsed_seconds
        else:
            # 发生了断粮绝食
            result["is_starving"] = True
            result["starve_duration_seconds"] = elapsed_seconds - time_to_empty
            # 库存清零，但农夫猫以 30% 求生本能继续翻野草产粮 (0.06/s 每只)
            result["gained_resources"]["catnip"] = -catnip + farmers * 0.06 * result["starve_duration_seconds"]
            # 绝食期间拾荒猫、科研猫、踩轮猫效率全部锁死为 0
            normal_scrap = scavengers * 0.5 * time_to_empty
            result["gained_resources"]["scrap"] = normal_scrap
            
    return result
```

### 2.2 智械股市漂移布朗运动推演引擎 (`stock_engine.py`)
```python
"""
Bitburner 风格股市推演引擎：
一阶胜率 + 二阶趋势锚点 + 均值回归力 + 动量衰减
"""
import random
import math
from typing import Dict, Any

def tick_stock_price(stock: Dict[str, Any]) -> Dict[str, Any]:
    """
    单次价格演化 Tick（通常每隔 6 秒执行一次）
    """
    price = stock["price"]
    forecast = stock["forecast"]                # 一阶胜率 (0.0 ~ 1.0)
    target_forecast = stock["target_forecast"]  # 二阶趋势锚点 (0.0 ~ 1.0)
    volatility = stock["volatility"]            # 波动率 (例如 0.03)
    momentum = stock.get("momentum", 0.0)       # 外部事件动量冲击
    
    # 1. 一阶胜率向二阶锚点缓慢靠拢 (均值回归)
    forecast += (target_forecast - forecast) * 0.02
    
    # 2. 判定本轮涨跌方向
    roll = random.random()
    is_up = roll < (forecast + momentum * 0.1)
    
    # 3. 几何布朗波动
    pct_change = random.uniform(0.1, 1.0) * volatility
    if is_up:
        price *= (1.0 + pct_change)
    else:
        price *= max(0.01, 1.0 - pct_change)
        
    # 4. 动量阻尼衰减
    momentum *= 0.92
    
    # 5. 趋势反转突变检验 (大周期钟摆)
    if random.random() < 0.01:
        target_forecast = 1.0 - target_forecast
        
    stock["price"] = round(price, 2)
    stock["forecast"] = round(forecast, 4)
    stock["target_forecast"] = round(target_forecast, 4)
    stock["momentum"] = round(momentum, 4)
    return stock
```

### 2.3 战术三层抗性交火结算 (`combat_engine.py`)
```python
"""
三层抗性交战推演：
护盾(能量穿透弱点) -> 装甲(破甲弱点) -> 结构(免死弹射)
"""
from typing import Dict, Any, Tuple

def resolve_attack(
    attacker_atk: float,
    damage_type: str,  # 'LASER', 'KINETIC', 'EXPLOSIVE'
    target: Dict[str, float]  # {'shield': 100, 'armor': 50, 'hull': 200, 'max_armor': 50}
) -> Tuple[Dict[str, float], float, bool]:
    """
    执行单次攻击结算：
    返回更新后的 target, 本次造成的实际伤害值, 是否触发弹射损毁
    """
    rem_damage = attacker_atk
    
    # 1. 护盾吸收层
    if target["shield"] > 0:
        shield_mult = 1.8 if damage_type == 'LASER' else 0.8
        effective_shield_dmg = rem_damage * shield_mult
        if target["shield"] >= effective_shield_dmg:
            target["shield"] -= effective_shield_dmg
            rem_damage = 0.0
        else:
            absorbed_dmg = target["shield"] / shield_mult
            rem_damage -= absorbed_dmg
            target["shield"] = 0.0
            
    # 2. 装甲吸收层
    if rem_damage > 0 and target["armor"] > 0:
        # 装甲提供百分比物理减伤，但破甲武器会削减装甲本身
        armor_reduction = min(0.75, target["armor"] / 100.0)
        if damage_type in ('KINETIC', 'EXPLOSIVE'):
            # 破甲削蚀
            shred = rem_damage * 0.3
            target["armor"] = max(0.0, target["armor"] - shred)
            
        real_dmg_to_hull = rem_damage * (1.0 - armor_reduction)
    else:
        real_dmg_to_hull = rem_damage
        
    # 3. 结构核心层
    target["hull"] -= real_dmg_to_hull
    is_ejected = False
    if target["hull"] <= 0:
        target["hull"] = 0.0
        is_ejected = True  # 触发 100% 弹射免死协议
        
    return target, real_dmg_to_hull, is_ejected
```

---

## 3. 前端双时钟与 Pinia 状态架构

### 3.1 双时钟协作模型
```text
┌──────────────────────────────────────────────────────────┐
│                   Vue 3 浏览器渲染主线程                  │
│                                                          │
│  [高频本地时钟: 100ms Tick]                               │
│  ├─ useGameLoop.ts 驱动平滑动画插值 (S曲线/进度条)        │
│  ├─ 维护离散进度进位池 (birth_progress, prod_fraction)    │
│  └─ 刷新战术全息沙盘实时交战日志与雷达扫描线             │
│                                                          │
│  [低频持久化时钟: 15,000ms 快照保存]                       │
│  ├─ useAutoSave.ts 提取 Pinia 状态快照                   │
│  └─ POST /api/v1/colony/snapshot 静默提交持久化至 MySQL  │
└──────────────────────────────────────────────────────────┘
```

### 3.2 六大 Pinia 状态仓划分与职责

#### 1. `colony.ts` (基地状态仓)
*   **State**:
    *   `resources`: `{ catnip: number, scrap: number, chips: number, alloys: number, battery: number, lube: number }`
    *   `population`: `{ total: number, max_cap: number, unassigned: number, birth_progress: number }`
    *   `workstations`: `{ farmer: number, scavenger: number, geek: number, power_runner: number, crew: number }`（键名一律用 `job_id`，与数据库 / `jobs.json` / 接口完全同构）
    *   `workstation_limits`: `{ farmer: number, scavenger: number, geek: number, power_runner: number, crew: number }`
    *   `hysteresis_rules`: `{ enabled: boolean, upper_limit: number, lower_limit: number, shift_count: number }`
    *   `suspicion`: `{ current: number, max: number, rate: number }`
    *   `power`: `{ net_kw: number, battery_kwh: number, battery_kwh_max: number }`
*   **Actions**:
    *   `assignLabor(role: string, delta: number)`: 工位增减（带上限与空闲猫口校验）
    *   `tick100ms(deltaSeconds: number)`: 本地高频数值插值累加
    *   `applyOfflineGains(payload: OfflinePayload)`: 注入离线结算奖励

#### 2. `military.ts` (军备战备与全息沙盘仓)
*   **State**:
    *   `hangar_capacity`: `number` (机库机位上界，母星 12 / 星际 24)
    *   `vehicles`: `Array<VehicleUnit>` (逐辆：昵称、模块、护盾/装甲/装甲上限/结构、乘员、状态)
    *   `pooled_armaments`: `{ laser_turrets: number, cruise_missiles: number, decoy_count: number }` (池化军备)
    *   `omega_intel`: `{ convoy_ends_at: number, factory_frozen_until: number, intel_level: number, rage: number }`
    *   `active_combat`: `{ in_battle: boolean, target_type: string, enemy_stats: UnitStats, ally_stats: UnitStats }`
    *   `combat_logs`: `Array<{ id: string, timestamp: string, text: string, type: 'info'|'warn'|'crit' }>`
    *   `hospital_queue`: `Array<{ unit_id: string, cats: string[], ends_at: number }>`
*   **Actions**:
    *   `executeTacticalCommand(type: 'OVERCLOCK' | 'EMP' | 'EJECT')`: 触发战术电力指令
    *   `ambushConvoy()`: 发动运输队半路伏击战役
    *   `launchCruiseMissile()`: 点火战略巡航导弹引爆堡垒
    *   `assembleVehicle(unitType: string, nickname: string)`: 组装新车（校验机库机位）
    *   `modifyVehicle(unitId: string, modules: Module[]): void`: 改装或退役拆解

#### 3. `tech.ts` (科技研发仓)
*   **State**:
    *   `unlocked_tech_ids`: `string[]`
    *   `current_research`: `{ tech_id: string, progress: number, target: number } | null`
    *   `tech_tiers_unlocked`: `number`
*   **Actions**:
    *   `startResearch(techId: string)`: 选定在研项目
    *   `rerollTechCard()`: 消耗算力重 Roll 候选科技卡

#### 4. `darknet.ts` (智械深网与股市仓)
*   **State**:
    *   `byte_credits`: `number`
    *   `exposure`: `number`
    *   `burner_id`: `string`
    *   `stocks`: `Array<StockQuote>` (包含历史 K 线高低收)
    *   `short_contracts`: `Array<ShortContract>`
    *   `black_market_deals`: `Array<MarketItem>`
*   **Actions**:
    *   `buyLong(stockId: string, shares: number)`: 现货做多
    *   `openShort(stockId: string, shares: number, leverage: number)`: 借券做空建仓
    *   `postRumorBBS(content: string)`: 玩家打字发帖做局 (触发 LLM 语义裁判)

#### 5. `garden.ts` (猫草基因实验室仓)
*   **State**:
    *   `grid_size`: `number` (已解锁边长 3 ~ 7)
    *   `unlocked_cells`: `number` (已解锁格子数 9 ~ 49)
    *   `grid_tiles`: `TileState[][]` (固定 7×7 场地，未解锁格不可播种)
    *   `unlocked_genes`: `string[]`
    *   `active_medium`: `'STERILE' | 'RADIATION' | 'ZERO_G'`
    *   `mechanical_arm_active`: `boolean`
*   **Actions**:
    *   `plantSeed(x: number, y: number, seedId: string)`
    *   `harvestTile(x: number, y: number)`

#### 6. `save.ts` (多槽位与存档导入导出仓)
*   **State**:
    *   `current_slot`: `1 | 2 | 3`
    *   `last_saved_at`: `string`
*   **Actions**:
    *   `exportBase64Save()`: 压缩打包生成 Base64 文本
    *   `importBase64Save(base64Str: string)`: 校验解析并重载游戏状态

---

## 4. RESTful API 规范契约（定稿）

所有 API 前缀统一为 `/api/v1`，数据格式为 `application/json`。

> **本节是接口的唯一清单。** 原《技术架构与数据库设计参考》（已归档）中的接口表已作废，其独有接口（设施建造、水培操作、股市与黑市、烧录身份、远征派遣与收取）已全部并入本节。

### 4.1 统一响应封装与错误码
*   成功：`{ "code": 200, "data": { ... } }`
*   失败：`{ "code": 400, "message": "WORKSTATION_LIMIT_EXCEEDED" }`
*   常用错误码：
    *   `400 BAD_REQUEST`：参数非法；
    *   `400 WORKSTATION_LIMIT_EXCEEDED`：工位已达上限或无空闲猫口；
    *   `400 INSUFFICIENT_RESOURCE`：资源/算力/电量不足；
    *   `409 CONFLICT`：状态冲突（如载具已出库、科技正在研发中）；
    *   `500 INTERNAL_ERROR`：服务端异常。

### 4.2 API 总清单

| 方法 | 路径 | 职责 |
| :--- | :--- | :--- |
| `GET` | `/game/load?slot=1&planet_id=0` | 读档并执行离线结算（返回状态与《离线休整报表》） |
| `GET` | `/colony/state` | 获取当前槽位与星球的完整状态 |
| `POST` | `/colony/snapshot` | 15 秒静默快照存盘 |
| `POST` | `/colony/scavenge` | 冷启动手点废墟（+1 废铁，5 次够造第一座纸箱窝） |
| `POST` | `/colony/dispatch` | 调配工位分桶与迟滞换班策略 |
| `POST` | `/facilities/build` | 建造/升级设施、推进发射井总装 |
| `POST` | `/tech/research` | 激活前置已满足的科技研发 |
| `POST` | `/tech/reroll` | 消耗算力手动重 Roll 当前科技卡 |
| `POST` | `/military/dispatch` | 派遣载具编队执行异步废墟远征 |
| `POST` | `/military/collect` | 收取已完成远征的战利品与战报 |
| `POST` | `/military/tactical-action` | 战术电力指令（超频 / EMP / 弹射） |
| `POST` | `/military/ambush-convoy` | 伏击欧米茄矿石补给车队 |
| `POST` | `/military/launch-missile` | 点火巡航导弹摧毁除菌要塞 |
| `GET` | `/vehicle/list` | 载具列表（逐辆的三层血条、模块、乘员、状态） |
| `POST` | `/vehicle/assemble` | 组装新载具（校验机库机位） |
| `POST` | `/vehicle/modify` | 改装模块 / 命名 / 退役拆解 |
| `POST` | `/vehicle/repair` | 支付 50% 材料维修受损载具 |
| `POST` | `/garden/action` | 播种 / 采摘 / 切换培养液 / 扩建 1 格 |
| `POST` | `/darknet/stock/trade` | 做多开平仓 |
| `POST` | `/darknet/short` | 借券做空开仓（含到期时间与爆仓价） |
| `POST` | `/darknet/market/trade` | 黑市大宗买卖与违禁品即买即用 |
| `GET` | `/darknet/forum/feed` | 论坛帖子流（含网段标记与遥测附件） |
| `POST` | `/darknet/forum/post` | 玩家自由发帖做局（LLM 裁判 + 生成跟帖） |
| `POST` | `/darknet/reroll-id` | 烧录新身份洗白暴露度 |
| `GET` | `/planet/state` | 星区星图：解锁进度、生态标签、在途航线 |
| `POST` | `/planet/switch` | 切换当前操作星球 |
| `POST` | `/planet/migrate` | 派跨星航线把猫口从母星运到外星球（§15.3 第②步） |
| `GET` | `/doctrine/list` | 政令清单（是否已点亮 / 凝聚力是否够 / 哪些效果已接线） |
| `POST` | `/doctrine/unlock` | 消耗文明凝聚力点亮政令（v2.54 落地：种子 + 服务 + 接口；效果白名单见 §15.2 落地口径） |
| `GET` | `/minigame/list` | 小游戏清单、配额与最好成绩 |
| `POST` | `/minigame/action` | 小游戏内操作（译码 / 扫描 / 投料） |
| `GET` | `/stats/career` | 生涯统计看板 |
| `GET` | `/stats/achievements` | 成就徽章进度与解锁时间 |
| `GET` | `/tech/tree` | 科技树面板数据（19 节点 DAG + 折扣 + 在研进度） |
| `GET` | `/garden/state` | 水培实验室状态（网格 / 图鉴 / 在轨光环 / 图鉴论文） |
| `GET` | `/darknet/state` | 深网状态（行情 / 持仓 / 暴露度 / 身份） |
| `POST` | `/darknet/data-eye` | 烧算力换线索（数据之眼） |
| `GET` | `/radio/feed` | 公频电台滚动战报（本地填槽 + LRU，0 Token） |
| `POST` | `/radio/generate` | 生成新的电台语料（LLM 场景 3） |
| `POST` | `/military/assemble-missile` | 总装巡航导弹（合金 30 + 芯片 10 + 电池 2） |
| `POST` | `/military/intercept-raid` | 拦截欧米茄掠夺突袭 |
| `POST` | `/military/final-assault` | 三段决战（顺序 + 物资校验） |
| `GET` | `/save/slots` | 槽位列表与元信息 |
| `GET` | `/save/export?slot=1` | 导出 Base64 存档（Gzip + 校验和） |
| `POST` | `/save/import` | 导入并校验存档（含版本迁移） |

### 4.3 基地与调度接口 (`/colony`、`/facilities`)
*   **`GET /game/load?slot=1&planet_id=0`**（读档入口）
    *   **描述**：与 `GET /colony/state` **共用同一套离线补算内核**，差别只有三条语义——① **不自动建档**（槽位为空报 `404 SAVE_NOT_FOUND`，自动建档只属于 `/colony/state` 的开箱即用入口）；② **读档即激活**（把目标星球写成该槽位的活跃星球）；③ **如实拒绝**（星球未解锁报 `400 PLANET_LOCKED`；外星球基地属于尚未开发的星级内容，报 `400 STAR_COLONY_NOT_IMPLEMENTED`，不产生任何副作用）；
    *   **响应**：与 `/colony/state` 同构（含 `offline_report`），便于前端两个入口共用一套解析。
*   **`GET /colony/state`**
    *   **描述**：获取当前激活槽位与星球的完整状态（含离线时长自动结算）；
    *   **响应示例**：
        ```json
        {
          "code": 200,
          "data": {
            "planet_id": 0,
            "resources": { "catnip": 120.5, "scrap": 45.0, "chips": 3, "alloys": 0, "battery": 5, "lube": 0 },
            "power": { "net_kw": 6.0, "battery_kwh": 140, "battery_kwh_max": 200 },
            "population": { "total": 3, "max_cap": 6, "unassigned": 1, "birth_progress": 0.42 },
            "workstations": { "farmer": 1, "scavenger": 1, "geek": 0, "power_runner": 0, "crew": 0 },
            "workstation_limits": { "farmer": 2, "scavenger": 2, "geek": 1, "power_runner": 0, "crew": 24 },
            "offline_report": { "elapsed_seconds": 3600, "gained_catnip": 180.0, "is_starved": false }
          }
        }
        ```
*   **`POST /colony/snapshot`**
    *   **描述**：15s 低频静默持久化快照存盘；
    *   **请求体**：当前 Pinia 状态快照（含前台计算校验值）；
    *   **响应**：`{ "code": 200, "message": "SNAPSHOT_PERSISTED", "saved_at": 1726131000 }`
*   **`POST /colony/dispatch`**
    *   **描述**：调配工位分桶并写入迟滞换班策略；
*   **请求体**：`{ "role": "farmer", "delta": 1, "policy": { "enabled": true, "upper": 0.8, "lower": 0.2, "shift": 2 } }`（`role` 取 `job_id`）；
    *   **错误码**：`400 WORKSTATION_LIMIT_EXCEEDED`（工位已达上限或无空闲猫口）。
*   **`POST /colony/scavenge`**
    *   **描述**：冷启动【手点废墟】，每次 `+1 机械废铁`（口径见《数值平衡表》§3.4）；
    *   **响应**：`{ "code": 200, "data": { "scrap": 5, "manual_scavenge_clicks": 5, "clicks_left": 35, "hint": "..." } }`；
    *   **错误码**：`400 COLD_START_FINISHED`（已有拾荒猫在岗，手点入口关闭）、`400 COLD_START_EXHAUSTED`（达 40 次上限）、`400 STORAGE_FULL`（废铁爆仓）。
*   **`POST /facilities/build`**
    *   **描述**：建造/升级设施或推进发射井总装；
    *   **请求体**：`{ "facility_id": "farm_plot", "count": 1 }`；
    *   **响应**：扣除材料并返回最新设施等级、工位上限、承载力与 `narrative`（第一座纸箱窝 ⇒ 第一只折耳猫入驻）；
    *   **冷启动特例**：第一座【瓦楞纸箱窝】建成时总猫口 `0 → 1`（GDD §1.3 阶段二），新猫一律进入空闲待命池；
    *   **错误码**：`400 INSUFFICIENT_RESOURCE`、`409 FACILITY_MAX_LEVEL`（唯一建筑如蓄电池已达 1 级）、`400 BAD_REQUEST`（未知设施 / 发射井造价待定稿 / count ≤ 0）；
    *   **阶段 3 暂缓项**：建造暂不做科技硬门槛（否则冷启动死锁），响应里的 `unlock_hint` 只作前端提示，模块 E 接线后再启用校验。

### 4.4 科技研发接口 (`/tech`)
*   **`POST /tech/research`**
    *   **请求体**：`{ "tech_id": "tech_acoustics_1" }`；
    *   **内部行为**：校验父节点 DAG 存在性后进入研发中，错误码 `400 BAD_REQUEST`。
*   **`POST /tech/reroll`**
    *   **请求体**：`{ "planet_id": 0 }`；
    *   **内部行为**：消耗算力重 Roll 候选科技卡；失败自动重试 2 次，仍失败取兜底池，玩家零感知。

### 4.5 军备、载具与远征接口 (`/military`、`/vehicle`)
*   **`POST /military/dispatch`**
    *   **描述**：派出载具编队执行异步废墟远征；
    *   **请求体**：`{ "target_id": "WALMART", "unit_ids": [3], "count": 1 }`；**响应**：返回任务 ID 与到期时间戳。
*   **`POST /military/collect`**
    *   **请求体**：`{ "expedition_id": "e1" }`；**响应**：收获材料与探险日志。
*   **`POST /military/tactical-action`**
    *   **描述**：下达即时战术电力指令（扣减蓄电池 kWh）；
    *   **请求体**：`{ "command": "OVERCLOCK" }` 或 `{ "command": "EMP" }` 或 `{ "command": "EJECT" }`；
    *   **响应**：返回指令生效帧与战报日志；电量不足时报 `400 INSUFFICIENT_RESOURCE`。
*   **`POST /military/ambush-convoy`**
    *   **描述**：派遣装甲猫车拦截欧米茄矿石补给车队（60 秒限时）；
    *   **响应**：返回交战初始化数据与敌方护卫编队参数。
*   **`POST /military/launch-missile`**
    *   **描述**：点火地对地战略巡航导弹，摧毁除菌要塞防护核心与主反应堆。
*   **`GET /vehicle/list`**
    *   **描述**：逐辆返回护盾 / 装甲 / 装甲上限 / 结构、装配模块、乘员猫与当前状态（待命 / 远征 / 维修）。
*   **`POST /vehicle/assemble`**
    *   **请求体**：`{ "unit_type": "armored_car", "nickname": "雷霆号" }`；
    *   **错误码**：`400 BAD_REQUEST`（机库机位已满或资源不足）。
*   **`POST /vehicle/modify`**
    *   **请求体**：`{ "unit_id": 3, "modules": [{ "slot": 0, "module_id": "laser_mk2" }], "nickname": "雷霆号" }`（`action: SCRAP` 表示退役拆解）。
*   **`POST /vehicle/repair`**
    *   **请求体**：`{ "unit_id": 3 }`；**响应**：扣除 50% 造价材料，返回维修到期时间戳。

### 4.6 水培实验室接口 (`/garden`)
*   **`POST /garden/action`**
    *   **请求体**：`{ "action": "PLANT" | "HARVEST" | "MEDIUM" | "EXPAND", "x": 1, "y": 2, "seed_id": "moss", "medium": "RADIATION" }`；
    *   **响应**：返回变现资源（采摘）+ 最新格盘状态（含未解锁格）。

### 4.7 智械匿名深网接口 (`/darknet`)
*   **`POST /darknet/stock/trade`**
    *   **请求体**：`{ "stock_id": "FORGE", "action": "BUY_LONG" | "SELL_LONG" | "CLOSE_LONG", "shares": 100 }`
*   **`POST /darknet/short`**
    *   **请求体**：`{ "stock_id": "FORGE", "shares": 50, "leverage": 2 }`；**响应**：生成 10 分钟合约并返回爆仓价与到期时间戳。
*   **`POST /darknet/market/trade`**
    *   **请求体**：`{ "item_id": "scrap", "side": "BUY" | "SELL", "qty": 100 }`；**响应**：扣款并生成 5 分钟在途空投。
*   **`GET /darknet/forum/feed`**
    *   **响应**：帖子流，每条含 `subnet_origin`（物理网段）、`telemetry_dump`（遥测附件）与机器人跟帖。
*   **`POST /darknet/forum/post`**
    *   **请求体**：`{ "title": "内幕爆料", "content": "听说熔岩铸造厂高炉炸膛了！" }`；
    *   **内部行为**：调用 LLM 裁判提取星级并注入股票动量，返回生成的散户跟帖列表；10 分钟冷却。
*   **`POST /darknet/reroll-id`**
    *   **请求体**：`{ "custom_id": "anon_meow_99" }`（可选）；
    *   **内部行为**：消耗 10 废铁 + 50 算力币，暴露度归零。

### 4.8 星区星图接口 (`/planet`)
*   **`GET /planet/state`**：星球解锁进度、生态标签、跨星在途航线；
*   **`POST /planet/switch`**：`{ "planet_id": 1 }`，切换当前操作星球（母星永远可用）。

### 4.9 统计与成就接口 (`/stats`)
*   **`GET /stats/career?slot=1`**：生涯统计大盘（游玩时长、历史总产量、深网战绩、轰炸幸存次数、`achievements_unlocked/total`、`completed`）；
    *   **口径**：只读聚合（`career_stats` + 各系统现状），不改变任何玩法数值；见《数值平衡表》§17.1。
*   **`GET /stats/achievements?slot=1`**：成就徽章进度与解锁时间；读取时顺带结算进度。
    *   **点亮规则**：`metric ≥ target` 时写 `achievements.unlocked_at`（整秒），**只写一次、不回退**；未开局槽位只返回空盘、不落库；15 枚徽章清单见《数值平衡表》§17.2。
    *   **响应**：`{ "code": 200, "data": { "achievements": [ { "achievement_id", "name", "desc", "target", "progress", "percent", "unlocked", "unlocked_at", "newly_unlocked" } ], "unlocked_count", "total", "newly_unlocked" } }`。

### 4.10 存档管理接口 (`/save`)
*   **`GET /save/slots`**：槽位列表与元信息（槽位名、游玩时长、猫口、活跃星球、凝聚力与政令数、最后写入时间、存档版本）；空槽位返回 `exists: false` 占位，便于前端直接渲染 1/2/3；
*   **`GET /save/export?slot=1`**：**响应** `{ "code": 200, "slot": 1, "base64_payload": "H4sIC...==", "checksum": "a3f8c...", "save_version": 1, "raw_bytes": 12345, "total_rows": 87 }`（按《数据库设计定稿》§8.5/§8.5.1：Gzip + Base64 + SHA-256，导出空槽位报 `404 SAVE_SLOT_EMPTY`）；
*   **`POST /save/import`**：`{ "slot": 2, "base64_payload": "H4sIC...==", "slot_name": "可选改名" }` → 校验和比对 + 版本迁移后**单事务整体替换**目标槽位（跨槽位导入会重定向 `slot_id`）；
    *   **错误码**：`400 INVALID_SAVE_PAYLOAD`（结构损坏 / 含未知表 / 解压超 8 MB）、`400 CHECKSUM_MISMATCH`、`400 UNSUPPORTED_SAVE_VERSION`；失败不留半截数据。

### 4.11 星际政令与小游戏接口 (`/doctrine`、`/minigame`)
*   **`POST /doctrine/unlock`**：`{ "doctrine_id": "sunbath_3pm" }`，扣除 `save_slot.unity` 并写入 `doctrines`；
*   **`GET /minigame/list`**：返回各小游戏的解锁状态、剩余配额（密电译码每日 3 次 / 矿脉扫描上限 5 次）与 `best_score`；
*   **`POST /minigame/action`**：`{ "minigame_id": "cipher_decode", "action": "SUBMIT_GUESS", "payload": { "guess": [1, 3, 5, 2] } }`，返回本轮反馈与奖励；
    *   **通用约定**：所有小游戏共用 `minigame_state` 表，`action` 与 `payload` 由各玩法自定义，**新增玩法无需改表结构**。

---

## 5. Milestone 1 (地表破晓 MVP) 实施工程路径

为稳步推进开发，Milestone 1 分为以下 4 个原子阶段：

```text
[阶段 1: 基础设施骨架] ──► [阶段 2: 前端控制台与双时钟] ──► [阶段 3: 冷启动与工位循环] ──► [阶段 4: 离线闭环与验证]
(FastAPI/MySQL/DDL)        (Vue3/Tailwind/HUD/100ms)        (翻废墟/纸箱/第一只猫/换班)      (15s快照/离线积分/无报错)
```

1. **阶段 1：基础设施与数据底座**
   - 搭建 `backend/` 工程，配置 SQLAlchemy 引擎与 MySQL 连接；
   - 按 [`数据库设计定稿.md`](数据库设计定稿.md) 执行 **16 张表** DDL 初始化；
   - 实现 `GET /colony/state` 与 `POST /colony/snapshot` 基础打通。
2. **阶段 2：前端极客界面与双时钟核心**
   - 搭建 Vite + Vue 3 + Tailwind CSS + Lucide Icons 脚手架；
   - 编写 `useGameLoop.ts`（100ms 丝滑高频刷新）与 `useAutoSave.ts`（15s 静默上报）；
   - 组装常驻顶栏 `ResourceBar.vue` 与中央终端控制台骨架。
3. **阶段 3：开局 15 分钟冷启动流程完全跑通**
   - 实现手工翻找废墟逻辑（5次废铁限制）；
   - 实现拼装瓦楞纸箱窝、叙事文字打字机弹出、第一只折耳猫加入待命池；
   - 实现工位增减滑块与猫草农夫自动产粮（S 曲线繁育启动）。
4. **阶段 4：离线结算与稳定性验收**
   - 验证关闭页面后重新进入时的离线收益计算报告弹窗；
   - 验证断粮绝食保护机制与迟滞调度防抖；
   - 达成 Milestone 1 验收标准！

### 5.1 阶段 1 落地状态（2026-09-12 完成）

| 交付项 | 落地位置 | 状态 |
| :--- | :--- | :--- |
| FastAPI + SQLAlchemy 2.0 async + pydantic-settings 骨架 | `backend/main.py`、`app/core/config.py`、`app/core/database.py` | ✅ |
| 全部数值常量唯一出处 | `app/core/balance.py`（§3 ~ §16 全覆盖 + 造价曲线 / 承载力 / 工位上限纯函数） | ✅ |
| 16 张表 ORM 模型 + 建表脚本 | `app/models/*`（按域 7 文件 + `types.py`）、`scripts/init_db.py` | ✅ 实机 MySQL 8.0.43 建表通过 |
| 离线结算引擎（§3 / §4 + §7 / §8 派生量） | `app/core/offline_engine.py` | ✅ 断粮 / 爆仓 / Δt=0 / 时间回拨 / 繁育曲线全覆盖 |
| 新游戏初始化流程 | `app/services/game_init_service.py`（§2.4 全表事务） | ✅ 5 工种 / 11 设施 / 19 科技 / 4 星球 / 3 小游戏 |
| `GET /colony/state`、`POST /colony/snapshot` | `app/api/endpoints/colony.py`、`app/services/colony_service.py` | ✅ 含对账协议（偏差 > 0.5% 记 `SNAPSHOT_DRIFT`） |
| 自动化测试 | `backend/tests/`（引擎 / 表结构 / 初始化 / 接口 / 真机 MySQL） | ✅ 64 用例 SQLite 全绿 + MySQL 实测通过 |
| 阶段 3（服务端）：冷启动 / 工位调度 / 设施建造 | `POST /colony/scavenge`、`POST /colony/dispatch`、`POST /facilities/build` | ✅ 冷启动闭环端到端测试通过（手点 → 第一只猫 → 农夫 → 第二只猫 → 拾荒猫 → 全自动） |
| 阶段 2（前端）：Vue 3 控制台与双时钟 | `frontend/src/`（HUD 双条 / 中央终端 / 设施建造 / 工位调度 / 公频底栏 + `useGameLoop` 100ms + `useAutoSave` 15s） | ✅ `vue-tsc` 类型检查 + `vite build` 通过；真实浏览器走通冷启动五步 |
| 模块 E 科技树（DAG + 科研折扣 + 建造门槛） | `app/services/tech_service.py`、`app/api/endpoints/tech.py`、`frontend/src/components/panels/TechPanel.vue` | ✅ 真机实测：极客猫 1.0/s → 30 算力解锁 Tier 1，Tier 2 随前置自动开放 |
| 模块 M 公频电台 + LLM 场景 3 | `app/core/template_engine.py`、`app/services/{llm_service,radio_service}.py`、`app/schemas/template_agent.py`、`frontend/src/components/radio/RadioTicker.vue` | ✅ 真机实测：点击生成 6 条新语料（Qwen2.5-7B，tokens 332+257） |
| 冷启动补链：拾荒猫拆解产出芯片 | `balance.SCAVENGER_CHIPS_PER_SEC` + `offline_engine` | ✅ 600 秒离线 → 6 芯片，图灵终端可造 |
| 模块 F 三级安防预案 | `offline_engine`（满值判定 / 诱饵 / 静默关灯 / 误报冷却）+ `colony_service._persist_security` + `ThreatBar` | ✅ 9 个用例；诱饵 −30 + 5 分钟冷却、无诱饵转静默关灯（全员停工、×5 衰减到 20 复工）、战车截杀记 `P2_PENDING` 待模块 G |
| 模块 G 载具 / 战斗 / 远征 | `core/combat_engine.py`、`services/combat_service.py`、`endpoints/military.py`、`static/expedition_targets.json` | ✅ 20 个用例；三层抗性（激光破盾 ×1.8 / **装甲减伤走 §9.2.1 的 MOBA 式凸曲线 `75% × 装甲 ÷ (装甲 + 100)`**、结构弹射免死）、破甲削蚀跨战斗持久化、组装机位与乘员校验、维修 60s、远征 `ends_at` 离线可完成、**满警戒度战车截杀真打一场**（胜：清警报 + 缴获 + rage+25；败：车损 + 乘员进急救舱） |
| 模块 G 前端：战备机库（WarRoom） | `stores/military.ts`、`components/panels/WarRoomPanel.vue`（右栏「工位调度 + 战备机库」） | ✅ 真机实测：组装轻装猫车（机位 0→1/12、CP 88、锁 2 只乘员）→ 勾选派遣沃尔玛（返航倒计时）→ 返航后【收取】入库（废铁 160→200 封顶、警戒度 +1.5、载具回待命） |
| 模块 H 水培基因实验室 | `core/garden_engine.py`、`services/garden_service.py`、`endpoints/garden.py`、`static/cat_plants.json`、`stores/garden.ts`、`components/panels/GardenPanel.vue` | ✅ 16 个用例；7×7 格盘与逐格扩建、四阶段生长（90s/阶）、放射枯萎 / 零重力保鲜、**确定性相邻突变**、14 种图鉴解锁、在田光环（荧光苔藓 +5kW / 消音绒草 −0.002 警戒）、机械臂自动收割与未知突变保护；左栏改三 Tab（设施 / 科技 / 猫草） |
| 模块 I 幽灵总线·智械深网 | `core/stock_engine.py`、`services/darknet_service.py`、`endpoints/darknet.py`、`schemas/forum_agent.py`、`stores/darknet.ts`、`components/panels/DarknetPanel.vue` | ✅ 25 个用例；Bitburner 行情（全局冲击 + 个股波动 + 软上限 + 牛熊周期）、现货多空与交易反噬、借券做空（10 分钟合约/利息/爆仓价）、黑市空投与 5 种违禁品、暴露度四段危机、匿名论坛三条线索、LLM 发帖裁判（关键词兜底）+ 10 分钟冷却；左栏改四 Tab |
| 模块 G 尾款：战术指令 / 伏击车队 / 巡航导弹 | `combat_service.tactical_action·ambush_convoy·assemble_missile·launch_missile`、`combat_engine`（超频 DPS 加成 + EMP 眩晕）、`WarRoomPanel` 战术区 | ✅ 15 个用例；超频 −20kWh / EMP −15kWh（下场交火生效且只用一次）、弹射撤离回收 50% 残骸、伏击车队（胜：缴获合金 25 + 芯片 12 + 兵工厂断料停工 30~60 分钟；败：车损 + 乘员进急救舱）、巡航导弹总装与点火（舰队 −20% / rage +40 / 情报 +20%）；真机实测全通 |
| 模块 K 火箭发射井 + 星区星图 | `balance.LAUNCH_SILO_STAGES`、`colony_service.build_facility`（阶段分支）、`endpoints/planet.py`、`FacilitiesPanel` 阶段视图 | ✅ 11 个用例；四阶段依次推进（竖坑 200 废铁 + 20 合金 / 导轨 60 合金 + 15 芯片 / 加注 60 合金 + 15 芯片 + 6 电池 / 总装 60 合金 + 20 芯片 + 4 电池），**阶段④需先摧毁除菌要塞**；点火后解锁二号熔岩星并开放 `/planet/switch`（母星不删档） |
| 模块 O 密电译码 + LLM 场景 1 | `core/minigame_engine.py`、`services/minigame_service.py`、`endpoints/minigame.py`、`services/planet_service.py`、`components/panels/MinigamePanel.vue` | ✅ 15 个用例；密电译码（4 位密钥 / 6 符号 / 每日 3 次 / 反馈只有位置对·符号对 / 成功情报 +8% / 确定性密钥 / **不泄漏答案**）+ 登录新行星生成生态标签（每颗星球 1 次、Pydantic 防火墙 + 本地生态池兜底）；左栏改五 Tab |
| 模块 O 矿脉扫描 + 熔炉配比 | `core/minigame_engine.py`（棋盘/配方生成与判定）、`services/minigame_service.py`（配额恢复/清盘/永久加成）、`MinigamePanel.vue` 8×8 网格与投料台 | ✅ 14 个用例；矿脉扫描（12 处矿脉 / 扫雷数字 / 配额 30 分钟回 1 上限 5 / 扫空不罚 / 清盘换新盘 + 奖励）、熔炉配比（三投料比例 / 三态反馈 / 20 废铁每轮 / 每条配方 +5% 上限 10 条）；真机实测两玩法均命中 |
| 模块 L 欧米茄终局 | `services/boss_service.py`（后台演化 / 突袭拦截 / 三段决战 / 碑文）、`schemas/epilogue_agent.py`、`WarRoomPanel` 终局区 | ✅ 9 个用例；30 分钟后台演化（舰队 +25 / 每 3 周期威胁 +1）、掠夺突袭到期惩罚（−15% + 设施受损）与拦截（胜缴获、败损失减半）、三段决战顺序与物资校验、**三战全胜 + Override Key + LLM 场景 6 通关碑文（断网走本地模板）+ 演职员表** |
| 模块 N3 生涯统计与成就馆 | `services/stats_service.py`（只读聚合 + 徽章点亮）、`endpoints/stats.py`、`balance.ACHIEVEMENTS`（15 枚）、`stores/stats.ts`、`components/panels/StatsPanel.vue` | ✅ 9 个用例；生涯大盘 11 项 + 15 枚徽章目录与《数值平衡表》§17 一一对应、进度按 0～100% 显示、点亮**只写一次**（第二次调用 `newly_unlocked` 为空、`unlocked_at` 不变）、数值回落不熄灭、密电/矿脉小游戏成绩与 Override Key 通关徽章联动、未开局槽位不落库；左栏改六 Tab |
| 模块 N4 多槽位存档 | `services/save_service.py`（15 表打包 / 校验 / 整事务替换）、`endpoints/save.py`、`schemas/save.py`、`stores/save.ts`、`components/panels/SavePanel.vue` | ✅ 11 个用例；`GET /save/slots`（空槽位占位）、导出 Gzip+Base64+SHA-256、**跨槽位导入**（导出槽位 1 直接导入槽位 2 并重定向 `slot_id`）、重复导入幂等替换不产生重复行、改内容不改校验和被识破、非法 Base64/Gzip/JSON 拒绝、未知表拒绝、高于后端的 `save_version` 拒绝、空槽位导出 404、**全局自增主键重分配 + 军事状态引用回写**（真机 MySQL 实测修复）；左栏改七 Tab |
| 模块 E4 外星球特化科技树 + LLM 场景 2 | `services/planet_tech_service.py`、`schemas/tech_agent.py`、`static/fallback_techs.json`、`planet_service` / `tech_service.reroll_tech` 挂载点 | ✅ 7 个用例；每颗外星球 12 节点（5×500 / 4×2,000 / 3×8,000）、首次登录**一次性批量生成**且幂等（再切回不重复调 LLM）、Pydantic 防火墙（名称/机制枚举/**禁改成本·阶梯·前置**）、逐张收卡（坏卡丢弃 + `rejected_cards` 计数）、本地卡池兜底、重 Roll 只换卡面（成本/进度不变）且 10 分钟冷却、母星手工节点仍 `400 TECH_NOT_REROLLABLE` |
| 模块 H2 异星植物学图鉴 + LLM 场景 5 | `services/plant_codex_service.py`、`schemas/plant_agent.py`、`garden_state.codex_papers`、`garden_service` 三个收录挂载点、`GardenPanel` 论文折叠区 | ✅ 6 个用例；新猫草首次收录补写 1 篇论文（**每种 1 次、幂等**，第二次采摘不重复生成）、Pydantic 防火墙（标题 4~24 字 / 正文 20~220 字 / 禁可疑内容）、失败与断网走本地模板文案、**离线机械臂路径不调 LLM**（离线补算绝不等网络）、`/garden/state` 暴露 `codex_papers` |
| 模块 N5 读档接口 | `endpoints/game.py`（复用 `colony_service.load_state`） | ✅ 6 个用例；`GET /game/load` 与 `/colony/state` 同构但语义更严：**不自动建档**（空槽位 404 `SAVE_NOT_FOUND` 且不偷偷建行）、**读档即激活**目标星球（`save.active_planet_id` + `is_active` 唯一）、未解锁星球 400 `PLANET_LOCKED`、**外星球基地未实现时 400 `STAR_COLONY_NOT_IMPLEMENTED` 且零副作用**（不生成生态标签/特化科技树）、重复读档幂等；~~接口清单至此 68/68 全部实现~~（**该数字有误，见 v2.53 更正**） |
| 模块 E5 科技效果接入结算 | `balance.TECH_ACTIVE_EFFECT_KEYS`（白名单）、`tech_service.unlocked_effects`、`colony_service.get_tech_effects`、`offline_engine.power/catnip` 公式、`schemas/colony.py` 的 `tech_effects` | ✅ 4 个用例；首个接入项 `catnip_efficiency`：`产粮/s = 农夫数 × 0.2 × 士气 × (1 + 加成)`（**独立乘算因子**，不与被士气吞掉）、满配上限 +60%、锁定节点贡献 0、**非白名单键（如 `power_kw`）绝不偷偷生效**；`/colony/state` 新增 `tech_effects` 块，玩家可以在面板上核对自己到底吃到了多少加成 |
| 模块 E5 前端镜像 | `stores/colony.ts` 的 `techEffects` + `catnipRate`、`types/game.ts`、`ProductionPanel` 的「猫薄荷 +20%（科技）」徽标 | ✅ 前端插值**同乘科技系数**（漏乘会让预测值低 20%、每 15 秒误报 `SNAPSHOT_DRIFT`，本轮一并修掉）；工位调度标题栏如实标出当前科技加成百分比 |
| 星际第①步 外星球基地建行 | `planet_service.ensure_star_colony`、`endpoints/game.py` / `endpoints/planet.py` 触发器、`tests/test_star_colony.py` | ✅ 5 个用例；解锁后切星/读档即按母星同构建 **5 张表的行**（`colony_state`/`labor_buckets`/`facility_state`/`military_state`/`garden_state`）、**锚点写"解锁那一刻"**（真机实测：写 0 会把历史时间补算成收益）、初始资源与猫口全 0、建行幂等（不重置进度与锚点）、未解锁星球仍 `400 PLANET_LOCKED` 且不建行；`/game/load` 对外星球从"如实拒绝"转为**正常读档** |
| 星际第②步 迁猫与跨星航线 | `planet_service.migrate_cats` / `settle_routes` / `route_slots`、`endpoints/planet.py` 的 `POST /planet/migrate`、`planet_state.logistics_routes`、`tests/test_star_routes.py` | ✅ 6 个用例；出发即离港（在途两边不占工位）、单趟 60 秒 1~20 只、带宽 2+物流官封顶 4（满了 `STAR_ROUTE_SLOTS_FULL`）、**被劫掠 8% 只延误 10 分钟绝不丢猫**（`sha256` 确定性判定，实测可复现）、星图与任意读档自动结算到点航线、目标基地缺失时猫口退回母星；接口总数 68 → **69** |
| 星际第③步首片 星球承载力 | `balance.cat_capacity_on()` + `STAR_PLANET_CAPACITY_MULTIPLIER`、`colony_service` 三处调用点、`tests/test_star_capacity.py` | ✅ 2 个用例；`K =（纸箱窝×1 + 猫猫公寓×10）× 星球系数`（熔岩星 0.8 / 冰卫星 0.9 / 星带 1.2），**单点实现、三处共用**；母星口径向后兼容不变（`cat_capacity()` 仍等价于 `planet_id=0`）；真机实测同级住房母星报 20、熔岩星报 16 |
| 星际前端 星区星图面板 | `stores/planet.ts`、`components/panels/PlanetPanel.vue`（左栏第 8 个 Tab） | ✅ 星球列表（解锁态 / 生态标签 / 当前星）、在途航线与**逐秒倒计时**（被劫掠标红但注明"猫一只不少"）、切星按钮、迁猫表单（目标星 + 数量 1~20 + 派出）；迁猫成功后在中央终端播报 ETA 与在途带宽占用；`npm run build` 通过 |
| 星际第③步第二片 星球产粮 | `balance.planet_catnip_multiplier()` + `STAR_PLANET_CATNIP_MULTIPLIER`、`offline_engine` 的产粮公式、`tests/test_star_catnip.py` | ✅ 2 个用例；`产粮/s = 农夫 × 0.2 × 士气 × (1 + 科技加成) × 星球系数`（熔岩星 0.7 / 冰卫星 1.0 / 星带 0.9），作为**独立乘算因子**不与科技加成混淆；母星口径不变；实测同 2 农夫 300 秒，熔岩星约为母星的 70% |

**阶段 2 落地补充**

* 前端**不重复抄写定义**：`backend/static/*.json` 通过 `app.mount("/static", StaticFiles(...))` 直接提供给前端读取（"定义进 JSON"原则的落地方式，不占用 API 清单）；
* 前端只保留 `src/utils/balance.ts` 这一份**显示用数值镜像**（0.05/s 消耗、r=0.01、警戒度系数等），仅喂 100ms 插值；任何结算以后端重算为准。改数值需同时改《数值平衡表》与 `backend/app/core/balance.py`；
* 前端把 `serverResources`（权威）与 `resources`（显示插值）双值分离，15s 快照提交的是显示层预测值，正好触发后端 0.5% 对账告警链路；
* 开发代理：`/api` 与 `/static` 默认反代到 `http://127.0.0.1:8000`；端口被占用时用 `VITE_BACKEND_TARGET` 覆盖；
* 离线报表弹窗阈值：**离线 ≥ 60 秒**才弹（页面刷新这类几秒空档不打扰），对齐验收清单 A-1 的"关闭页面一小时后重进"；
* `/colony/dispatch` 支持 `delta = 0 + policy`：只更新迟滞换班策略、不动工位（前端策略面板用）。
* 底栏公频电台（模块 M）已接真数据：`GET /radio/feed`（本地填槽 + LRU，0 Token）与
  `POST /radio/generate`（LLM 场景 3，点一次生成 6 条）；页面上的「生成语料」按钮实测可让语料池 28 → 34。
* 底栏公频电台（模块 M）已接真数据：`GET /radio/feed`（本地填槽 + LRU，0 Token）与
  `POST /radio/generate`（LLM 场景 3，点一次生成 6 条）；页面上的「生成语料」按钮实测可让语料池 28 → 34。

**阶段 1 暂缓项**（按里程碑推进，不属于本阶段）：

* `combat_engine.py` / `stock_engine.py` / `labor_engine.py` / `template_engine.py` —— 对应模块 G / I / C3 / M；
* `static/` 下的 `cat_plants.json`、`vehicle_types.json`、`expedition_targets.json`、`doctrines.json`、`templates_default.json`、`fallback_techs.json` —— 随对应玩法落地；
* `GET /game/load`、`/colony/dispatch`、`/save/*` **均已完成**（模块 N5 / 阶段 3 / 模块 N4）；阶段 1 的暂缓项至此全部补齐。

---

## 6. 环境变量与 LLM 调用策略

### 6.1 `.env.example` 变量清单

```env
# ---- 数据库 ----
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=dawn_meow

# ---- LLM（星区推演导演）----
LLM_PROVIDER=deepseek
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=                  # 只写进本地 .env，绝不提交、绝不上传
LLM_MODEL=deepseek-chat
LLM_TIMEOUT_SECONDS=30
LLM_MAX_RETRY=2
LLM_BATCH_SIZE_MAX=200        # 单次语料批处理上限
LLM_DAILY_CALL_BUDGET=200     # 每日调用次数上限
LLM_DAILY_TOKEN_BUDGET=200000 # 每日 token 预算

# ---- 游戏 ----
GAME_TICK_MS=100              # 前端高频插值周期
AUTOSAVE_INTERVAL_MS=15000    # 静默快照周期
LOG_LEVEL=INFO
```

### 6.2 LLM 调用场景（全案仅 6 处，低频高影响）

| # | 场景 | 触发频率 | 失败兜底 |
| :--- | :--- | :--- | :--- |
| 1 | 新行星登录：生成生态环境与词缀 | 每颗星球 1 次 | 本地生态标签池 |
  | 2 | 生成机制型特化科技卡 | 每颗外星球 **1 次批量**（≤12 张卡）+ 每次重 Roll 1 张 | `fallback_techs.json` |
| 3 | 时代/星球语料批处理 | 每次跨时代 1 次，**最多 200 条** | `templates_default.json` |
| 4 | 玩家发帖做局的意图与煽动星级裁判 | 每次发帖（10 分钟冷却） | 本地关键词规则打分 |
  | 5 | 首次杂交出新图鉴的《异星植物学图鉴》 | 每种图鉴 1 次（全案 ≤ 25~30 次） | 本地模板文案（离线路径不走 LLM） |
| 6 | 通关文明史诗碑文 | 全案 1 次 | 本地模板碑文 |

### 6.3 降级与预算策略

1. **校验链**：Pydantic Schema → DAG 前置校验 → 本地静态池抽卡（三道防火墙）；
2. **重试**：校验或超时失败自动重试最多 `LLM_MAX_RETRY`（2）次，仍失败直接抽兜底池，**玩家零感知**；
3. **预算熔断**：达到 `LLM_DAILY_CALL_BUDGET` 或 `LLM_DAILY_TOKEN_BUDGET` 后**自动停止调用**，全部走本地语料池，界面温和提示"星区推演导演今日已下线"；
4. **例外放行**：场景 6（通关碑文）作为"一生一次"的场合，允许超预算放行一次；
5. **可观测**：每次调用记录「场景 / 耗时 / prompt token / completion token / 是否走兜底」，便于事后查账。

---

## 7. 星球作用域矩阵（v2.39 半截链路审计产出）

> **为什么要有这张表**：星际内容上线后，出现了两次"文档说 A、代码做 B"的不一致（外星球死锁、手点废墟能在外星球用）。
> 根因是**没有一张表说清"哪些机制属于整档、哪些属于每颗星球、哪些只属于母星"**。新增机制时请先在这张表登记作用域，再写代码。

| 机制 | 作用域 | 数据落点 | 代码依据 |
| :--- | :--- | :--- | :--- |
| 手点废墟 | **母星专属** | `colony_state.manual_scavenge_clicks`（每星球一列，但只允许母星写入） | `manual_scavenge` → `400 COLD_START_HOME_ONLY` |
| 火箭发射井 / 升空 | **母星专属** | `facility_state(launch_silo)`（每星球一行，只允许母星升级） | `build_facility` → `400 LAUNCH_SILO_HOME_ONLY` |
| 资源与人口、设施、工位 | **每星球独立** | `colony_state` / `facility_state` / `labor_buckets` | `ensure_star_colony` 五表建行（§2.5）；清单声明在 `planet_service.STAR_COLONY_TABLES`，由 `test_scope_matrix` 断言"声明 == 实际" |
| 水培实验室 | 每星球独立 | `garden_state`（含 `codex_papers`） | 建行即就绪，实测可播种 |
| 机库与载具 | 每星球独立 | `military_state` / `vehicle_units(planet_id)` | `_military()` 按星球取行 |
| 科技树 | 每星球独立 | `tech_records(planet_id)` | 母星 19 手工节点、外星球 12 动态节点 |
| 小游戏配额与成绩 | 每星球独立 | `minigame_state(slot, planet)` | 按 `minigames.json` 的解锁星球建行 |
| 离线结算与报表 | 每星球独立 | `colony_state.last_tick_time` 为锚点 | `settle_offline(planet_id)` |
| 跨星航线（迁猫 + 带货） | **存档级**（行挂在目标星的 `planet_state.logistics_routes`） | `planet_state.logistics_routes` | `migrate_cats` / `settle_routes` |
| 智械深网（行情 / 做空 / 黑市 / 论坛） | **存档级**（状态）／资金走当前星球 | `darknet_state(slot)` / `forum_posts(slot)`；出入金记在 `colony_state(slot, planet)` | 行情与暴露度全局唯一；`planet_id` 只用来决定"从哪颗星的仓库扣/收"（默认母星） |
| 欧米伽后台演化 / 突袭 / 决战 | **存档级**（战斗在地面由某星球机库发起） | `boss_state(slot)` | `boss_service` 默认 `planet_id = 母星` |
| 文明凝聚力与政令 | **存档级** | `save_slot.unity` / `save_slot.doctrines` | 全星系共享 |
| 生涯统计与成就徽章 | **存档级** | `career_stats(slot)` / `achievements(slot)` | 跨星球汇总 |
| 多槽位存档 / 导入导出 | **存档级** | 15 张带 `slot_id` 的表 | `save_service` |
| 公频电台语料池 | **存档级** | `event_templates(slot)` | 与星球无关 |

**判据（新增机制时照这个问）**

1. 它是"这颗星球在干什么"吗？→ 每星球独立（表里带 `planet_id`，建行走 `ensure_star_colony`）；
2. 它是"整个文明在干什么"吗？→ 存档级（表里只带 `slot_id`）；
3. 它依赖母星独有的叙事或地貌（避难所废墟、地壳厚度）吗？→ 母星专属，并且**必须加显式校验 + 明确错误码**，不能靠"玩家不会那么做"。

---

## 修订记录

| 版本 | 日期 | 内容 |
| :--- | :--- | :--- |
| v1.0 | 2026-09-12 | 首次成文：工程架构、目录树、后端引擎详解、前端双时钟与 Pinia 分仓、API 契约、Milestone 1 实施路径 |
| v1.1 | 2026-09-12 | 收尾修订：目录树根定名为 `dawn-for-meow/`，补 `core/balance.py` 与 10 个 JSON 种子；离线公式对齐《数值平衡表》（消耗 0.05/s、r=0.01、断粮农夫 30%）；store 字段与表结构对齐；第 4 章重写为《API 定稿》（33 个接口）；新增第 6 章《环境变量与 LLM 调用策略》 |
| v1.2 | 2026-09-12 | 阶段 1 落地校正：§5 阶段 1 表数 13 → **16**（对齐数据库设计定稿）；目录树补 `core/errors.py`、`core/seed_loader.py`、`models/types.py`、`schemas/common.py`、`services/game_init_service.py`、`scripts/init_db.py`、`tests/`、`pytest.ini`；`models/` 标注改为 16 张表按域分文件；新增 §5.1《阶段 1 落地状态》 |
| v1.3 | 2026-09-12 | 命名收敛：工位相关字段全部改用 `job_id` 单数（`farmer/scavenger/geek/power_runner/crew`），`/colony/dispatch` 的 `role` 同步为 `job_id`，消除与《术语表》ID 规范的复数别名冲突 |
| v1.4 | 2026-09-12 | 阶段 3（服务端）落地：API 清单 33 → **34**，新增 `POST /colony/scavenge`（冷启动手点废墟）；§4.3 补齐 scavenge / dispatch / build 的请求响应、校验顺序与错误码，并写明"建造暂不做科技硬门槛 + 第一座纸箱窝带来第一只猫"；§5.1 补阶段 3 交付行；`/facilities/build` 示例设施 ID `farm_plots` → `farm_plot` |
| v1.5 | 2026-09-12 | 阶段 2（前端）落地：§5.1 补交付行与《阶段 2 落地补充》（static 静态挂载、前端显示镜像、双值分离、开发代理与 `VITE_BACKEND_TARGET`、离线弹窗阈值）；`/colony/dispatch` 支持 `delta=0 + policy` 只更新迟滞策略 |
| v1.6 | 2026-09-12 | 模块 M（公频电台）落地：API 清单 34 → **36**，新增 `GET /radio/feed`（本地填槽 + LRU，0 Token）与 `POST /radio/generate`（LLM 场景 3 语料批处理）；落地 `core/template_engine.py`、`services/llm_service.py`、`services/radio_service.py`、`schemas/template_agent.py`、`static/templates_default.json`；LLM 默认走硅基流动 + 便宜小模型（`Qwen/Qwen2.5-7B-Instruct`），预算熔断账本落 `backend/var/llm_usage.json`；防火墙 1 改为**逐条**校验（一条不合格不废整批） |
| v1.7 | 2026-09-12 | 模块 E（科技树）落地：API 清单 36 → **39**，新增 `GET /tech/tree`、`POST /tech/research`、`POST /tech/reroll`；落地 `services/tech_service.py`、`schemas/tech.py`、`endpoints/tech.py`、`stores/tech.ts`、`components/panels/TechPanel.vue`；离线结算把研究算力注入在研节点、建造进阶设施需科技解锁（开荒组件豁免，含图灵终端）；冷启动补链：拾荒猫拆解产出芯片 0.01/s |
| v1.8 | 2026-09-12 | 模块 F（三级安防预案）落地：满警戒度自动处置（诱饵 →（模块 G 就绪前）静默关灯）、误报冷却与加速衰减、静默期全员停工；`/colony/state` 增 `security` 块（诱饵数 / 冷却剩余 / 关灯态 / 策略）供 HUD 展示 |
| v1.9 | 2026-09-13 | 模块 G（载具/战斗/远征）落地：API 清单 39 → **45**，新增 `GET /vehicle/list`、`POST /vehicle/{assemble,modify,repair}`、`POST /military/{dispatch,collect}`；`core/combat_engine.py`（三层抗性 + 战力评分 + 士气）、`services/combat_service.py`（组装/维修/拆解/远征/急救舱/战车截杀）、`static/expedition_targets.json`；满警戒度 P2 由占位改为真交火；**JSON 列就地改 dict 会漏写库**的坑已修（必须整条复制后替换） |
| v2.0 | 2026-09-13 | 模块 G 前端落地：`stores/military.ts` + `components/panels/WarRoomPanel.vue`（机位条 / 逐辆三层血条与装甲上限 / 状态徽章 / 组装下拉 / 远征派遣与收取 / 急救舱倒计时）；右栏改为「工位调度 + 战备机库」双面板；真机走通组装→派遣→收取全链路 |
| v2.1 | 2026-09-13 | 模块 H（水培实验室）落地：API 清单 45 → **47**，新增 `GET /garden/state`、`POST /garden/action`（PLANT/HARVEST/MEDIUM/EXPAND/ARM）；`core/garden_engine.py`（阶段/枯萎/突变/光环/扩建选址）、`services/garden_service.py`、`static/cat_plants.json`（14 种四流派图鉴）；在田光环接入净电力与警戒度；左栏改三 Tab；**再次修掉"JSON 列就地改 dict 漏写库"**（播种/采摘/扩建三处） |
| v2.2 | 2026-09-13 | 模块 I（智械深网）落地：API 清单 47 → **55**，新增 `GET /darknet/state`、`POST /darknet/{stock/trade,short,market/trade,forum/post,reroll-id,data-eye}`、`GET /darknet/forum/feed`；`core/stock_engine.py`（Bitburner 行情 + 交易反噬 + 爆仓价）、`services/darknet_service.py`、`schemas/forum_agent.py`（发帖裁判防火墙）；离线结算接入行情 tick / 做空到期 / 空投送达；左栏改四 Tab |
| v2.3 | 2026-09-13 | 模块 G 尾款落地：API 清单 55 → **59**，新增 `POST /military/{tactical-action,ambush-convoy,assemble-missile,launch-missile}`；`combat_engine.resolve_skirmish` 支持超频 DPS 加成与 EMP 敌方眩晕；欧米茄车队班次（20~30 分钟）与兵工厂断料停工状态写入 `boss_state`；合金仓储上限 50 → 200（对齐载具造价）；顺带修掉一个秒粒度脆弱测试 |
| v2.4 | 2026-09-13 | 模块 K（发射井 + 星图）落地：API 清单 59 → **61**，新增 `GET /planet/state`、`POST /planet/switch`；发射井四阶段造价定稿并开放建造（`LAUNCH_SILO_STAGES`），阶段④以"除菌要塞已毁"为前置；`/colony/state` 增 `launch_silo` 块（阶段进度/下一阶段造价/是否被要塞挡住/是否已升空）；前端设施面板显示阶段名与"需先摧毁除菌要塞"提示 |
| v2.5 | 2026-09-13 | 模块 O 首个玩法 + LLM 场景 1 落地：`core/minigame_engine.py`（确定性密钥 + Mastermind 反馈）、`services/minigame_service.py`（配额/奖励/成绩）、`GET /minigame/list` 与 `POST /minigame/action` 转正式实现（接口总数不变）、`services/planet_service.py`（新行星生态标签：LLM + 本地生态池 + 幂等）、左栏改五 Tab；密电译码成功直接联动伏击车队的 `intel_level` |
| v2.6 | 2026-09-13 | 模块 O 补齐：矿脉扫描（8×8 扫雷式，12 处矿脉，配额 30 分钟回 1/上限 5，清盘换新盘）与熔炉配比（三投料比例，反馈只有太热/太冷/比例偏差，每条配方永久 +5% 熔炼上限 10 条）；两者与密电译码共用 `minigame_state`，**新增玩法不改表结构**的设计得到三次验证 |
| v2.7 | 2026-09-13 | 模块 L（欧米茄终局）落地：API 清单 61 → **63**，新增 `POST /military/intercept-raid`、`POST /military/final-assault`；`services/boss_service.py`（后台演化 / 突袭 / 三段决战 / 通关碑文）、`schemas/epilogue_agent.py`（场景 6 防火墙）；离线结算接入 BOSS 演化与突袭惩罚；`/vehicle/list` 暴露舰队战力 / 突袭倒计时 / 决战进度 / 碑文；前端 WarRoom 加「欧米茄终局」操作区 |
| v2.8 | 2026-09-13 | 模块 N3（生涯统计与成就馆）落地：API 清单 63 → **65**，§4.9 补齐 `GET /stats/career`、`GET /stats/achievements` 的响应与点亮规则；`services/stats_service.py` 只读聚合各系统指标并结算 15 枚徽章（`unlocked_at` 幂等写入、未开局槽位不落库）、`balance.ACHIEVEMENTS` 为唯一出处、前端 `StatsPanel.vue` 第 6 个左栏 Tab |
| v2.9 | 2026-09-13 | 模块 N4（多槽位存档）落地：API 清单 65 → **68**，兑现 §4.10 的 `GET /save/slots`、`GET /save/export`、`POST /save/import`；`services/save_service.py` 把 15 张带 `slot_id` 的表打包成 Gzip+Base64+SHA-256 信封（`event_templates` 除外），导入走"校验 → 迁移 → 单事务整体替换"，未知表/校验和不符/高版本一律拒绝；前端 `SavePanel.vue` 第 7 个左栏 Tab（槽位切换、导出复制、粘回导入） |
| v2.10 | 2026-09-13 | 模块 N4 真机修正：跨槽位导入 `vehicle_units` / `forum_posts` 的**全局自增主键**会撞 `PRIMARY`（MySQL 实测 `Duplicate entry '1'`），改为交回数据库重分配并把 `military_state` 的 `unit_id` / `unit_ids` 引用按映射回写；该冲突在 SQLite 用例里被 `test_cross_slot_import_reassigns_autoincrement_ids` 固化；README 首页按"68 接口 / 七面板"重写并如实标注 LLM 场景 4 已接入 / 2 未实现 |
| v2.11 | 2026-09-13 | 模块 E4（外星球特化科技树）+ LLM 场景 2 落地：§6.2 场景 2 改为"每颗外星球 1 次批量（≤12 张卡）+ 每次重 Roll 1 张"；`services/planet_tech_service.py` 铺树与单卡重 Roll、`schemas/tech_agent.py` 防火墙（LLM 只填命题，成本/阶梯/DAG 由 Python 夹紧）、`static/fallback_techs.json` 三颗星球共 36 张兜底卡；`/planet/switch` 顺带铺树并回传 `specialized_techs`、`/tech/reroll` 真正刷新卡面；接口总数不变（68），前端复用既有 TechPanel 的"重 Roll"入口并回显新卡名与来源 |
| v2.12 | 2026-09-13 | 模块 H2（异星植物学图鉴）+ LLM 场景 5 落地：**六个 LLM 场景全部接完**；`services/plant_codex_service.py`（论文生成 + 本地模板）、`schemas/plant_agent.py` 防火墙、`garden_state.codex_papers`（JSON 列，表数仍 16 张）、`/garden/state` 暴露 `codex_papers`、采摘/机械臂收录处挂载；离线路径 `allow_llm=False` 保证离线补算不等网络；目录树补 `planet_tech_service.py` / `plant_codex_service.py` / `plant_agent.py` / `epilogue_agent.py` |
| v2.13 | 2026-09-13 | 模块 N5 落地：`GET /game/load` 补齐（接口清单 **68/68 全部实现**）——与 `/colony/state` 共用离线补算内核但语义更严（不自动建档 / 读档即激活 / 未解锁星球与未实现的星球基地如实拒绝且零副作用）；§4.3 补该接口口径、§5 阶段 1 暂缓项标注全部补齐 |
| v2.14 | 2026-09-13 | 模块 E5 落地：科技 `buff_payload` 从"全体声明性"改为**白名单制接入结算**——`TECH_ACTIVE_EFFECT_KEYS`（当前仅 `catnip_efficiency`）、`tech_service.unlocked_effects` 汇总已解锁节点、`catnip` 产出公式乘上 `(1 + 加成)` 且上限 +60%、`/colony/state` 新增 `tech_effects` 块；真机验证"解锁 Tier 1 猫草水培基础后同一批农夫产量 ×1.2"（离线引擎与展示口径一致） |
| v2.15 | 2026-09-13 | 模块 E5 前端镜像补齐：`stores/colony.ts` 的 100ms 插值 `catnipRate` 同乘 `tech_effects.catnip_efficiency`，**修掉"前端预测值比后端低 20% ⇒ 每次快照误报对账告警"的隐患**；工位调度标题栏新增「猫薄荷 +N%（科技）」徽标，兑现"玩家能核对加成"的承诺 |
| v2.16 | 2026-09-13 | 星际第①步落地（《数值平衡表》§15.3 / 《数据库设计定稿》§2.5）：`planet_service.ensure_star_colony` 在切星与读档时按母星同构建 **5 张表的行**，锚点写"解锁那一刻"；`GET /game/load` 对已解锁外星球从 `400 STAR_COLONY_NOT_IMPLEMENTED` 转为正常读档（`/colony/state` 保持只读不建行，旧档自愈保留）；真机踩到并修掉"只建 3 张表 ⇒ 读档在 `military_state` 处 404"的坑 |
| v2.17 | 2026-09-13 | 星际第②步·迁猫与跨星航线落地：API 清单 68 → **69**（新增 `POST /planet/migrate`）；`planet_service.migrate_cats/settle_routes/route_slots`、`planet_state.logistics_routes` 承载在途航线（不改表结构）、出发即离港、被劫掠只延误不丢猫、星图与读档双结算点 |
| v2.18 | 2026-09-13 | 星际第③步首片落地：承载力按星球计算（`cat_capacity_on` + 三颗星系数 0.8/0.9/1.2），母星口径向后兼容；离线引擎、状态报文与建造响应三处共用同一函数，避免"面板显示一套、结算用另一套" |
| v2.19 | 2026-09-13 | 星际第③步第二片落地：星球产粮系数（熔岩星 0.7 / 冰卫星 1.0 / 星带 0.9）作为独立乘算因子接进离线引擎产粮公式，单点实现 `planet_catnip_multiplier()`；用例改用 2% 容差规避秒级时间截断造成的脆弱断言 |
| v2.20 | 2026-09-13 | 第③步第三片落地：`power_kw` 进白名单并接入电力**发电侧**（`power_balance(..., tech_power_kw=…)`，离线引擎与展示报文共用同一口径）；用例改为"白名单键生效 / 非白名单键不生效"双向断言；顺带修掉航线用例的偶发失败（route_id 含时间戳 ⇒ 被劫掠判定随机命中，现按用例显式关闭劫掠） |
| v2.21 | 2026-09-13 | 第③步收官片落地：**星球专属产出加成**（`planet_output_multiplier()`：熔岩星 0.9/1.1、冰卫星 1.0/1.3、星带 1.25/0.9）接进拾荒废铁与芯片产出；**§15.3 三步全部落地**。顺带修两个真机暴露的问题：兜底卡名"氦-3 精馏塔"含空格过不了防火墙（改为"氦三精馏塔"）、`suspicion` 在 SQLite 下被序列化成整数 `0`（显式转 float） |
| v2.22 | 2026-09-13 | 星际玩法补前端：新增 `stores/planet.ts` 与 `PlanetPanel.vue`（左栏第 8 个 Tab「星区星图」）——星球列表与解锁态、在途航线逐秒倒计时、切星与迁猫表单；星际内容不再是"只有接口没有 UI" |
| v2.23 | 2026-09-13 | §6.4 三类载荷的**接口级可见性**落地：`GET /tech/tree` 的 `TechNode` 新增 `active_effects`（白名单内、真在结算）与 `pending_effects`（数字型但未接线），把"哪些加成是实的"从口头承诺变成接口事实；用例覆盖"已接线 / 未接线 / 新接一个白名单键立刻生效"三种情形 |
| v2.24 | 2026-09-13 | 全军装甲加成落地（第③步收尾）：`resolve_skirmish` 新增 `attacker_armor_bonus` 单一入口，`fleet_armor` + `armor_bonus` 合并生效（`combat_service._fleet_armor_bonus` 供远征/伏击/突袭/决战共用，只加我方）；用例覆盖"装甲 ×1.5 且敌方不变、零加成时数值与原始锚点一致" |
| v2.25 | 2026-09-13 | `battery_kwh_max` 接入电容池：结算与建造两处统一按"基础 200 + 科技扩容"重算（幂等、不累加），只认白名单键；技术验收用例同步改为"白名单键集合"断言，避免每加一个键就要改一次测试 |
| v2.26 | 2026-09-13 | 熔炼玩法第一步落地（《数值平衡表》§3.5 口径定稿）：`static/facilities.json` 与 `balance.FACILITY_SPECS` **双份同步**新增【高频感应电炉】（废铁 120 + 芯片 25 / 递增 1.4 / 科技门槛【冶炼入门】），设施总数 11 → 12；电力侧正式接线 `induction_furnaces`（−10 kW/座，欠载即停炉），把早已预留的电炉负载从"永远为 0"变成真实数据 |
| v2.27 | 2026-09-13 | 熔炼循环落地（§3.5 完成）：离线引擎按"可炼炉次 = min(时间, 废铁, 合金仓)"把废铁 10:1 转合金，欠载停炉并在 note 留痕；`smelt_speed` / `smelt_yield` 进白名单，速度加成 = 科技 + 小游戏配方（相加后乘在炉次速率上）；离线报表与 `OfflineReport` Schema 新增 `smelted_batches` / `gained_alloys`（**又一次踩到 Pydantic 静默过滤**——引擎写了、报文裁剪函数没带出来，两处都要补） |
| v2.28 | 2026-09-13 | 排查一次**无法复现的偶发失败**（`test_stats` 的 `achievements_unlocked` 期望 0 得到 1）：连跑三次全量与两次定序子集均绿，未能复现；按"偶发绿灯不算绿"原则，在断言里加了**诊断信息**（失败时直接打印被点亮的徽章 ID），下次若复现即可定位；推测与跨模块真实 MySQL 连接配置或时间截断有关，留待复现后处理 |
| v2.29 | 2026-09-13 | 熔炼可见化：`OfflineReport` 类型补 `smelted_batches` / `gained_alloys`，离线报表弹窗在"机械废铁"下方显示「航空钛合金 x（电炉 N 炉次）」，中央终端读档播报也带上合金与炉次；熔炼从此不只在数值上生效，玩家也能看见 |
| v2.30 | 2026-09-13 | 车载模块落地（《数值平衡表》§9.10）：`balance.VEHICLE_MODULES`（3 种）+ `VEHICLE_MODULE_SLOTS`（1~2 槽）+ `vehicle_module_slots()`；`combat_service.equip_module / unequip_module`（科技门槛 → 槽位 → 材料三段校验，拆卸返还 50%）；`/vehicle/modify` 的 `action` 扩到 `SCRAP / EQUIP / UNEQUIP` 并新增 `module_id` 字段；**红灯区 `vs_shield` / `module` 双双解除**——它们本就是模块属性，不是全军加成 |
| v2.31 | 2026-09-13 | 模块效果接入战斗（§9.10 闭环）：`combat_engine.apply_module_effects()` 写快照（DPS / 装甲 / `vs_shield`），`resolve_attack` 新增 `attacker_vs_shield` 并与全局相克表**取较大值**；伏击与外派两条战斗链的快照构建处各调一次；用例覆盖"动能车装激光对盾 0.8→1.8""激光车装它仍是 1.8（防双记账）""未知模块 ID 被忽略"；`armor_shred` 明确列为**待定稿**（改动会影响既有平衡） |
| v2.32 | 2026-09-13 | 模块装配补前端：`/vehicle/list` 新增 `vehicle_modules` 目录（名称/槽位/造价/门槛科技/效果）与每车型 `module_slots`；战备机库每辆车下方新增模块行（已装模块可点 ✕ 拆卸、下拉选装、待命态才可操作）；`military` store 增 `equipModule`/`unequipModule`，操作后播报槽位占用与扣料/返还；顺带修掉 `VehicleView.modules` 的**类型漂移**（前端写成 `{slot, module_id}[]`，后端实际是字符串数组） |
| v2.33 | 2026-09-13 | 科技面板把 §6.4 的三类载荷画出来：每个节点新增「生效中：猫薄荷 ×+20%」（accent）与「卡面机制（待接线）：battery_kwh_max 200」（dim）两行，百分比类键自动换算成人话；接口上轮已透出的 `active_effects` / `pending_effects` 至此**玩家可见**，不再需要读接口才知道哪些加成是真的 |
| v2.34 | 2026-09-13 | `armor_shred` 落地（§9.10 收尾）：`resolve_attack` 新增 `attacker_armor_shred`，与基础 30% **取较大值**；钻头模块 45%（`ARMOR_SHRED_RATIO_DRILL`），`apply_module_effects` 一并写入快照；**量化发现破甲机制偏弱**（对比脚本只读分析：关底战 45 回合多削约 20 点装甲），已在《数值平衡表》§9.10 列出三个改法待拍板 |
| v2.35 | 2026-09-13 | 装甲减伤换曲线（《数值平衡表》§9.2.1 拍板 K=100）：`armor_reduction_ratio()` 从"线性 ÷400 封顶 75%"改为凸曲线 `75% × 装甲 ÷ (装甲 + 100)`，`ARMOR_REDUCTION_CURVE_K` 新增、旧分母保留为历史对照；关底近卫装甲 500 → 430；回归用例改为"凸曲线无平段（200/300/400/500 四个值互不相同）+ 装甲 200 处仍 50% + 渐近不触顶"；分析脚本 `scripts/armor_curve_analysis.py` 增列编队战例 |
| v2.36 | 2026-09-13 | 外星球开荒引导（App.vue 中央终端）：切到外星球且基地为空时，**优先于母星冷启动步骤**显示引导块——说明"按 §15.3 初始资源/设施/猫口全 0，需要先从母星运猫"，提示该星球的承载力/产粮性格差异，并给两个动作按钮（「去星区星图运猫」直接切左栏 Tab、「回母星调度」调 `planetStore.switchTo(0)`）；`bootstrap()` 顺带拉一次 `/planet/state` 以显示星球名 |
| v2.37 | 2026-09-13 | 跨星物流可运物资 + 外星球开荒清单：**修掉"外星球死锁"**（初始资源 0 且无手点废墟、航线只能运猫 ⇒ 永远造不出纸箱窝）——`/planet/migrate` 新增 `cargo`（单趟废铁 60，出发即扣、抵达入库按仓储夹紧、超量/不支持/不足明确拒绝）；星图面板新增「随船废铁」输入；中央终端在猫口落地后显示**外星球开荒清单**（运猫 → 纸箱窝 → 农田 → 派农夫 → 拾荒台，带一键执行按钮） |
| v2.38 | 2026-09-13 | **半截链路审计**：① 发现并修掉"手点废墟在外星球同样可用"——`manual_scavenge` 现校验 `planet_id == 母星`，否则 `400 COLD_START_HOME_ONLY`（否则外星球点 40 次即可绕过跨星物流全套机制）；② 补用例确认外星球水培实验室可用（`garden_state` 随 `ensure_star_colony` 就绪、初始母本与 9 格解锁齐全、可直接播种）；③ 复核星图 / 迁猫 / 离线报表 / 军备 / 小游戏在外星球的行为均无类似断链 |
| v2.39 | 2026-09-13 | ① 发射井补**母星专属**校验（`400 LAUNCH_SILO_HOME_ONLY`，避免"每颗星各升空一次"）；② 新增 **§7 星球作用域矩阵**：把全部机制按"母星专属 / 每星球独立 / 存档级"三类登记（含数据落点与代码依据），并给出新增机制时的三问判据——两次不一致（外星球死锁、手点废墟越界）都是"缺少作用域声明"导致的，此表用于从源头堵住 |
| v2.40 | 2026-09-13 | §7 矩阵**变成可执行检查**：新增 `tests/test_scope_matrix.py`（6 个用例）——母星专属入口必须返回 `COLD_START_HOME_ONLY` / `LAUNCH_SILO_HOME_ONLY`；每星球独立的五张表在三颗星球下各自成行且资源互不影响；**存档级状态（`save_slot`/`career_stats`/`darknet_state`/`boss_state`）在开三颗分基地后仍只有 1 行**，并静态断言这三张表的主键只含 `slot_id`（防止以后被"顺手"改宽）；同轮澄清深网的 `planet_id` 只用于决定出入金的星球（状态本身存档级） |
| v2.41 | 2026-09-13 | 建行清单**声明化**：`planet_service.STAR_COLONY_TABLES` 显式列出外星球建行必须齐活的 5 张表，`ensure_star_colony` 回传 `tables` 字段，`test_scope_matrix` 新增"声明 == 实际建出的行"断言——真机曾因只建 3 张表导致读档 404，现在往建行流程加表却忘了同步清单会直接红 |
| v2.42 | 2026-09-13 | 星球性格透出（玩法侧）：`balance.planet_traits()` 作为**唯一出处**汇总承载力/产粮/专属产出三项系数，`GET /planet/state` 每颗星球带 `traits`；星图面板在每颗外星球名下列出「承载力 ×0.8 ｜ 产粮 ×0.7 ｜ 废铁 ×0.9 ｜ 芯片 ×1.1」，外星球开荒清单也带上这行；前端只读不算法，避免再出现"前端一套系数、后端一套系数" |
| v2.43 | 2026-09-13 | 外星球专属设施落地（《数值平衡表》§15.4）：`geothermal_forge` / `cryo_salvage_bench` / `asteroid_sorter` 进 `facilities.json` 与 `FACILITY_SPECS`（15 个设施），`balance.STAR_EXCLUSIVE_FACILITIES` + `facility_planet_scope()` 声明星球限定，`build_facility` 拒绝跨星建造（`FACILITY_PLANET_MISMATCH`）；产出加成（相加不相乘）与熔炼速度加成经 `colony_service._exclusive_output_bonus/_exclusive_smelt_bonus` 并入引擎；同轮修正"专属设施科技门槛在外星球永远查不到"的口径问题（改为星球限定即门槛） |
| v2.44 | 2026-09-13 | 设施面板**星球感知**：`facilities.json` 的专属设施带 `planet_scope`（由一致性测试保证与 `balance.facility_planet_scope()` 不漂移），`FacilityDefinition` 类型补该字段；设施面板对"不属于本星"的专属设施显示「仅限二号星·极热熔岩铸造星」徽标、按钮置灰并改文案为「属地不符」——不再需要"点一下才知道不能建" |
| v2.45 | 2026-09-13 | 冷启动手感打磨（纯体验、不碰数值）：① 顶栏断粮提示**分阶段**——还没有农夫猫时是引导口吻（"猫猫在等开饭：点废墟攒废铁 → 造水培农田 → 派农夫上工"，warn 色），真有农夫却断粮才回到红色事故警报；② 「第一只猫入驻」的温情叙事末尾接上下一步动作提示（"造一座水培农田，再把它派去当农夫"），把情感高点和操作指引串起来 |
| v2.46 | 2026-09-13 | 冷启动**即时目标**（纯 UI）：中央终端的冷启动块新增「目标：水培农田（废铁 3 / 10）」+ 细进度条，按当前阶段自动切目标（纸箱窝 5 → 农田 10 → 操作台 8），手点废墟时能实时看到"离目标还有多远"；阶段为"派猫上工"时不显示（那步不耗废铁） |
| v2.47 | 2026-09-13 | 科技面板**耗时预估可读化**（承接 §6.4.3 的节奏分析）：研发中的节点从"约 129600s"改为 `formatDuration` 的人类可读形式（"还需约 2.4 小时"）；**可研发节点**也新增一行「当前极客产出下约需 X」——玩家在 18 小时的 T4 节点上不再盲选，能一眼看出"多派两只极客猫能省一半时间" |
| v2.48 | 2026-09-13 | 科技面板顶部把"**派猫 ⇄ 省时间**"连起来：在研节点旁显示「再派 1 只极客猫可省约 X」（按 `remaining / rate − remaining / (rate+1)` 现算，省时不足 1 分钟则不显示，避免噪音）；玩家由此知道等待期该做什么（多派猫）而不是被动挂机 |
| v2.49 | 2026-09-13 | **端到端通关演练**（新增 `scripts/e2e_cold_start.py`，含 `--reset` 清测试槽位）在真机上从零点走开局闭环，抓到两个问题并修掉：① `/colony/dispatch` 实际只认 `role`，而 §4.3 定稿字段是 `job_id` ⇒ schema 改为**两者都接受、`job_id` 优先**（兼容旧前端）；② **冷启动引导漏了「第二座纸箱窝」这一步**——只有 1 座窝时承载力 K=1，逻辑斯蒂繁育增长为 0，第 2 只猫永远不来，玩家会卡死在"派拾荒猫"，而 §3.4 的路径本就含「第二座窝 6 废铁」；现新增 `SECOND_BOX` 阶段与引导文案，演练已能从零点跑到"农产粮 + 拾荒产铁"全自动 |
| v2.50 | 2026-09-13 | 中期演练暴露"**科研起步窄门**"并补引导：图灵终端建成但净电力为负时，科技面板顶部直接提示「图灵终端断电了（净电力 −6 kW）：造两座猫力发电滚轮并派猫踩轮，科研才会转起来」——此前科研只是"数字不动"，玩家无从判断原因；规范侧同步把这条写进 AGENTS.md 铁律 11（多条件依赖必须给引导）与项目 skill 的"多条件依赖走查"专项（含实测依赖链：芯片 → 供电 → 科技门 → 猫口） |
| v2.51 | 2026-09-13 | 中期端到端演练**跑通**（`scripts/e2e_cold_start.py --stage mid`：冷启动 → 供电 → 极客 → T1+T2 科技 → 感应电炉 → 熔炼出 20 合金），途中把三处"静默失败"写进 skill：① **粮食/人口必须同步扩**（低士气下 1 农夫只养 2.8 猫，断粮 ⇒ 科研归零）；② **工位来自设施**（1 农田 = 2 农夫工位）；③ **长挂机推满警戒度 ⇒ 静默关灯 ⇒ 全员停工**（玩家需靠隔音层/诱饵/截杀，脚本用测试专用 `calm()` 跳过）；演练脚本补 `calm()`、镇静循环与缺料等待 |
| v2.52 | 2026-09-13 | 静默关灯**引导补全**（铁律 11 的又一次应用）：HUD 警戒条在「静默关灯中」徽标下新增一行可操作提示——「全员停工，科研与拾荒一并归零。降噪靠【多孔纸浆隔音降噪层】或种【消音绒草】；也可以放【诱饵】引开巡逻机、或用战车截杀处理警报。警戒度 ×5 衰减，跌回 20 自动复工」；容器改 `flex-wrap` 以容纳整行提示 |
| v2.53 | 2026-09-13 | **接口账本更正（重要）**：新增只读工具 `scripts/api_doc_consistency.py` 逐条比对文档清单与真实路由，发现**此前所有"接口总数 63 / 65 / 68 / 69"的说法都是抄文档修订记录得到的，与代码不符**——真实数字是 **代码 43 个接口 / 文档清单 44 项**。差异明细：① 文档写了但**代码没实现**：`POST /doctrine/unlock`（政令系统只有 `save_slot.doctrines` 列与 §15.2 口径，没有种子文件、服务与效果接线，已在该行标"尚未实现"）；② 代码实现了但**文档漏登记** 9 个（`/tech/tree`、`/garden/state`、`/darknet/state`、`/darknet/data-eye`、`/radio/feed`、`/radio/generate`、`/military/assemble-missile`、`/military/intercept-raid`、`/military/final-assault`，已补进 §4.2 表）。README 的接口数同步为 43（+1 待落地）。**教训**：接口数这类"汇总数字"必须由脚本从代码算，不能引用文档里手写的数字 |
| v2.54 | 2026-09-13 | **政令系统落地**（补齐 v2.53 指出的唯一未实现接口）：新增 `static/doctrines.json`（8 条政令，消耗与 §15.2 一一对应）、`services/doctrine_service.py`（清单 / 点亮 / 幂等与凝聚力校验 / **效果白名单** `WIRED_EFFECTS`）、`endpoints/doctrine.py`（`GET /doctrine/list`、`POST /doctrine/unlock`）；接口数 43 → **45**（清单 46 项）。效果接线分批：本轮接线 `production_multiplier` / `cat_capacity` / `fleet_armor` / `suspicion_growth` 四项，其余四项（防空战力、黑市手续费、情报速度、士气）留在 `pending` 里如实标注 |
| v2.55 | 2026-09-13 | **修掉一个潜伏的真 bug：跨存档状态泄漏**（同时终结了困扰多轮的"偶发失败"）。根因：`game_init_service` 与 `planet_service` 用 **`dict(DEFAULT_*)` 浅拷贝**初始化 `security_policy` / `bombardment_state`，而这两个默认值里有**嵌套列表**（如 `damaged_facilities: []`）⇒ 所有新存档（含切槽位、开分基地）**共享同一个列表对象**，谁 append 一次，后续每个新档都会"继承"上一档的受损设施/策略。表现就是测试里的偶发失败（同一 DB 顺序不同、结果不同）。现改为 **`copy.deepcopy`**；同类写法已全仓清点（仅这两处）。教训写入 skill：**默认值带嵌套可变对象时必须深拷贝** |
| v2.56 | 2026-09-13 | 政令补前端：新增 `stores/doctrine.ts` + `DoctrinePanel.vue`（左栏第 9 个 Tab「星系法典」）——列出 8 条政令的消耗/效果/是否已点亮，凝聚力够时才可点；**未接线效果的政令仍可点亮**（后端扣点并记录），面板用「效果待接线（可点亮）」徽标如实标注，`wired_effects` 由接口透出、前端不自己判断；真机实测 `/doctrine/list` 返回 unity=0 / 8 条清单 / 4 项已接线白名单 |
| v2.57 | 2026-09-13 | **补上政令的收入来源**（发现"有商店没收入"）：`UNITY_PER_PURR_MASTER_PER_SEC` 原本只定义未消费，且星际职业工位被硬编码 0 ⇒ 凝聚力永远 0。① `workstation_limits` 新增 `star_jobs_unlocked`，**升空后**按 §15.1 上限开放星际职业（升空前 0），四处调用点（状态报文 / 快照对账 / 调度 / 建造响应）全部传入 `star_jobs_unlocked()`；② 离线结算按"本星球呼噜大师 × 0.02/s × Δt"累加 `save_slot.unity`。用例覆盖"升空前派不了 / 升空后可派 / 2 只 1000 秒 ≈ 40 点 / 政令商店同步看到" |
| v1.6 | 2026-09-12 | 模块 M（公频电台）落地：API 清单 34 → **36**，新增 `GET /radio/feed`（本地填槽 + LRU，0 Token）与 `POST /radio/generate`（LLM 场景 3 语料批处理）；落地 `core/template_engine.py`、`services/llm_service.py`、`services/radio_service.py`、`schemas/template_agent.py`、`static/templates_default.json`；LLM 默认走硅基流动 + 便宜小模型（`Qwen/Qwen2.5-7B-Instruct`），预算熔断账本落 `backend/var/llm_usage.json`；防火墙 1 改为**逐条**校验（一条不合格不废整批） |
