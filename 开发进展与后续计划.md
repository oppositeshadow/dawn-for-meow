# 《喵星破晓》开发进展与后续计划

> **这是什么**：一页式进度看板 —— 现状数字、最近迭代、四道收尾关卡、后续计划。
> **它不是口径来源**：数值 / 数据模型 / 接口 / 玩法口径仍以 README 里的权威顺序为准；本文件只回答"现在到哪了、接下来做什么"。
> **怎么维护**：每轮收尾时更新第二、三节；**汇总数字一律用只读脚本从代码算**（铁律 13），不抄别处的文字。

**更新时间**：2026-09-14　｜　**当前阶段**：Milestone 1 四个阶段全部完成、15 个模块 A~O 全部落地、**可完整通关**；重心已从"加玩法"转到"**把承诺做实**"。

---

## 一、一句话现状

玩家现在能从**地下避难所的第一座纸箱窝**一路玩到**丝带尾声**：冷启动手点废墟 → 猫草农场与工位循环 → 科技树 → 设施与电力 → 军备战斗 → 猫草基因实验室 → 智械深网 → 三颗外星球（建行 / 迁猫 / 专属设施 / 周期事件 / 特化科技）→ 火箭升空 → 欧米伽三段决战 → LLM 写的通关碑文与演职员表。

近几轮的重心是**消除"说的和做的不一致"**：离线报表口径、航线劫掠接线、一个真 bug（幽灵航线）、潮汐信息分档揭示、以及文档/界面里的过期承诺。

---

## 二、从代码算出来的数字（2026-09-14）

| 项目 | 数字 | 怎么算出来的（可复跑） |
| :--- | ---: | :--- |
| 接口 | **45 个**（文档清单 45 = 代码 45，完全一致） | `python scripts/api_doc_consistency.py` |
| 数据表 | **16 张** | `tests/test_models_and_init.py`（主键与表数断言） |
| 测试用例 | **415 条**（全量跑：1 skipped、0 失败） | `python -m pytest -p no:cacheprovider --collect-only` |
| 母星科技节点 | **19 个**（T1 4 / T2 4 / T3 5 / T4 6） | `static/techs_planet0.json` |
| 外星球特化节点 | **12 个 / 星**（LLM 一次性批量铺满） | `planet_tech_service` |
| 设施 | **15 座**（含 3 座星球专属） | `static/facilities.json` |
| 猫草 | **14 种**（图鉴目标 25~30） | `static/cat_plants.json` |
| 行星 | **4 颗**（母星 + 3 颗外星球，各有专属周期事件） | `balance.PLANETS` / `balance.PLANET_CYCLES` |
| 政令 | **8 条**（效果 100% 接线） | `balance.DOCTRINES` |
| 成就徽章 | **15 枚** | `balance.ACHIEVEMENTS` |
| 小游戏 | **3 个**（密电译码 / 矿脉扫描 / 熔炉配比） | `static/minigames.json` |
| LLM 场景 | **6 个**（全部带本地兜底，断网不阻塞） | 工程稿 §6.2 |

---

## 三、最近几轮的迭代（倒序）

| 提交 | 做了什么 |
| :--- | :--- |
| `dbc5efe` | **承诺一致性清扫**：修 4 处过期声明（含"读档报 `STAR_COLONY_NOT_IMPLEMENTED`"——**该错误码在代码里已完全不存在**）；猫草光环拆成"生效 / 待接线"并在面板如实标注；§18 死常量清单复核 48 → 46 |
| `3dd989d` | **潮汐监测分档揭示**：L0 星图完全看不到潮汐（后端不返回）→ L1 短波电台点亮后看到相位 → L2 导航中枢点亮后加倒计时与进度条；不新增科技节点 |
| `7f515bf` | **碎星流「被劫掠 ×2」接线**（按结算时点相位、两端取较大者）+ 修掉**幽灵航线 bug**（被劫掠的船每 10 分钟重复判定、永远到不了，还白占带宽） |
| `12b3b65` | **周期事件接进离线报表**：长离线用区间时间加权平均（整轮相抵），报表首条写明"经历 N 轮【岩浆潮汐】" |
| `43afde6` / `78a7aad` | 外星球**周期事件**落地（岩浆潮汐 / 极地日照 / 碎星流）+ 星图显示当期事件与倒计时 |

---

## 四、每轮收尾的四道关（当前全绿）

| 关卡 | 命令 | 最近结果 |
| :--- | :--- | :--- |
| 全量测试 | `python -m pytest -p no:cacheprovider -q`（在 `backend/`） | 415 条，1 skipped、0 失败 |
| 前端类型 + 构建 | `cd frontend; npm run build` | `vue-tsc` 与 `vite build` 通过 |
| 一键总检 | `python scripts/audit_all.py --with-server`（需 8010 在跑） | **五项全绿**：接口账本 / 文档声明 / 科技节奏 / 减伤曲线 / 契约扫查（42 接口 0 个 5xx） |
| 真机抽查 | 重启 8010 → 用**测试槽位 2/3**（`scripts/e2e_cold_start.py 3 --reset`）走关键链路 | 每轮手动跑一次；举例：本轮验证"生效光环 vs 待接线光环"在真机 MySQL 上正确分家 |

---

## 五、后续计划

### P0 · 把"看得到但还没接线"的东西接掉（接下来 1~2 轮）

| # | 事项 | 依赖 | 完成标志 |
| :--- | :--- | :--- | :--- |
| 1 | 猫草光环【肥宅高能薄荷】**繁育 r 上限 +20%** 接线 | 接入点现成（离线引擎繁育乘数） | 种 1 株 → 同样时长猫口增长变快；键从 `halo_pending` 移进 `halo` |
| 2 | 猫草光环【金刚地衣】**载具装甲 +15%** 接线 | 接入点现成（`combat_service._fleet_armor_bonus`） | 开战瞬间装甲真的 +15%；键同样移进 `halo` |

### P1 · 需要先定玩法口径

| # | 事项 | 卡在哪 |
| :--- | :--- | :--- |
| 3 | 猫草光环【爆裂刺果】**诱饵造价 −20%** | 诱饵造价系统本身还没落地（`DECOY_SCRAP_COST` / `DECOY_CHIPS_COST` 目前零引用） |
| 4 | 猫草光环【铁线荆棘藤】**敌方巡逻到达时间 +30%** | 当前没有"敌方巡逻到达时间"这套机制 |
| 5 | **规划 / 预约发货**（循环航线：A→B 持续往返 N 趟、危险相位自动暂停） | 全新机制；它落地后，"未来危险窗口条"才有落点（你先前提过，我也把窗口条留给了这一期） |
| 6 | C 类待接线：乘员休养时长公式、范式突破加速 | 需要先定"休养 / 范式突破"的结算口径 |

### P2 · 文档里承诺、但还没做的大块

| # | 事项 | 出处 |
| :--- | :--- | :--- |
| 7 | 轨道轰炸（洗劫比例 / 重建成本比 / 猫口下限 / 和平期） | §12 灾难与保底（B 类常量） |
| 8 | 暴露度四段危机的部分分支（禁言时长 / 审讯资产罚金 / 追踪器拆除） | §11.4 |
| 9 | 匿名论坛裁判的本地关键词规则（8 个 `FORUM_*`） | 模块 I |
| 10 | 救急舱物资、润滑脂/电池换算配方、重整旗鼓 | §12 / §3 |
| 11 | 循环巡逻航线（远征自动收货）、量化条件单（大盘自动买卖与止盈止损） | GDD §12《阶梯式自动化体系》 |

> 完整的"定义了但没人用"清单在《数值平衡表》**§18**：v1.60 复核 **46 个**（A 类死副本 13 / B 类未实现 29 / C 类待接线 4）。

### 需要你拍板（我这边不动手，等决定）

| # | 事项 | 选项 |
| :--- | :--- | :--- |
| A | 星球周期的 `production_multiplier` 要不要**按资源拆开** | ① 保持现状（周期效果作用于**全局产出**：产粮 / 拾荒 / 科研，文档已按此改口径）；② 拆成"只加产粮不加拾荒"等（属**重平衡**，要先改《数值平衡表》再动代码） |
| B | "未来危险窗口条"什么时候做 | ① 与 P1 第 5 项的"规划发货"同批做（推荐，那时才有落点）；② 先单独做（价值有限：现在只能决定"现在发还是等一会儿"） |

### 已知问题（没有证据前不动断言）

| # | 问题 | 现状 |
| :--- | :--- | :--- |
| 1 | `tests/test_stats.py::test_career_panel_baseline` **偶发失败**（约 1/4 概率） | 已带自诊断（失败时打印被点亮的徽章 ID）、连跑多次未复现、已排除"浅拷贝默认值"那一类；按 skill §八的纪律**先取证再改** |

---

## 六、不变的约定（新手看这里）

* **文档权威顺序**（冲突时以此裁决）：数值 → 《数值平衡表》｜数据模型 → 《数据库设计定稿》｜接口与目录 → 《代码结构与核心工程详细设计规范》｜玩法 → GDD｜模块与优先级 → WBS；
* **13 条开发铁律**在 `AGENTS.md`（数值三处同步、新增机制先登记作用域、文档承诺必须与代码一致、多条件依赖必须给引导、真实演练优先于加新功能…）；
* **8 节流程与自查清单**在 `.agents/skills/dawn-for-meow-dev/SKILL.md`（改数值 / 加机制 / 半截链路审计 / 白名单≠接线 / 错过成本表 / 死常量审计 / 偶发失败纪律 / 一键总检）；
* 常见编码陷阱：JSON 列**整条替换**、默认值含嵌套对象必须 `copy.deepcopy`、加成**相加不相乘**、确定性判定用 `sha256`、新字段**三处同步**（引擎/裁剪 → Schema → 前端）。

---

## 修订记录

| 版本 | 日期 | 内容 |
| :--- | :--- | :--- |
| v1.0 | 2026-09-14 | 首次成文：现状数字（全部从代码算）、最近 5 轮迭代、四道收尾关卡、后续计划 P0~P2、两条待拍板、一条已知偶发失败项 |
