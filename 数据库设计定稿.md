# 《喵星破晓》（Dawn for Meow）—— 数据库设计定稿

> **文档定位**：本文档是《喵星破晓》**数据模型（表、字段、类型、时间基准）的唯一真值来源**。
> 原 [归档/技术架构与数据库设计参考（早期稿·已归档）.md](归档/技术架构与数据库设计参考（早期稿·已归档）.md) 中的表设计已**全部作废并由本文档取代**。
> 配套文档：[数值平衡表.md](数值平衡表.md)（所有数值口径）、[代码结构与核心工程详细设计规范.md](代码结构与核心工程详细设计规范.md)（工程结构与接口契约）

**编制日期**：2026-09-12　|　**版本**：v1.1（抽象化改造版，随玩法推进迭代）

---

## 目录
1. [设计原则](#1-设计原则)
2. [全局约定](#2-全局约定)
3. [表清单总览](#3-表清单总览)
4. [完整 DDL](#4-完整-ddl)
5. [JSON 种子文件（不入库）](#5-json-种子文件不入库)
6. [与旧稿的差异说明](#6-与旧稿的差异说明)
7. [抽象化之后的扩展方式](#7-抽象化之后的扩展方式以后加东西怎么加)
8. [存档与对账协议](#8-存档与对账协议)
9. [修订记录](#9-修订记录)

---

## 1. 设计原则

1. **定义数据化、状态通用化（核心原则）**：一切"可以无限追加的东西"——工种、设施、植物、载具车型、科技、拾荒目标、政令、小游戏——**定义写进 `backend/static/*.json`，玩家状态写进通用表**（`labor_buckets` / `facility_state` / `minigame_state` / `vehicle_units` 等）。加内容只改 JSON 与《数值平衡表》，**永远不用改表结构**。
2. **每张表都带 `slot_id`**：三个存档槽天然并存于同一套表中，导出就是把该 `slot_id` 的行打包成 Gzip + Base64。
3. **池化优先**：猫、炮台、导弹、诱饵等"数量型"对象一律存整数；只有**可命名、可改装、会战损要维修**的贵重单位（载具）才逐辆建行。
4. **离线可推进**：任何随时间演化的系统都必须能靠时间戳补算，绝不依赖"运行时定时器"。
5. **建表默认值全为 0**：新存档的初始状态由"新游戏初始化"流程显式写入，不靠 DDL 默认值"送"玩家资源。
6. **结构易变的状态用 JSON 承载**：单机自用、不需要按字段查询的复杂状态（格盘、持仓、大单异动、灾害重建态）存 JSON，避免为每一次玩法迭代做表迁移。

---

## 2. 全局约定

### 2.1 主键约定

- 单行星系统：`PRIMARY KEY (slot_id, planet_id)`；
- 单存档系统（深网、BOSS、统计）：`PRIMARY KEY (slot_id)`；
- 行型系统（载具、帖子、语料）：自增 `id` + `slot_id` 索引。

### 2.2 星球 ID 约定

| planet_id | 天体 |
| :--- | :--- |
| 0 | 翠绿母星 · 07 号避难所（**不删档，永久保留**） |
| 1 | 二号星 · 极热熔岩铸造星 |
| 2 | 三号星 · 极寒气态冰卫星 |
| 3 | 四号星 · 巨型小行星带矿区 |

### 2.3 时间基准（重要）

| 类型 | 字段形式 | 适用 |
| :--- | :--- | :--- |
| 持续演化型 | `last_tick_time BIGINT`（秒） | 资源、繁育、警戒度、植物生长、股价、BOSS 扩张 |
| 倒计时型 | `xxx_ends_at BIGINT`（绝对到期时间戳，秒） | 远征、维修、做空合约、车队、空投、冷却 |

**禁止**再出现 `remaining_seconds` 这类相对值字段（关掉游戏就不会走动）。

### 2.4 开局初始化

新游戏创建时执行一次初始化事务：写入 `save_slot`（槽位、`save_version`、`unity = 0`、`doctrines = {}`）、`career_stats`（全 0）、`planet_state`（母星解锁 + 激活）、`colony_state`（资源与人口全 0，等玩家手点废墟攒 5 废铁造第一个纸箱）、`labor_buckets`（按 `jobs.json` 为母星建 5 个工种行，计数 0）、`facility_state`（按 `facilities.json` 建全部设施行，等级 0）、`boss_state`、`darknet_state`、`minigame_state`（按 `minigames.json` 建行），并把 `techs_planet0.json` 里的母星 19 个科技节点以 `LOCKED` 状态写入 `tech_records`。

**编码落地补充（2026-09-12 阶段 1 实测）**：

* `planet_state` 一次写入**全部 4 颗天体行**——母星 `unlocked = true` 且 `is_active = true`、`biome_tag = "翠绿生态摇篮·07 号地下避难所"`；其余三颗 `unlocked = false`、`biome_tag` 留空（登录时由 LLM 生成），避免切星图时缺行；
* `garden_state` 写入固定 **7×7 = 49 格**网格，其中中央 **3×3 = 9 格** `unlocked = true`；
* `minigame_state` 按 `minigames.json` 为每个玩法的**解锁星球**建行（`cipher_decode` → 2 号、`vein_scan` → 3 号、`forge_recipe` → 1 号），初始 `state` 取 JSON 的 `initial_state`；
* `military_state` 落库默认值：机库 12 机位、池化编制全 0、三级安防预案三项默认开启；`boss_state`：威胁 1 / 扩张 1.0 / 舰队 500 / `rage = 0`；`darknet_state`：四只标的按《数值平衡表》§11.1 落初始快照，其余数组为空。

---

## 3. 表清单总览

| # | 表名 | 状态 | 职责 |
| :--- | :--- | :--- | :--- |
| 1 | `save_slot` | ★ 新增 | 存档槽位：版本号、校验和、活跃星球、游玩时长、**文明凝聚力与星系法典政令** |
| 2 | `colony_state` | ✎ 改造 | 基地核心：资源、电力三口径、人口总数、警戒度、自动化策略（**设施列与工种列已外移**） |
| 3 | `labor_buckets` | ★ 新增 | **通用工种分桶**：母星 5 工种 / 星际 4 大高维职业 / 未来新职业共用一张表 |
| 4 | `facility_state` | ★ 新增 | **通用设施状态**：取代 `colony_state` 中 11 个硬编码设施列 |
| 5 | `planet_state` | ★ 新增 | 星图：星球解锁、生态标签、跨星在途航线 |
| 6 | `tech_records` | ✎ 改造 | 科技：DAG 前置关系、阶梯、进度、LLM 生成标记 |
| 7 | `garden_state` | ✎ 改造 | 水培：7×7 场地与逐格扩建、介质、机械臂托管、母本图鉴 |
| 8 | `darknet_state` | ✎ 改造 | 深网：股价、持仓、做空合约、在途空投、发帖冷却、**盘口大单异动** |
| 9 | `forum_posts` | ★ 新增 | 帖子与机器人跟帖（遥测线索、账号画像、事件滞后模式） |
| 10 | `military_state` | ✎ 改造 | 军备池化层：机位上限、炮台、导弹、诱饵库存、安防预案 |
| 11 | `vehicle_units` | ★ 新增 | **每辆车一行**：昵称、模块、三层血条、装甲上限、乘员 |
| 12 | `boss_state` | ✎ 改造 | 欧米伽：威胁演化、通缉热度、大轰炸状态、情报破译 |
| 13 | `career_stats` | ★ 新增 | 生涯统计数字（增量累加） |
| 14 | `achievements` | ★ 新增 | 成就徽章进度与解锁时间 |
| 15 | `event_templates` | ✎ 改造 | 语料模板库：补 LRU 冷却字段 |
| 16 | `minigame_state` | ★ 新增 | **通用小游戏状态**：密电译码 / 矿脉扫描 / 熔炉配比共用 |

> 共 **16 张表**（初版 7 张 → 首次定稿 13 张 → 本次抽象化后 16 张）。

---

## 4. 完整 DDL

```sql
-- ========================================================
-- 1. 存档槽位表 (save_slot)：多存档的根节点
-- ========================================================
CREATE TABLE save_slot (
    slot_id           TINYINT      PRIMARY KEY COMMENT '存档槽位 1~3',
    slot_name         VARCHAR(32)  DEFAULT NULL COMMENT '玩家自定义槽位名',
    save_version      INT          NOT NULL DEFAULT 1 COMMENT '存档格式版本，导入旧档时用于迁移',
    checksum          CHAR(64)     DEFAULT NULL COMMENT '导入导出时的完整性校验和',
    active_planet_id  INT          NOT NULL DEFAULT 0 COMMENT '当前操作的星球 ID',
    playtime_seconds  BIGINT       NOT NULL DEFAULT 0 COMMENT '累计游玩时长(秒)',
    unity             DOUBLE       NOT NULL DEFAULT 0 COMMENT '文明凝聚力(全局点数，由呼噜大师产出)',
    doctrines         JSON         NOT NULL COMMENT '已点亮星系法典政令 {"doctrine_id": level}',
    created_at        DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at        DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='存档槽位表';

-- ========================================================
-- 2. 基地核心状态表 (colony_state)：单行星一行，纯数字池化
-- ========================================================
CREATE TABLE colony_state (
    slot_id        TINYINT NOT NULL,
    planet_id      INT     NOT NULL,
    last_tick_time BIGINT  NOT NULL DEFAULT 0 COMMENT '上次结算时间戳(秒)，离线补算锚点',

    -- 存量资源 (当前值 / 上限)
    catnip        DOUBLE NOT NULL DEFAULT 0 COMMENT '野生高能猫薄荷',
    catnip_max    DOUBLE NOT NULL DEFAULT 200,
    scrap         DOUBLE NOT NULL DEFAULT 0 COMMENT '机械废铁',
    scrap_max     DOUBLE NOT NULL DEFAULT 200,
    chips         DOUBLE NOT NULL DEFAULT 0 COMMENT '工业逻辑芯片',
    chips_max     DOUBLE NOT NULL DEFAULT 100,
    alloys        DOUBLE NOT NULL DEFAULT 0 COMMENT '航空钛合金',
    alloys_max    DOUBLE NOT NULL DEFAULT 50,
    battery       DOUBLE NOT NULL DEFAULT 0 COMMENT '高能蓄能电池(道具型，消耗品)',
    battery_max   DOUBLE NOT NULL DEFAULT 50,
    lube          DOUBLE NOT NULL DEFAULT 0 COMMENT '生物润滑脂(出口硬通货)',
    lube_max      DOUBLE NOT NULL DEFAULT 50,
    manual_scavenge_clicks INT NOT NULL DEFAULT 0 COMMENT '冷启动手点废墟次数(上限见数值平衡表 §3.4)',

    -- 电力 (三种口径，见数值平衡表 §7)
    power_net        DOUBLE NOT NULL DEFAULT 0 COMMENT '净电力平衡(kW)，流量指标',
    battery_kwh      DOUBLE NOT NULL DEFAULT 0 COMMENT '蓄电池电容池存量(kWh)',
    battery_kwh_max  DOUBLE NOT NULL DEFAULT 200 COMMENT '电容池上限(kWh)',

    -- 人口总览（工种明细见 labor_buckets，设施等级见 facility_state）
    total_cats        INT    NOT NULL DEFAULT 0 COMMENT '基地总猫口',
    birth_progress    DOUBLE NOT NULL DEFAULT 0 COMMENT '繁育累计进度 0~1',
    job_idle          INT    NOT NULL DEFAULT 0 COMMENT '空闲待命猫口',

    -- 对抗属性
    suspicion DOUBLE NOT NULL DEFAULT 0 COMMENT '天网警戒度 0~100',

    -- 自动化策略
    labor_automation_policy  JSON DEFAULT NULL COMMENT '迟滞换班: {"enabled":true,"upper":0.8,"lower":0.2,"shift":2}',
    smelt_automation_policy  JSON DEFAULT NULL COMMENT '材料溢出自动熔炼策略',

    PRIMARY KEY (slot_id, planet_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='基地核心状态表';

-- ========================================================
-- 3. 通用工种分桶表 (labor_buckets)
-- 母星 5 工种 / 星际 4 大高维职业 / 未来新增职业均共用此表
-- 工种定义（产出、工位上限来源）见 static/jobs.json + core/balance.py
-- ========================================================
CREATE TABLE labor_buckets (
    slot_id   TINYINT     NOT NULL,
    planet_id INT         NOT NULL,
    job_id    VARCHAR(32) NOT NULL COMMENT 'farmer/scavenger/geek/power_runner/crew/fleet_commander/logistics/terraformer/purr_master',
    cat_count INT         NOT NULL DEFAULT 0 COMMENT '该工种猫口数',
    PRIMARY KEY (slot_id, planet_id, job_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='通用工种分桶表';

-- ========================================================
-- 4. 通用设施状态表 (facility_state)
-- 取代 colony_state 中原先 11 个硬编码设施列
-- 设施定义（造价曲线、工位产出、解锁条件）见 static/facilities.json
-- ========================================================
CREATE TABLE facility_state (
    slot_id     TINYINT     NOT NULL,
    planet_id   INT         NOT NULL,
    facility_id VARCHAR(32) NOT NULL COMMENT 'housing_box/farm_plot/scavenge_station/turing_terminal/acoustic_layer/power_wheel/solar_panel/refinery/battery_bank/cat_condo/launch_silo',
    level       INT         NOT NULL DEFAULT 0 COMMENT '等级或数量，0 = 未建造',
    PRIMARY KEY (slot_id, planet_id, facility_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='通用设施状态表';

-- ========================================================
-- 5. 星球与星图表 (planet_state)：解锁进度与跨星物流
-- ========================================================
CREATE TABLE planet_state (
    slot_id       TINYINT     NOT NULL,
    planet_id     INT         NOT NULL COMMENT '0母星 1熔岩星 2冰卫星 3小行星带',
    unlocked      BOOLEAN     NOT NULL DEFAULT FALSE,
    unlocked_at   BIGINT      DEFAULT NULL COMMENT '解锁时间戳',
    biome_tag     VARCHAR(64) DEFAULT NULL COMMENT '生态标签(LLM生成的星球风味)',
    is_active     BOOLEAN     NOT NULL DEFAULT FALSE COMMENT '是否当前操作星球',
    -- 在途跨星航线: [{"from":0,"to":1,"resource":"catnip","amount":200,"ends_at":1773000000}]
    logistics_routes JSON     NOT NULL COMMENT '在途空投/货运航线数组',
    PRIMARY KEY (slot_id, planet_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='星球解锁与星际物流表';

-- ========================================================
-- 6. 科技研发记录表 (tech_records)：DAG 节点状态
-- ========================================================
CREATE TABLE tech_records (
    slot_id       TINYINT      NOT NULL,
    planet_id     INT          NOT NULL,
    tech_id       VARCHAR(64)  NOT NULL,
    tech_name     VARCHAR(64)  NOT NULL,
    parent_ids    JSON         NOT NULL COMMENT 'DAG 前置科技 ID 数组(防火墙2 校验用)',
    tier          INT          NOT NULL DEFAULT 1 COMMENT '所属阶梯 1~4',
    node_order    INT          NOT NULL DEFAULT 0 COMMENT '同阶梯内排序',
    status        ENUM('LOCKED','RESEARCHING','UNLOCKED') NOT NULL DEFAULT 'LOCKED',
    current_progress DOUBLE    NOT NULL DEFAULT 0 COMMENT '已累积算力',
    target_cost   DOUBLE       NOT NULL DEFAULT 0 COMMENT '解锁所需算力',
    is_agent_generated BOOLEAN NOT NULL DEFAULT FALSE COMMENT '是否 LLM 动态推演生成',
    flavor_text   VARCHAR(255) DEFAULT NULL COMMENT '风味文案',
    mechanic_type VARCHAR(32)  DEFAULT NULL COMMENT 'PASSIVE_BUFF / CONVERSION / TRADE_OFF / UNLOCK_ABILITY',
    buff_payload  JSON         DEFAULT NULL COMMENT '机制或数值加成载荷',
    PRIMARY KEY (slot_id, planet_id, tech_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='科技树状态记录表';

-- ========================================================
-- 7. 水培基因实验室表 (garden_state)
-- ========================================================
CREATE TABLE garden_state (
    slot_id        TINYINT     NOT NULL,
    planet_id      INT         NOT NULL,
    last_tick_time BIGINT      NOT NULL DEFAULT 0 COMMENT '生长/枯萎离线补算锚点',
    grid_size      INT         NOT NULL DEFAULT 3 COMMENT '已解锁边长 3~7',
    unlocked_cells INT         NOT NULL DEFAULT 9 COMMENT '已解锁格子数 9~49(每次扩建 +1 格)',
    current_medium VARCHAR(16) NOT NULL DEFAULT 'STERILE' COMMENT 'STERILE / RADIATION / ZERO_G',
    mechanical_arm_enabled BOOLEAN NOT NULL DEFAULT FALSE COMMENT '智能机械臂托管',
    auto_protect_unknown   BOOLEAN NOT NULL DEFAULT TRUE  COMMENT '自动保护未知突变幼苗',
    -- 固定 7x7 场地: [{"x":0,"y":0,"unlocked":true,"seed_id":"moss","stage":"MATURE","age":120}]
    grid_data      JSON        NOT NULL COMMENT '水培网格二维数组',
    unlocked_seed_ids JSON     NOT NULL COMMENT '已解锁母本基因图鉴',
    PRIMARY KEY (slot_id, planet_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='水培基因实验室状态表';

-- ========================================================
-- 8. 智械匿名深网表 (darknet_state)：单存档一行
-- ========================================================
CREATE TABLE darknet_state (
    slot_id        TINYINT     PRIMARY KEY,
    last_tick_time BIGINT      NOT NULL DEFAULT 0 COMMENT '大盘离线补算锚点',
    byte_credits   DOUBLE      NOT NULL DEFAULT 0 COMMENT '算力币余额',
    exposure       DOUBLE      NOT NULL DEFAULT 0 COMMENT '节点异常暴露度 0~100',
    burner_id      VARCHAR(64) NOT NULL DEFAULT 'robot_4a932cz' COMMENT '匿名代号(可自定义)',
    has_4s_data    BOOLEAN     NOT NULL DEFAULT FALSE COMMENT '是否已购买 4S 深度数据眼',
    stocks_data    JSON        NOT NULL COMMENT '大盘快照 [{stock_id,price,forecast,target_forecast,momentum,volatility}]',
    kline_history  JSON        NOT NULL COMMENT '近 N 根 K 线，供前端绘图',
    positions      JSON        NOT NULL COMMENT '现货多头持仓',
    short_contracts JSON       NOT NULL COMMENT '做空合约(含绝对到期时间戳 ends_at)',
    limit_orders   JSON        NOT NULL COMMENT '量化条件单配置',
    black_market_items JSON    NOT NULL COMMENT '黑市货架 [{item_id,base_price,current_price,tag}]',
    pending_deliveries JSON    NOT NULL COMMENT '在途空投 [{item_id,qty,ends_at}]',
    whale_events   JSON        NOT NULL COMMENT '盘口匿名大单异动 [{ts,stock_id,side,amount}]，看破迷雾线索二',
    last_post_time BIGINT      DEFAULT NULL COMMENT '上次论坛发帖时间戳(10 分钟冷却判定)'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='智械深网金融与论坛状态表';

-- ========================================================
-- 9. 论坛帖子表 (forum_posts)：玩家发帖 + 机器人跟帖
-- ========================================================
CREATE TABLE forum_posts (
    id             BIGINT      AUTO_INCREMENT PRIMARY KEY,
    slot_id        TINYINT     NOT NULL,
    author_id      VARCHAR(64) NOT NULL COMMENT '发帖代号(机器散户哈希 / 玩家 burner_id)',
    is_player      BOOLEAN     NOT NULL DEFAULT FALSE COMMENT '是否玩家自己发的',
    telemetry_code VARCHAR(64) DEFAULT NULL COMMENT '遥测报错码附件(线索一，可空)',
    author_post_count INT      NOT NULL DEFAULT 0 COMMENT '该代号历史发帖数(线索三·账号画像)',
    author_hit_rate DOUBLE     NOT NULL DEFAULT 0 COMMENT '该代号历史爆料命中率 0~1(线索三)',
    content        TEXT        NOT NULL COMMENT '正文',
    created_at     BIGINT      NOT NULL COMMENT '发帖时间戳',
    target_stock   VARCHAR(32) DEFAULT NULL COMMENT 'LLM 判定关联股票',
    sentiment      ENUM('BULL','BEAR','NEUTRAL') DEFAULT NULL,
    persuasiveness TINYINT     DEFAULT NULL COMMENT '煽动星级 1~5',
    suspicion_risk TINYINT     DEFAULT NULL COMMENT '判定增加的暴露度',
    momentum_impact DOUBLE     NOT NULL DEFAULT 0 COMMENT '注入的行情动量',
    reaction_pattern ENUM('FRONT_RUN','SLOW_BURN','BEAR_TRAP','FATIGUE') NOT NULL DEFAULT 'SLOW_BURN' COMMENT '事件到行情的滞后模式',
    is_fake        BOOLEAN     NOT NULL DEFAULT FALSE COMMENT '后台真假标记(玩家不可见)',
    reply_to_id    BIGINT      DEFAULT NULL COMMENT '跟帖所回复的主帖 ID',
    INDEX idx_slot_time (slot_id, created_at),
    INDEX idx_slot_stock (slot_id, target_stock)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='深网匿名论坛帖子表';

-- ========================================================
-- 10. 军备池化表 (military_state)：不需要个性的部分
-- ========================================================
CREATE TABLE military_state (
    slot_id        TINYINT NOT NULL,
    planet_id      INT     NOT NULL,
    last_tick_time BIGINT  NOT NULL DEFAULT 0,
    hangar_capacity   INT  NOT NULL DEFAULT 12 COMMENT '机库车位上界(母星12/星际24)',
    laser_turrets     INT  NOT NULL DEFAULT 0 COMMENT '固定激光防空炮台(池化)',
    cruise_missiles   INT  NOT NULL DEFAULT 0 COMMENT '已总装巡航导弹(池化)',
    decoy_count       INT  NOT NULL DEFAULT 0 COMMENT '发条机械鼠诱饵库存(池化)',
    security_policy   JSON NOT NULL COMMENT '三级安防预案 {"p1_use_decoy":true,...}',
    hospital_queue    JSON NOT NULL COMMENT '急救舱 [{unit_id,cats:[...],ends_at}]',
    active_expeditions JSON NOT NULL COMMENT '远征 [{expedition_id,target_id,unit_ids:[...],ends_at}]',
    PRIMARY KEY (slot_id, planet_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='军备池化状态表';

-- ========================================================
-- 11. 载具实例表 (vehicle_units)：每辆车单独计算
-- ========================================================
CREATE TABLE vehicle_units (
    unit_id       BIGINT     AUTO_INCREMENT PRIMARY KEY,
    slot_id       TINYINT    NOT NULL,
    planet_id     INT        NOT NULL,
    unit_type     VARCHAR(32) NOT NULL COMMENT '车型 ID，定义见 static/vehicle_types.json',
    nickname      VARCHAR(32) DEFAULT NULL COMMENT '昵称，如 雷霆号',
    modules       JSON        NOT NULL COMMENT '装配模块 [{slot,module_id}]',
    shield        DOUBLE      NOT NULL DEFAULT 0 COMMENT '当前护盾(能量，战斗外快速回充)',
    armor         DOUBLE      NOT NULL DEFAULT 0 COMMENT '当前装甲(提供百分比减伤)',
    armor_max     DOUBLE      NOT NULL DEFAULT 0 COMMENT '被破甲削蚀后的装甲上限',
    hull          DOUBLE      NOT NULL DEFAULT 0 COMMENT '结构值(归零则损毁并弹射)',
    status        ENUM('IDLE','EXPEDITION','REPAIR','SCRAPPED') NOT NULL DEFAULT 'IDLE',
    crew_cats     INT         NOT NULL DEFAULT 0 COMMENT '乘员猫数量(轻装/装甲 2，机甲 3)',
    repair_ends_at BIGINT     DEFAULT NULL COMMENT '维修完成时间戳',
    expedition_id VARCHAR(32) DEFAULT NULL COMMENT '当前占用它的远征任务 ID',
    acquired_at   BIGINT      NOT NULL COMMENT '获得时间戳',
    INDEX idx_slot_planet_status (slot_id, planet_id, status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='载具实例表(每辆车一行)';

-- ========================================================
-- 12. 敌对天网与 BOSS 状态表 (boss_state)：单存档一行
-- ========================================================
CREATE TABLE boss_state (
    slot_id        TINYINT PRIMARY KEY,
    last_tick_time BIGINT  NOT NULL DEFAULT 0 COMMENT '后台演化离线补算锚点',
    threat_level   INT     NOT NULL DEFAULT 1 COMMENT '欧米伽威胁等级 1~10',
    expansion_rate DOUBLE  NOT NULL DEFAULT 1.0 COMMENT '无人工厂扩张速率',
    fleet_strength DOUBLE  NOT NULL DEFAULT 500 COMMENT '核心舰队总战力',
    suspicion_toward_player DOUBLE NOT NULL DEFAULT 0 COMMENT '对玩家的锁定戒备度',
    rage           DOUBLE  NOT NULL DEFAULT 0 COMMENT '通缉热度 0~100(与警戒度独立)',
    convoy_ends_at BIGINT  DEFAULT NULL COMMENT '下一班矿石车队出发/到达时间戳',
    factory_frozen_until BIGINT DEFAULT NULL COMMENT '兵工厂断料停工到期时间戳',
    intel_level    DOUBLE  NOT NULL DEFAULT 0 COMMENT '侦察情报破译度 0~1',
    raid_ends_at   BIGINT  DEFAULT NULL COMMENT '下一次前哨掠夺突袭时间戳',
    raid_target_planet INT DEFAULT NULL COMMENT '突袭目标星球 ID',
    satellite_count INT    NOT NULL DEFAULT 0 COMMENT '在轨侦察卫星存活数',
    -- {"active":false,"damaged_facilities":[],"scattered_cats":0,"regroup_progress":0,"cooldown_until":null}
    bombardment_state JSON DEFAULT NULL COMMENT '大轰炸进行态与灾后重建状态'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='欧米伽天网演化状态表';

-- ========================================================
-- 13. 生涯统计表 (career_stats)：增量累加
-- ========================================================
CREATE TABLE career_stats (
    slot_id        TINYINT PRIMARY KEY,
    playtime_seconds  BIGINT NOT NULL DEFAULT 0 COMMENT '累计游玩时长(秒)',
    total_catnip      DOUBLE NOT NULL DEFAULT 0 COMMENT '历史采摘猫薄荷总量',
    total_scrap       DOUBLE NOT NULL DEFAULT 0 COMMENT '历史产出废铁总量',
    total_chips       DOUBLE NOT NULL DEFAULT 0 COMMENT '历史获得芯片总量',
    total_alloys      DOUBLE NOT NULL DEFAULT 0 COMMENT '历史熔炼合金总量',
    total_battery     DOUBLE NOT NULL DEFAULT 0 COMMENT '历史合成电池总量',
    total_kwh         DOUBLE NOT NULL DEFAULT 0 COMMENT '历史发电总量(kWh)',
    total_cats_born   INT    NOT NULL DEFAULT 0 COMMENT '历史诞生猫口总数',
    best_short_profit DOUBLE NOT NULL DEFAULT 0 COMMENT '单笔做空最高收益(算力币)',
    smuggling_volume  DOUBLE NOT NULL DEFAULT 0 COMMENT '走私总流水(算力币)',
    expeditions_completed INT NOT NULL DEFAULT 0 COMMENT '完成远征次数',
    bombardment_survived  INT NOT NULL DEFAULT 0 COMMENT '轨道轰炸幸存次数',
    fastest_rebuild_seconds INT DEFAULT NULL COMMENT '最快重建复兴耗时(秒)',
    updated_at     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='生涯统计表';

-- ========================================================
-- 14. 成就徽章表 (achievements)
-- ========================================================
CREATE TABLE achievements (
    slot_id        TINYINT     NOT NULL,
    achievement_id VARCHAR(32) NOT NULL COMMENT '徽章 ID，定义见前端常量',
    progress       DOUBLE      NOT NULL DEFAULT 0 COMMENT '当前进度(可大于目标值)',
    unlocked_at    BIGINT      DEFAULT NULL COMMENT '解锁时间戳，NULL 表示未解锁',
    PRIMARY KEY (slot_id, achievement_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='成就徽章表';

-- ========================================================
-- 15. 动态语料模板库 (event_templates)：补 LRU 冷却
-- ========================================================
CREATE TABLE event_templates (
    id             BIGINT      AUTO_INCREMENT PRIMARY KEY,
    slot_id        TINYINT     NOT NULL,
    phase_id       VARCHAR(32) NOT NULL COMMENT '所属时代阶段 PHASE_1_SURFACE / PHASE_2_ORBIT ...',
    category       ENUM('RADIO_NEWS','BBS_POST','DISASTER_ALERT') NOT NULL,
    template_text  TEXT        NOT NULL COMMENT '含槽位 {cat} {building} {resource} {stock}',
    impact_stock   VARCHAR(32) DEFAULT NULL COMMENT '联动股票代码(可空)',
    impact_val     DOUBLE      NOT NULL DEFAULT 0 COMMENT '联动大盘动量冲击值',
    last_used_time BIGINT      DEFAULT NULL COMMENT '上次被展示时间戳(LRU 防重复)',
    use_count      INT         NOT NULL DEFAULT 0 COMMENT '被展示次数',
    created_at     DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_lru (slot_id, phase_id, category, last_used_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='动态事件语料模板库';

-- ========================================================
-- 16. 通用小游戏状态表 (minigame_state)
-- 星球特色玩法共用：密电译码(cipher_decode) / 矿脉扫描(vein_scan) / 熔炉配比(forge_recipe)
-- 玩法定义（解锁星球、奖励池、冷却）见 static/minigames.json
-- ========================================================
CREATE TABLE minigame_state (
    slot_id        TINYINT     NOT NULL,
    planet_id      INT         NOT NULL,
    minigame_id    VARCHAR(32) NOT NULL,
    state          JSON        NOT NULL COMMENT '玩法私有状态（棋盘/配方/扫描配额）',
    last_tick_time BIGINT      NOT NULL DEFAULT 0 COMMENT '离线推进锚点',
    best_score     DOUBLE      NOT NULL DEFAULT 0 COMMENT '历史最好成绩（成就用）',
    play_count     INT         NOT NULL DEFAULT 0 COMMENT '累计游玩次数',
    PRIMARY KEY (slot_id, planet_id, minigame_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='通用小游戏状态表';
```

---

## 5. JSON 种子文件（不入库）

| 文件 | 内容 | 说明 |
| :--- | :--- | :--- |
| `backend/static/jobs.json` | 工种字典 | 母星 5 工种 + 星际 4 大高维职业：产出公式、工位上限来源、解锁条件、加成 |
| `backend/static/facilities.json` | 设施字典 | 造价基数与递增系数、工位产出、承载力加成、发电量、解锁条件 |
| `backend/static/cat_plants.json` | 猫草图鉴 | 25~30 种异星猫草：四大流派标签、生长/突变参数、在田光环、采摘变现 |
| `backend/static/vehicle_types.json` | 车型字典 | 轻装猫车 / 全地形装甲猫车 / 重型破拆机甲（+ 星际星舰）：三层属性、造价、乘员、机位、模块槽 |
| `backend/static/expedition_targets.json` | 拾荒与远征目标 | 废弃沃尔玛 / 市中心电脑城 / 地下军火库 / 地下矿洞：耗时、警戒度代价、掉落表、所需载具、随机事件标签 |
| `backend/static/doctrines.json` | 星系法典政令 | 8 条政令：所需文明凝聚力、效果、是否可升级 |
| `backend/static/minigames.json` | 小游戏字典 | 解锁星球、玩法类型、奖励池、冷却与配额规则 |
| `backend/static/techs_planet0.json` | 母星 19 节点科技树 | 手工编排的 DAG（前置、阶梯、算力成本、风味文案），初始化时写入 `tech_records` |
| `backend/static/templates_default.json` | 初始时代语料 | 第一批电台/BBS 段子，避免首启无内容 |
| `backend/static/fallback_techs.json` | 兜底科技卡池 | 三道防火墙的最终保底（沿用原设计） |

**原则**：这些文件的数值必须与 [数值平衡表.md](数值平衡表.md) 保持一致；改数值只改 JSON 与平衡表，不动表结构。

---

## 6. 与旧稿的差异说明

| 旧稿问题 | 定稿处理 |
| :--- | :--- |
| 缺电池、生物润滑脂字段 | `colony_state` 补 `battery`、`lube`（道具型资源） |
| 电力只有 `power_net` 一种口径 | 拆分净电力(kW) / 蓄电池(kWh) / 电池道具三级口径 |
| 缺载具乘员猫分桶 | `colony_state` 补 `job_crew` |
| 缺发电设施等级 | 补 `power_wheels`、`solar_panels` |
| `tech_records` 无 DAG 前置关系 | 补 `parent_ids`、`tier`、`node_order`、风味与机制字段 |
| `event_templates` 无 LRU 字段 | 补 `last_used_time`、`use_count` 与索引 |
| 深网无时间基准、无 K 线、无在途订单、无发帖冷却 | 全部补齐 |
| 无多槽位存档位置 | 新增 `save_slot`，全表带 `slot_id` |
| 无成就与统计表 | 新增 `career_stats`、`achievements` |
| 无星图/跨星物流位置 | 新增 `planet_state` |
| 大轰炸惩罚状态无处存 | `boss_state` 补 `rage` 与 `bombardment_state` |
| 只有基地表有时间戳 | 全部演化型系统补 `last_tick_time`，倒计时统一改 `ends_at` |
| 载具只按数量存，与"每辆车独立三层血条 / 改装 / 乘员"矛盾 | 拆出 `vehicle_units`，逐辆一行（机位上界 12/24 卡死） |
| 建表默认值直接送 5 猫 5 窝 3 农田 | 默认值全 0，开局走初始化流程 |
| 拾荒目标没有定义 | 移入 `expedition_targets.json` 种子 |
| 工种与设施硬编码在 `colony_state` 的列里，加一个就要改表 | 抽成 `labor_buckets` / `facility_state` 通用表 + `jobs.json` / `facilities.json` 定义 |
| 论坛字段写成了"物理网段来源"，等于暴露 IP（与匿名设定矛盾） | 删除 `subnet_origin`，改为遥测报错码 + 账号画像，**保持完全匿名** |
| 事件与行情之间没有时序关系 | `forum_posts` 加 `reaction_pattern`（四种事件滞后曲线） |
| 缺"盘口大单抢跑"这条线索 | `darknet_state` 加 `whale_events` |
| 文明凝聚力与星系法典政令无处存储 | `save_slot` 加 `unity` + `doctrines` |
| 星球特色小游戏的状态无处存储 | 新增 `minigame_state` 通用表 |

---

## 7. 抽象化之后的扩展方式（以后加东西怎么加）

| 想加什么 | 要做的事 | 要不要改表结构 |
| :--- | :--- | :--- |
| 新工种 / 新职业 | 改 `jobs.json` + 《数值平衡表》 | ❌ 不用（`labor_buckets` 自动装下） |
| 新设施 / 新建筑 | 改 `facilities.json` + 造价曲线 | ❌ 不用（`facility_state` 自动装下） |
| 新植物 / 新图鉴 | 改 `cat_plants.json` | ❌ 不用（`garden_state` 已是 JSON 网格） |
| 新车型 | 改 `vehicle_types.json` | ❌ 不用（`vehicle_units` 逐辆建行） |
| 新小游戏 | 改 `minigames.json` + 一个前端组件 | ❌ 不用（`minigame_state` 通用） |
| 新星球 | 加 `planet_state` 行 + 特化科技种子 | ❌ 不用 |
| 新的复杂玩法状态 | 先塞进对应表的 JSON 字段 | ❌ 不用（除非要按字段查询才拆表） |

> **已否决项**：原对齐稿的"第三个通关大奖小游戏（4×4 模块化网格拼装 / 图灵主机接线）"**不再采用**，其位置由本节的**星球特色小游戏矩阵**取代（见《数值平衡表》§16）。

---

## 8. 存档与对账协议

> **核心矛盾**：前端每 100ms 累加数值是为了"好看"，后端才是"作数"。两者必须共用同一套公式，且**一律以后端为准**。

### 8.1 职责划分

| 角色 | 干什么 | 不干什么 |
| :--- | :--- | :--- |
| 前端 | 100ms 插值渲染、进度条动画、每 15 秒提交一次预测快照 | **不作为数值真值**；不改动任何公式参数 |
| 后端 | 用 `core/balance.py` 的同一套公式重算时间区间并落库 | 不信任前端提交的数值 |

### 8.2 快照落库流程

1. 前端 `POST /colony/snapshot` 提交 `{ 预测值, client_time }`；
2. 后端取出该槽位的 `last_tick_time`，用 $\Delta t = now - last\_tick\_time$ **自己重算**这段时间的收益（直接复用离线结算引擎）；
3. 落库，并把 `last_tick_time` 刷新为 `now`；
4. 比对"前端预测值 vs 后端重算值"，偏差 **> 0.5%** 记一条 `SNAPSHOT_DRIFT` warning（开发期排查用），**结算结果仍以后端为准**。

### 8.3 离线与"关页面最后 15 秒"

- 关闭页面时最后 ≤15 秒的收益：前端内存里的值**不落库**，重开时由后端从 `last_tick_time` 重算覆盖，**既不丢失也不重复**；
- 读档流程：后端从 `last_tick_time` 补算到当前 → 生成《离线休整报表》→ 一次性返回；
- **时间回拨**（$\Delta t < 0$）：把 `last_tick_time` 重置为当前时间并记日志，**不做任何补偿**（单机自用，不做防作弊，但绝不允许出现负数资源）。

### 8.4 精度与舍入

| 数据 | 存储精度 | 说明 |
| :--- | :--- | :--- |
| 资源存量 | 保留 2 位小数 | 展示层可再四舍五入 |
| 进度类（繁育 / 研究 / 暴露度） | 保留 4 位小数 | 避免小数进度被反复截断而"永远涨不满" |
| 时间戳 | 秒级 `BIGINT` | 全部用绝对时间戳，不用相对剩余秒数 |

### 8.5 存档导出与导入

- **导出**：把该 `slot_id` 下所有表的行序列化为 JSON → Gzip 压缩 → Base64，并带上 `save_version` 与 `checksum`；
- **导入**：先校验 `checksum` 与 `save_version` → 走版本迁移函数 → 在**单个事务内整体替换**；
- 校验失败的存档一律拒绝，**不做"尽力恢复"**（避免脏数据死锁）。

#### 8.5.1 编码落地口径（v1.5 补充）

* **存档信封**：`{"save_version": 1, "slot_id": 1, "exported_at": <秒时间戳>, "checksum": "<64 位十六进制>", "tables": { "<表名>": [ {行}, ... ] }}`，整体用 `Gzip(level=6) → Base64` 编码；导出接口直接返回 `base64_payload / checksum / save_version / raw_bytes / total_rows`。
* **checksum 算法**：`SHA-256` 作用于 `tables` 子树的标准 JSON（`sort_keys=True`、`separators=(",", ":")`、`ensure_ascii=False` 的 UTF-8 字节）。**改内容不改校验和会被识破**（测试已覆盖）。
* **参与搬运的表**：15 张带 `slot_id` 的表（`event_templates` 是全局模板，不随存档走）；导入时若出现未知表名，按"一律拒绝"处理（`400 INVALID_SAVE_PAYLOAD`）。
* **跨槽位导入**：行按**目标槽位**重定向（导出槽位 1 可直接导入槽位 2），`save_slot.save_version` 与 `save_slot.checksum` 在导入后刷新为当前值与本次校验和；`active_planet_id` 若越界回落到母星。
* **全局自增主键重分配**：`vehicle_units.unit_id` 与 `forum_posts.id` 是**全局自增**（不属于 `(slot_id, …)` 复合键），跨槽位导入若原样写回会撞主键（真机 MySQL 实测 `Duplicate entry '1' for key 'vehicle_units.PRIMARY'`）。因此导入时这两列交回数据库重新分配，并把 `military_state` 的 `hospital_queue[].unit_id` / `active_expeditions[].unit_ids` 引用**按映射回写**；`forum_posts.id` 无外部引用，直接丢弃原值。
* **错误码**：`INVALID_SAVE_PAYLOAD`（Base64/Gzip/JSON 结构损坏、含未知表、超过 8 MB 解压上限）、`CHECKSUM_MISMATCH`（校验和不符）、`UNSUPPORTED_SAVE_VERSION`（存档版本高于后端）、`SAVE_SLOT_EMPTY`（导出空槽位，404）。
* **版本迁移**：`_migrate()` 为单一收口钩子；当前只有 `save_version=1`，旧档升级逻辑将来在此逐版补写，**不散落在各业务服务里**。

---

## 9. 修订记录

| 版本 | 日期 | 内容 |
| :--- | :--- | :--- |
| v1.0 | 2026-09-12 | 首次成文：取代早期稿的表设计，7 张表扩为 13 张；确立 slot_id 全局隔离、时间基准规则、载具逐辆实例化、"玩家状态进库 / 策划数值进种子"四项原则 |
| v1.1 | 2026-09-12 | 抽象化改造：新增 `labor_buckets`（工种通用分桶）、`facility_state`（设施通用状态）、`minigame_state`（小游戏通用状态），表数 13 → 16；`colony_state` 瘦身为纯核心状态；`save_slot` 接入文明凝聚力与星系法典；`forum_posts` 移除"物理网段"改为遥测码 + 账号画像并新增事件滞后模式；`darknet_state` 新增盘口大单异动；写入"定义数据化、状态通用化"核心原则 |
| v1.2 | 2026-09-12 | 新增第 8 章《存档与对账协议》：明确后端权威 + 前端预测、15 秒快照重算覆盖、时间回拨处理、精度舍入规则、导入导出校验流程 |
| v1.3 | 2026-09-12 | §2.4 补《编码落地补充》：16 张表在 SQLAlchemy 2.0 ORM 全部落地（实机 MySQL 8.0.43 建表通过）；明确 `planet_state` 四星一次建行、`garden_state` 7×7/中央 9 格、`minigame_state` 按解锁星球建行，以及 `military_state` / `boss_state` / `darknet_state` 的开局落库默认值 |
| v1.4 | 2026-09-12 | `colony_state` 新增 `manual_scavenge_clicks INT NOT NULL DEFAULT 0`（冷启动手点废墟次数，口径见数值平衡表 §3.4）；表数仍为 16 张，随阶段 3（工位循环与冷启动）落地 |
| v1.5 | 2026-09-13 | §8.5 补《编码落地口径》：存档信封结构、SHA-256 作用于标准 JSON 化的 `tables` 子树、15 张表参与搬运（`event_templates` 除外）、跨槽位导入重定向与 `save_slot` 校验和刷新、**全局自增主键（`vehicle_units.unit_id` / `forum_posts.id`）导入时重分配 + 军事状态引用回写**、四类错误码、`_migrate()` 单一版本迁移收口；表结构未变（仍 16 张） |
