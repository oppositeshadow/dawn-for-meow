# 《喵星破晓》项目全量任务清单与协作节流指南

> 更新时间：2026-09-19  
> 文档性质：跨会话总览、任务索引与协作交接入口。  
> 重要说明：本文件不是数值、数据模型或接口契约的权威来源，也不代表已经授权开始编码。实际开发仍按 `AGENTS.md` 的文档权威顺序与开发铁律执行。

最后通过：`B4 / 阶段 22 / P2-8B-19`（第 2 轮验收通过）  
当前任务：`B5 / 阶段 23 / P2-8B-20`（第 3 轮验收退回，按协同文件单点返修）  
下一候选：`B6 / 状态契约、事件回看与后端可观测性`

## 1. 如何使用本文件

后续不必反复重读整个协同历史。每轮只需要：

1. 在本文件中选择一个任务 ID；
2. 把该任务的“目标、范围、验收门槛”复制到 `协同工作计划与验收反馈.md`，补足精确文件白名单后实施；
3. 实施方完成后只填写差异、测试数字与遗留风险；
4. 验收方只审该任务的差异，不顺手进入下一任务；
5. 验收通过后，在本文件将状态由 `待完成` 更新为 `已完成`，再选择下一项。

状态标记：

- `✅ 已完成`：已有实现并经过独立验收；
- `🟡 已实现待返修/验收`：主体已完成，但尚未达到本轮验收门槛；
- `⬜ 待完成`：尚未开始；
- `🔍 待审计定稿`：已有需求方向，但开工前仍需核对真实调用链、数据落点或玩法边界。

## 2. 当前项目所处位置

### 2.1 已达到的产品能力

当前项目已经不是“原型尚不能玩”的状态。依据 README、WBS、开发进展文档与现有代码，主线已经可以从冷启动一路推进到终局：

- 地表生存、资源、电力、人口、设施、科技与离线结算可运行；
- 载具、战斗、远征、急救舱、安防和警戒度可运行；
- 猫草实验室、全部在田光环、小游戏、深网基础经济可运行；
- 母星与三颗外星球、跨星航线、周期事件、特化科技可运行；
- 火箭升空、欧米伽终局决战、通关碑文和演职员表可运行；
- 项目文档称其为“Milestone 1 模块 A～O 全部落地、可完整通关”。

因此，当前工作的性质是：**在已可完整通关的单机版本上，把尚未做实的深层机制、灾难闭环与文档承诺补齐，最终收敛成稳定的本地单机 v1.0 候选版本。**

### 2.2 当前可复现基线

| 项目 | 当前结果 | 说明 |
| :--- | :--- | :--- |
| 后端接口 | 48/48 | 文档账本与代码一致 |
| 数据表 | 16 张 | 现有模型基线 |
| 后端测试 | 595 collected、594 passed、1 skipped | 2026-09-19 B5 第 2 轮独立复跑 |
| 文档声明检查 | 211/211 | 代码中缺失 0 项 |
| 静态总检 | 全绿 | 接口、声明、科技节奏、减伤曲线均通过 |
| 当前活动任务 | B5 / 阶段 23 / P2-8B-20 | 第 3 轮验收退回；为四星隔离快照补入 garden_state 后复验 |

### 2.3 当前已知管理问题

- `开发进展与后续计划.md` 的开头日期、测试数字和“最近结果”存在滞后；文件后半还出现了重复表格/章节内容，需要在专门的文档清理任务中整理，不能继续把其中的手写汇总数字当作事实来源。
- `tests/test_stats.py::test_career_panel_baseline` 曾登记为约 1/4 概率偶发失败；目前没有稳定复现证据，继续遵守“先取证、后修复”。
- 当前工作区包含长期累计差异。每轮必须按任务白名单审查，不能把“工作区里已经存在”误当作“本轮可以随意修改”。

## 3. 已完成任务总表

### 3.1 主体模块 A～O

下表表示主路径已经具备可玩实现；其中少数增强分支仍列在第 5 节待办中。

| 模块 | 已完成主能力 | 状态 |
| :--- | :--- | :--- |
| A 底层时钟与离线结算 | 客户端 Tick、快照存盘、确定性离线收益 | ✅ 已完成 |
| B 资源与罢工 | 仓储、电力、断粮/低需求停工 | ✅ 已完成 |
| C 人口与劳动力 | S 曲线繁育、工种池、迟滞换班 | ✅ 已完成 |
| D 设施 | 建造、升级、工位与能源设施 | ✅ 已完成 |
| E 科技树 | DAG 研发、Tier 节奏、范式突破 | ✅ 已完成 |
| F 安防 | 警戒度、诱饵、静默、自动安防预案 | ✅ 已完成 |
| G 军备战斗 | 载具、三层抗性、远征、截杀与急救舱 | ✅ 已完成 |
| H 猫草实验室 | 网格、突变、图鉴、采摘与全部 7 项在田光环 | ✅ 已完成 |
| I 智械深网 | 身份、股市、黑市和基础论坛能力 | ✅ 主路径完成，增强分支待办 |
| J LLM 导演 | 低频生成、本地兜底与安全边界 | ✅ 已完成 |
| K 火箭与星图 | 巡航导弹、发射井、升空、多星后勤与职业 | ✅ 已完成 |
| L Boss 与终局 | 欧米伽演化、掠夺防卫、终局决战 | ✅ 主路径完成，轨道轰炸闭环待办 |
| M 公频电台 | 本地模板与批量语料 | ✅ 已完成 |
| N 冷启动与存档 | 新手节奏、多槽位、导入导出、统计成就 | ✅ 已完成 |
| O 小游戏矩阵 | 密电译码、矿脉扫描、熔炉配比 | ✅ 已完成 |

### 3.2 P0/P1 增强事项

| ID | 任务 | 已完成结果 | 状态 |
| :--- | :--- | :--- | :--- |
| P0-1 | 肥宅高能薄荷繁育加成接线 | 在田每株使繁育倍率 +20%，真实进入结算 | ✅ 已完成 |
| P0-2 | 金刚地衣装甲加成接线 | 在田装甲 +15% 真实进入战斗 | ✅ 已完成 |
| P1-3 | 爆裂刺果与诱饵制造 | 诱饵制造接口、科技门槛、造价折扣与 UI 全链路 | ✅ 已完成 |
| P1-4 | 铁线荆棘藤巡逻延缓 | 扫描到达和诱饵冷却均按每株 +30% 延缓 | ✅ 已完成 |
| P1-5 | 跨星循环航线与未来危险窗口 | 1～10 趟、缺料/危险挂起、恢复、取消与时间轴 | ✅ 已完成 |
| P1-6 | 乘员休养与范式突破 | 动态休养、前代科技 5 折、极客产出 ×2 | ✅ 已完成 |
| P1-7 | 星球周期资源倍率拆分 | 发电、产粮、拾荒与科研倍率按资源拆分 | ✅ 已完成 |

### 3.3 协同阶段 1～18

| 阶段 | 任务与成果 | 状态 |
| ---: | :--- | :--- |
| 1 | 恢复默认 pytest 临时目录与全量测试基线 | ✅ 已完成 |
| 2 | 冻结既有批次，完成后端、前端、文档、契约与真机收尾审计 | ✅ 已完成 |
| 3 | 轨道轰炸口径冲突清理与可执行决策包；定稿触发、目标、防空、设施、人口、修复和引导规则 | ✅ 已完成 |
| 4 / P2-8B-1 | 75% 物资损失与 20+20 应急保底纯计算内核 | ✅ 已完成 |
| 5 / P2-8B-2 | 怒气阈值 100 与 600 秒和平期纯状态转换内核 | ✅ 已完成 |
| 6 / P2-8B-3 | 设施累计原造价 ×50% 修复费纯计算内核 | ✅ 已完成 |
| 7 / P2-8B-4 | 受损设施有效等级归零、多星隔离纯计算内核 | ✅ 已完成 |
| 8 / P2-8B-5 | 每 15 秒归队 2 只、支持余秒累计的纯计算内核 | ✅ 已完成 |
| 9 / P2-8B-6 | 地面猫保底 3 只与保护人口守恒纯计算内核 | ✅ 已完成 |
| 10 / P2-8B-7 | 在途远征与急救舱保护人口只读聚合器 | ✅ 已完成 |
| 11 / P2-8B-8 | 四条载具损毁路径统一入院，修复人口蒸发与休养公式绕行 | ✅ 已完成 |
| 12 / P2-8B-9 | 住院期间离线待命重算与到期出院人口守恒 | ✅ 已完成 |
| 13 / P2-8B-10 | 按人口、母星优先和星球 ID 稳定选择受灾目标 | ✅ 已完成 |
| 14 / P2-8B-11 | 和平期、怒气阈值、目标选择的只读触发规划器 | ✅ 已完成 |
| 15 / P2-8B-12 | 指定受灾星四项物资损失只读规划器 | ✅ 已完成 |
| 16 / P2-8B-13 | 显式 seed 选择 1～2 座设施、修复费与有效等级只读规划器 | ✅ 已完成 |
| 17 / P2-8B-14 | 目标星总猫口、保护人口、保底与走散量只读规划器 | ✅ 已完成 |
| 18 / P2-8B-15 | 0～3 枚巡航导弹防空梯级减损纯内核；专项测试读取数值真源核对三个常量登记名 | ✅ 已完成（第 3 轮） |

## 4. 轨道轰炸完整闭环：剩余任务与推荐顺序

这组任务是当前最高优先级。推荐继续保持“一轮一个高风险状态边界”，但把全路线固定下来，避免每轮重新规划。

### B0. 阶段 18 最小返修

- **状态**：✅ 已完成（第 3 轮验收通过）。
- **目标**：在现有第 7 条专项测试中读取根目录 `数值平衡表.md`，断言三个防空常量名均已登记。
- **只允许修改**：`backend/tests/test_bombardment_air_defense.py` 与协同记录。
- **不得修改**：防空函数、数值、服务、接口、前端和三份开发文档。
- **完成标志**：保持 7 个专项收集项；专项 7、组合 50、全量 556、文档审计、总审计及 `git diff --check` 全绿。

### B1. 受灾星防空只读规划器

- **状态**：✅ 已完成（第 2 轮验收通过）。
- **目标**：按 `(slot_id, planet_id)` 读取受灾星 `MilitaryState.cruise_missiles`，复用 `calculate_bombardment_air_defense()`，返回灾前、参战/拟消耗、灾后导弹和最终损失率。
- **核心要求**：缺行明确报错；不回退母星或当前活跃星；不写库存；不调用资源损失规划器；运行时调用方保持 0。
- **验收重点**：0/1/2/3/超额库存、跨槽跨星隔离、只读无副作用、返回对象隔离、常量推导测试。
- **完成后的推进程度**：防空读取和计算完整，但玩家仍不会真实消耗导弹或获得减损。

### B2. 地面工种撤岗与乘员同步只读规划器

- **状态**：✅ 已完成（第 3 轮验收通过）。
- **目标**：把 B17 已算出的走散人数映射成确定性的地面撤岗计划，不立即写库。
- **核心要求**：
  - 急救舱与在途远征乘员不可撤；
  - 优先保留生存工种，农夫最后撤；
  - 若需要撤 `crew`，只处理同星地面、未出征载具，并同步规划 `LaborBucket("crew")` 与 `VehicleUnit.crew_cats`；
  - 输出撤岗前后各桶、载具乘员变化与守恒证明；
  - 不修改 `total_cats`、`job_idle` 或 ORM。
- **验收重点**：保护人数超过保底、工种不足、多个地面载具、远征载具排除、跨星隔离、撤岗总数恰好等于走散人数。
- **完成后的推进程度**：人口写入所需的最后一个危险计算环节被锁定。

### B3. 完整轰炸只读总计划

- **状态**：✅ 已完成（第 2 轮验收通过）。
- **目标**：用一个只读编排入口组合触发、目标、防空、资源、设施、人口和撤岗计划，生成未来原子事务的完整写入包。
- **核心要求**：
  - 防空得到的 `loss_ratio` 必须覆盖资源规划器基础 75% 比例，不能重复扣减；
  - 设施选择 seed 由完整事件稳定输入派生，重载可复现；
  - 所有子计划共用同一个目标星和灾前快照；
  - 任一子计划失败时不产生半份结果、不写库；
  - 返回明确的存档级变化、星球级变化、事件摘要与守恒数据。
- **验收重点**：阈值前无计划、阈值到达完整计划、和平期冻结、空设施、零导弹、跨存档、重复调用确定性。
- **完成后的推进程度**：所有规则被合成一张可审计“事务蓝图”，但仍未在玩法中生效。

### B4. 受损设施运行时门控与修复后端闭环

- **状态**：✅ 已完成（第 2 轮验收通过）。
- **目标**：让 `active_damages` 真正影响有效设施、工位、离线产出、电网出力和电池容量，并提供紧急修复接口。
- **核心要求**：
  - `damaged_facilities` 继续作为历史日志；活动停工只使用 `active_damages`；
  - 受损设施保留原等级记忆，有效等级视为 0；
  - 修复费复用累计原造价 ×50% 内核；
  - `POST /colony/repair-facility` 按 `(slot_id, planet_id, facility_id)` 校验、扣款并恢复；
  - 修复后自动恢复有效工位、产出、电网、电池容量及先前岗位记忆；
  - 资源不足、非受损设施、跨星修复均有明确错误码。
- **验收重点**：电池容量 200/400/200/400 四态、真实停产而非只在界面标红、修复幂等、50% 多资源扣款准确。
- **完成后的推进程度**：设施灾后恢复链可单独运行，不会出现“受损却后台照产”或“能坏不能修”。

### B5. 走散猫持久化与在线/离线归队闭环

- **状态**：🟡 已实现待返修（第 3 轮验收未通过；详见协同文件 §27.9）。
- **目标**：实现 `scattered_cats` 与 `regroup_progress` 的持久化推进，在线 Tick 与离线结算复用同一纯函数。
- **核心要求**：
  - 走散时原子更新活跃 `total_cats`、工种/载具撤岗结果和走散队列；
  - 纸箱窝未受损或已修好后，每 15 秒归队 2 只；
  - 归队执行 `total_cats += K`、`job_idle += K`、`scattered_cats -= K`；
  - 离线待命重算不得再次减走散猫；
  - 分段 Tick 与一次长离线结果相同，余秒不丢失；
  - 多次重载、重复结算和完成后再推进均幂等。
- **验收重点**：7s+8s 等价 15s、分批归队后离线不回退、纸箱窝阻断/修复恢复、跨星隔离、人口总量守恒。
- **完成后的推进程度**：猫口灾后自愈链完整，不会永久卡死或人口蒸发。

### B6. 状态契约、事件回看与后端可观测性

- **状态**：⬜ 待完成。
- **目标**：通过现有可达接口把轰炸状态完整提供给前端和存档重载。
- **核心要求**：
  - 优先复用 `ColonyStateEnvelope`、`MilitaryEnvelope`、`HangarView`，避免创建无消费点的新 Envelope；
  - 后端 Schema 至少透出 `active_damages`、`scattered_cats`、和平期剩余秒数、最近轰炸摘要和可执行修复信息；
  - 新字段完成“服务/裁剪 → Pydantic Schema → 前端类型”三处同步；
  - 事件摘要能说明目标星、导弹消耗、实际损失率、资源损失、设施受损和走散人数；
  - 存档导出/导入与旧存档缺字段有默认兼容。
- **验收重点**：Pydantic 不静默丢字段、旧存档可读、跨星状态不串、接口契约扫查通过。
- **完成后的推进程度**：后端信息链完整，前端无需猜测或自行计算。

### B7. 灾后前端操作与指名道姓引导

- **状态**：⬜ 待完成。
- **目标**：让玩家看得到灾害、知道下一步、可以完成修复和等待归队。
- **核心要求**：
  - `ThreatBar.vue` 常驻紧急横幅，明确提示去设施面板修复；
  - `FacilitiesPanel.vue` 将活动受损设施标红、显示维修费并提供紧急抢修按钮；
  - `WarRoomPanel.vue` 展示和平期、导弹参战/剩余和防空减损结果；
  - 显示走散人数、归队条件和剩余进度；
  - 多条件静默失败必须给出具体下一步，禁止只有数字停住。
- **验收重点**：前端类型检查与构建通过；无数据、旧存档、多个星球、资源不足、修复成功均有明确状态。
- **完成后的推进程度**：灾后恢复在 UI 上可操作，不依赖手改数据库。

### B8. 单事务原子应用与真实致怒入口接线

- **状态**：⬜ 待完成。
- **目标**：最终实现唯一 `trigger/apply_bombardment` 状态机，并接入真实致怒动作。
- **核心要求**：
  - 在一个数据库事务中应用 Boss 怒气/和平期、巡航导弹消耗、资源扣减与保底、设施活动受损、人口撤岗与走散、事件记录；
  - 接入真实致怒入口，例如发射巡航导弹和战车截杀相关链路；
  - 达到阈值的同一动作立即结算，不允许“满怒但悬挂到下一 Tick”；
  - 任一校验失败整体回滚；
  - 重复请求、重载和截止边界不重复轰炸；
  - 不把政令“对空伤害 +25%”或不可获取的激光炮台混入轨道防空。
- **验收重点**：阈值前、恰好阈值、超过阈值、和平期、事务回滚、重载幂等、跨槽跨星、数值实际变化和事件摘要一致。
- **完成后的推进程度**：轨道轰炸从“纯函数与只读规划器”升级为玩家真正会遭遇、能防御、能恢复的完整玩法。

### B9. 轨道轰炸端到端终验与文档收口

- **状态**：⬜ 待完成。
- **目标**：用真实存档跑通“致怒 → 防空 → 受灾 → 停工 → 修复 → 归队 → 和平期后再次触发”。
- **验收要求**：
  - 全量 pytest、前端构建、文档审计、接口账本、契约扫查、`git diff --check` 全绿；
  - 真机覆盖 0/1/3/5 枚导弹、空设施、低资源应急包、猫口保底、修复和离线归队；
  - 所有数值从权威文档/常量推导；
  - 更新数值、数据库、工程、GDD、WBS 和进度文档中真正受影响的部分；
  - 不再保留“已落地但运行时零调用”的过期声明。
- **完成后的推进程度**：P2 事项 8 完整关闭，GDD Milestone 2 的“欧米伽大轰炸、75% 掠夺、50% 修复与闻香回流”成为可玩闭环。

## 5. 轨道轰炸之后的全部已登记待办

以下事项来自当前进度看板与《数值平衡表》§18。它们的优先级低于第 4 节，开工前仍应各做一次小型真实调用链审计。

### C1. 暴露度四段危机

- **状态**：⬜ 待完成。
- **权威数值**：
  - 一级禁言 300 秒；
  - 二级审讯扣除 15% 算力币；
  - 三级追踪器拆除消耗 50 芯片；
  - 四级危机使警戒度增加 40。
- **任务细节**：建立暴露度状态机、绝对截止时间、资产扣减、追踪器状态与拆除入口、四级危机和现有安防链路联动；补状态透出、前端操作、离线/重载和幂等测试。
- **完成标志**：4 个 `EXPOSURE_*` 常量真实消费，四段危机均能由玩家触发、理解和恢复。
- **建议轮数**：2～3 轮（状态机/后端闭环、前端与终验）。

### C2. 匿名论坛本地裁判与发帖质询

- **状态**：🔍 待审计定稿。
- **任务细节**：
  - 使用 8 组账号画像与 `FORUM_*` 概率建立可复现的帖子真假/流言生成；
  - 使用 `REACTION_PATTERN_WEIGHTS` 建立本地关键词与反应模式裁判；
  - 支持玩家发帖质询、舆论引导、做多/做空影响；
  - 明确管理员封禁、水军和黑客揭露的边界；
  - LLM 只能生成文案，不得决定数值真值；断网必须有本地结果。
- **完成标志**：9 个论坛相关死常量真实消费；同一确定性输入可复现；前端有因果说明；不会由 LLM 任意改库存或行情。
- **建议轮数**：3～4 轮。

### C3. 工业副产物应急换算

- **状态**：⬜ 待完成。
- **权威数值**：猫薄荷 10:1 转润滑脂；芯片 5:1 转蓄电池组。
- **任务细节**：先确认物品真实资源键与存储位置；增加纯换算/校验、批量转换服务与接口；前端显示最大可转数量、输入校验和资源不足原因；在线和离线库存口径一致。
- **完成标志**：`CATNIP_PER_LUBE`、`CHIPS_PER_BATTERY` 真实消费且有从常量推导的测试。
- **建议轮数**：1～2 轮。

### C4. 循环远征与股票条件单自动化

- **状态**：🔍 待审计定稿。
- **任务细节**：
  - 远征自动收货、载具健康复检、可继续时自动再次派遣；
  - 战损或乘员休养时暂停并明确提示；
  - 股票止盈/止损条件单在离线 Tick 中按确定性价格序列撮合；
  - 明确订单优先级、资金冻结、重复撮合、导入导出和跨离线区间语义；
  - 若需新表/字段，先更新《数据库设计定稿》并做旧存档兼容。
- **完成标志**：挂机期间无需手工重复操作，离线与在线结果等价且不会重复收货/成交。
- **建议轮数**：3～5 轮。

### C5. 深网与拦截边角分支

- **状态**：🔍 待审计定稿。
- **涉及常量**：
  - `STOCK_PHYSICAL_SABOTAGE_MOMENTUM = 30.0`；
  - `BLACK_MARKET_PRICE_SWING = 0.30`；
  - `INTERCEPT_SUSPICION_CLEAR = 1.0`。
- **任务细节**：分别核对股票物理破坏事件的行情冲击入口、黑市自适应价格波动、战车截杀后警戒清除语义；不能为了消灭死常量而把互不相关的机制硬接在一起。
- **完成标志**：三个常量各有自然调用点、玩家可观察的数值变化和独立测试。
- **建议轮数**：2～3 轮。

### C6. A 类死副本治理

- **状态**：⬜ 待完成，低优先级。
- **范围**：当前 9 个 A 类项，包括 JSON 真源副本、旧口径历史对照和恒真开关。
- **任务细节**：逐个决定“删除、转为一致性测试消费、或明确作为历史对照保留”；不得改变玩法数值，不得仅为扫描数字好看而制造伪调用。
- **完成标志**：每一项都有书面归因，扫描分类与代码真实引用一致。
- **建议轮数**：1 轮审计，必要时再 1 轮最小清理。

### C7. 偶发测试取证

- **状态**：⬜ 持续观察。
- **目标**：只在 `test_career_panel_baseline` 再次失败时保存随机输入、点亮徽章、数据库快照与复现命令。
- **禁止**：没有复现证据时放宽断言、增加重试或吞掉失败。
- **完成标志**：要么获得稳定复现并最小修复，要么在足够多次独立运行后记录为暂不可复现。

### C8. 进度文档结构与数字清理

- **状态**：⬜ 待完成，建议在 B0 后尽快做一次。
- **目标**：清除 `开发进展与后续计划.md` 的重复章节/错位表格，更新日期与 556 测试基线，并让“当前阶段”与协同文件一致。
- **核心要求**：只整理结构和由脚本产生的数字，不改变玩法口径；历史修订记录保留；进度文档仍不得成为数值真源。
- **完成标志**：标题层级唯一、表格闭合、无重复后半段、汇总数字可由脚本复现、`git diff --check` 通过。

## 6. 推荐总顺序与剩余轮数

### 6.1 保守、低风险顺序

1. B0：阶段 18 最小返修；
2. C8：进度文档结构清理；
3. B1～B3：防空、撤岗、完整只读总计划；
4. B4～B7：恢复后端、归队、状态契约和前端操作；
5. B8～B9：最后才接真实原子触发并做端到端终验；
6. C3：工业换算（小而独立，可作为轰炸大任务后的短周期任务）；
7. C1：暴露度四段危机；
8. C5：深网边角分支；
9. C2：论坛本地裁判；
10. C4：挂机自动化；
11. C6、C7：债务治理和偶发失败取证；
12. 最终全项目收尾审计。

### 6.2 轮数预估

| 范围 | 按当前“一阶段一任务”模式 | 适度合并低风险项后 |
| :--- | ---: | ---: |
| 结束轨道轰炸事项 8 | 10 轮左右（含当前返修） | 7～8 轮 |
| P2 事项 9～13 | 11～17 轮 | 7～11 轮 |
| 文档、死副本、终验 | 2～4 轮 | 2～3 轮 |
| **预计合计** | **23～31 轮** | **16～22 轮** |

这里的“轮”是独立实施/验收批次，不是自然日。发现新的真实缺陷会增加轮数；反之，纯文档修正、前端类型同步和相邻低风险测试可在不扩大状态机风险时合并。

## 7. 全部完成后项目能推进到什么程度

### 7.1 完成 B0～B9 后

项目将从“主线能通关、轨道轰炸只有大量纯函数/规划器”推进到：

- Boss 怒气达到阈值会真实触发轨道轰炸；
- 受灾目标星、防空导弹、资源、设施和人口在单事务中结算；
- 玩家可用巡航导弹降低损失；
- 受损设施会真实停工并可按 50% 累计造价修复；
- 走散猫可在线/离线自动归队；
- 状态、事件和下一步操作在 UI 中清楚可见；
- 存档重载、离线结算和重复请求不会重复打击或破坏人口守恒。

这意味着 GDD Milestone 2 的最后一个显著半截机制完成闭环。

### 7.2 完成 C1～C5 后

- 深网暴露度从展示/常量升级为完整四段危机；
- 匿名论坛拥有不依赖网络的确定性裁判和玩家做局闭环；
- 工业瓶颈资源可通过权威比例应急换算；
- 远征与股票具备真正的挂机自动化；
- 股票、黑市和战车拦截的剩余边角数值均有真实消费点；
- 当前 18 个 B 类“功能未落地”死常量原则上可以全部毕业。

### 7.3 完成 C6～C8 与最终审计后

项目可视为：**功能完整、可全程离线/在线游玩、文档与代码一致、适合作为 Localhost 单机自娱版本发布的 v1.0 候选。**

仍不代表：

- 公网服务、多用户并发或云存档已经完成；
- 移动端适配、无障碍、国际化或商业化运维已经完成；
- 所有美术、音效和动画都达到正式商业游戏规格；
- 可以跳过一次完整的新存档长流程体验测试。

## 8. 节省额度与上下文的协作方法

### 8.1 使用固定任务 ID，不再重新描述整个背景

推荐用户指令格式：

```text
开始 B1。按《项目全量任务清单与协作节流指南》把 B1 写入协同文件，只展开这一项。
```

实施完成后：

```text
B1 已完成，按协同文件验收并写回结论。
```

这样每轮只需引用任务 ID，不必重复讲项目历史。

### 8.2 每轮只保留一份“最小交接包”

实施记录固定只写六项：

1. 状态；
2. 实际修改文件；
3. 核心行为与边界；
4. 新增/修改测试；
5. 命令和最终数字；
6. 遗留风险。

不要粘贴完整测试日志、完整代码或大段推理。失败时只记录失败测试名、关键报错和复现命令。

### 8.3 验收只读三类内容

每轮验收优先读取：

1. 当前任务计划段；
2. 本轮 `git diff` 或白名单文件；
3. 实施记录中的测试命令。

除非出现冲突，不重复读取 3000 多行历史协同记录。历史记录只作为争议追溯。

### 8.4 高风险机制单独一轮，低风险同步允许合并

- 必须单独：数据库状态机、人口守恒、原子事务、离线时间推进、随机/幂等语义；
- 可以随主任务合并：对应 Schema、前端类型、准确的修订记录、同一行为的测试维护；
- 可以合并成一轮：纯文档结构修复、脚本生成数字更新、无业务影响的命名同步。

这能减少“每个文件一轮”的浪费，同时避免把多个状态机塞进一个长任务。

### 8.5 测试照跑，但不把输出塞进上下文

项目铁律要求全量测试、真机抽查和文档修订，不能为了省额度跳过。可以节省的是输出：

- 绿灯只记录当前实测汇总（本轮为 `586 collected / 585 passed / 1 skipped`）；
- 只在失败时保存相关堆栈；
- 文档审计只记录 `211/211`、接口 `48/48`；
- 前端只记录类型检查与构建是否通过；
- 真机只记录场景、输入、实际结果和关键截图/日志位置。

### 8.6 把稳定事实留在文件，不依赖聊天记忆

- 长期路线：本文件；
- 当前唯一任务与验收：`协同工作计划与验收反馈.md`；
- 真实进度数字：只读脚本输出；
- 数值、模型、接口和玩法：各自权威文档；
- 聊天内容只用于发出“选择任务、开始、验收、返修”的短指令。

### 8.7 每次通过后维护一个短指针

建议在本文件顶部只维护三行：

```text
最后通过：B0
当前任务：B1
下一候选：B2
```

新会话先读这三行和对应任务段即可恢复工作，不必重放全部历史。

## 9. 全项目最终完成定义

只有同时满足以下条件，才能把本清单标为全部完成：

- 第 4、5 节所有任务均已通过，或经用户明确取消并写明理由；
- B 类未实现常量清单清零，A 类每项有明确处置理由；
- 后端全量测试、前端构建、接口账本、文档声明、静态总检与 `git diff --check` 全绿；
- 新存档冷启动、中期发展、多星拓荒、终局决战及轨道轰炸恢复链完成真机演练；
- 在线与离线关键状态转换结果等价；
- 旧存档导入、缺字段默认值和多槽位隔离通过；
- README、进度看板、权威文档和实际代码不再互相冲突；
- 不存在界面宣称生效、代码却没有消费的机制；
- 不存在玩家遇到静默停摆却没有明确下一步引导的链路。

## 10. 本次快照的来源与限制

本清单综合自：

- `AGENTS.md`；
- `README.md`；
- `开发进展与后续计划.md`；
- `协同工作计划与验收反馈.md`；
- `数值平衡表.md`；
- `数据库设计定稿.md`；
- `代码结构与核心工程详细设计规范.md`；
- `挂机游戏详细设计与开发需求文档.md`；
- `需求细分与功能模块拆解.md`；
- 2026-09-19 的 B4 第 2 轮后端测试与静态审计结果。

它是一份“当前已知全部任务”的冻结快照。未来如果真机、测试或代码审计发现新的真实缺陷，应新增有证据的任务；不得为了保持轮数不变而忽略缺陷，也不得在没有证据时凭猜测扩张范围。
